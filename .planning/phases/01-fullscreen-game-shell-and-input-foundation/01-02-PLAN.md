---
phase: 01-fullscreen-game-shell-and-input-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - apps/web/src/components/game/useGameControls.ts
  - apps/web/src/components/game/useGameControls.test.tsx
  - apps/web/src/components/game/AnalogPad.tsx
  - apps/web/src/components/game/AnalogPad.test.tsx
  - apps/web/src/components/game/ActionButton.tsx
  - apps/web/src/components/game/ActionButton.test.tsx
autonomous: true
requirements:
  - CTRL-02
  - CTRL-04
  - ARCH-01
user_setup: []
must_haves:
  truths:
    - "Player can drag an analog control that outputs normalized x, y, and magnitude values."
    - "Player can press/release primary and secondary action controls without browser scroll gestures taking over."
    - "Keyboard users can produce equivalent analog, primary, and secondary input intents."
    - "Input state resets to neutral on cancellation, blur, visibility loss, and unmount."
  artifacts:
    - path: "apps/web/src/components/game/useGameControls.ts"
      provides: "Keyboard, sequence, timestamp, input emission, and reset lifecycle"
    - path: "apps/web/src/components/game/AnalogPad.tsx"
      provides: "Reusable pointer-capture analog primitive"
    - path: "apps/web/src/components/game/ActionButton.tsx"
      provides: "Reusable fixed-size press/release action primitive"
  key_links:
    - from: "apps/web/src/components/game/useGameControls.ts"
      to: "packages/shared/src/game.ts"
      via: "RaceGameInput and clampRaceAnalogVector imports"
      pattern: "RaceGameInput"
    - from: "apps/web/src/components/game/AnalogPad.tsx"
      to: "apps/web/src/components/game/useGameControls.ts"
      via: "onVectorChange callback shape"
      pattern: "RaceAnalogVector"
---

<objective>
Create reusable touch, pointer, mouse, and keyboard control primitives for fullscreen race play.

Purpose: CTRL-02 and CTRL-04 require fixed thumb controls with desktop parity. This plan implements D-06 through D-15 and D-21 through D-24 as reusable client-side primitives that emit shared `RaceGameInput` intents.
Output: `useGameControls`, `AnalogPad`, `ActionButton`, and focused tests.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/1-CONTEXT.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-UI-SPEC.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-VALIDATION.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-PATTERNS.md
@packages/shared/src/game.ts
@apps/web/src/lib/useLiveRaceSocket.ts
@apps/web/src/components/RetroRaceView.tsx
@apps/web/src/lib/useLobbySocket.test.ts

<interfaces>
Expected shared exports from Plan 01:
```typescript
export interface RaceAnalogVector {
  x: number;
  y: number;
  magnitude: number;
}
export type RaceGameInput =
  | RaceAnalogInput
  | RaceButtonInput
  | RaceActionInput;
export function clampRaceAnalogVector(vector: RaceAnalogVector): RaceAnalogVector;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 control tests</name>
  <files>apps/web/src/components/game/useGameControls.test.tsx, apps/web/src/components/game/AnalogPad.test.tsx, apps/web/src/components/game/ActionButton.test.tsx</files>
  <read_first>
    - apps/web/src/lib/useLiveRaceSocket.ts
    - apps/web/src/components/RetroRaceView.tsx
    - apps/web/src/lib/useLobbySocket.test.ts
    - packages/shared/src/game.ts
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-VALIDATION.md
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `useGameControls` maps `ArrowLeft` and `a` to an analog input with negative `x`, `ArrowRight` and `d` to positive `x`, `ArrowUp` and `w` to negative `y`, and `ArrowDown` and `s` to positive `y`.
    - Test 2: `useGameControls` maps `Space` and `Enter` to primary button pressed/released input, and `Shift` and `Control` to secondary button pressed/released input.
    - Test 3: `useGameControls` increments `sequence` on emitted inputs and includes numeric `clientTimeMs`.
    - Test 4: `useGameControls` emits or stores a neutral analog vector `{ x: 0, y: 0, magnitude: 0 }` on `window.blur`, `document.visibilitychange` to hidden, and unmount.
    - Test 5: `AnalogPad` calls `onVectorChange` with clamped `{ x, y, magnitude }` during pointer drag and calls neutral vector on `pointerup`, `pointercancel`, and `lostpointercapture`.
    - Test 6: `ActionButton` calls `onPressedChange(true)` on pointer down and keyboard activation, calls `onPressedChange(false)` on pointer up/cancel/lost capture/key up, and does nothing when `disabled` is true.
  </behavior>
  <action>Create the three test files. Use Vitest, `@testing-library/react`, `fireEvent`, `render`, `renderHook`, and `act`; do not add `@testing-library/user-event` unless existing `fireEvent` cannot express the required events. Use test names exactly `maps keyboard fallback to race game inputs`, `resets active input on cancellation lifecycle`, `emits normalized analog vector from pointer movement`, and `emits action button press and release states`. Per D-10 and T1-02, include pointer cancel and lost pointer capture cases in tests before implementation.</action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- useGameControls.test.tsx AnalogPad.test.tsx ActionButton.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/game/useGameControls.test.tsx` contains `maps keyboard fallback to race game inputs`.
    - `apps/web/src/components/game/useGameControls.test.tsx` contains `resets active input on cancellation lifecycle`.
    - `apps/web/src/components/game/AnalogPad.test.tsx` contains `emits normalized analog vector from pointer movement`.
    - `apps/web/src/components/game/ActionButton.test.tsx` contains `emits action button press and release states`.
    - The first run of `pnpm --filter @blitz/web test -- useGameControls.test.tsx AnalogPad.test.tsx ActionButton.test.tsx` fails before Task 2 because the components/hook do not exist yet.
  </acceptance_criteria>
  <done>Wave 0 control tests exist and fail only because the implementation files are missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reusable controls and input lifecycle</name>
  <files>apps/web/src/components/game/useGameControls.ts, apps/web/src/components/game/AnalogPad.tsx, apps/web/src/components/game/ActionButton.tsx</files>
  <read_first>
    - apps/web/src/components/game/useGameControls.test.tsx
    - apps/web/src/components/game/AnalogPad.test.tsx
    - apps/web/src/components/game/ActionButton.test.tsx
    - packages/shared/src/game.ts
    - apps/web/src/lib/useLiveRaceSocket.ts
    - apps/web/src/components/RetroRaceView.tsx
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-UI-SPEC.md
  </read_first>
  <action>Create `useGameControls.ts` exporting `createNeutralAnalogVector()`, `combineKeyboardVector(keys: Set<string>)`, and `useGameControls(options)`. The hook options must be `{ modeId: RaceShellModeId; enabled?: boolean; onInput: (input: RaceGameInput) => void; }`. It must track `sequence` in a ref starting at 0, stamp every emitted input with `clientTimeMs: Date.now()`, emit `kind: 'analog'` for analog changes, `kind: 'button'` for primary/secondary pressed/released changes, and `kind: 'action'` for one-shot named actions when called. Add handlers returned from the hook: `setAnalogVector(vector)`, `setButtonState(button, pressed)`, `emitAction(action)`, `resetInput()`, and state `{ analog, primaryPressed, secondaryPressed }`. Add `window` keydown/keyup listeners: WASD/Arrow keys update analog, Space/Enter control primary, Shift/Control control secondary. Ignore repeated keydown events for button pressed repeats. Add reset listeners for `window.blur` and `document.visibilitychange` when `document.visibilityState === 'hidden'`, and call `resetInput()` in the effect cleanup. Create `AnalogPad.tsx` with props `{ label?: string; value: RaceAnalogVector; disabled?: boolean; onVectorChange: (vector: RaceAnalogVector) => void; }`; use Pointer Events, call `setPointerCapture(event.pointerId)` when available, calculate vector from the pad center using `getBoundingClientRect()`, clamp with `clampRaceAnalogVector`, and reset to neutral on pointer up/cancel/lost capture. Create `ActionButton.tsx` with props `{ label: string; button: RaceGameButton; pressed?: boolean; disabled?: boolean; onPressedChange: (pressed: boolean) => void; }`; use pointer down/up/cancel/lost capture and keyboard Space/Enter to call `onPressedChange`. Add stable class names `game-analog-pad`, `game-analog-knob`, `game-action-button`, `game-action-button--primary`, `game-action-button--secondary`, and `is-pressed`; styling comes in Plan 03. Per D-06 through D-15 and D-21 through D-24, do not add one-handed mode, haptics, sound, or per-mode physics.</action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- useGameControls.test.tsx AnalogPad.test.tsx ActionButton.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/game/useGameControls.ts` contains `export function createNeutralAnalogVector`.
    - `apps/web/src/components/game/useGameControls.ts` contains `window.addEventListener('blur'`.
    - `apps/web/src/components/game/useGameControls.ts` contains `document.addEventListener('visibilitychange'`.
    - `apps/web/src/components/game/useGameControls.ts` contains `clientTimeMs: Date.now()`.
    - `apps/web/src/components/game/AnalogPad.tsx` contains `setPointerCapture`.
    - `apps/web/src/components/game/AnalogPad.tsx` contains `onLostPointerCapture`.
    - `apps/web/src/components/game/ActionButton.tsx` contains `onPointerCancel`.
    - `pnpm --filter @blitz/web test -- useGameControls.test.tsx AnalogPad.test.tsx ActionButton.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Controls emit shared typed intent, keyboard fallback exists, and all cancellation paths reset input to neutral.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| physical input->React state | Browser pointer/keyboard events become local control state. |
| React controls->Socket.IO emit callback | Local state becomes compact client intent; it must not encode authoritative results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T1-02 | Denial of Service / Tampering | `useGameControls`, `AnalogPad`, `ActionButton` | mitigate | Reset input on pointer up, pointer cancel, lost pointer capture, window blur, visibility hidden, and hook unmount; tests must cover all listed paths. |
| T1-04 | Tampering | `useGameControls` emitted input | mitigate | Emit only `RaceGameInput` intent fields: `kind`, `modeId`, `sequence`, `clientTimeMs`, vector/button/action; do not emit positions, ranks, finish times, or server snapshot fields. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/web test -- useGameControls.test.tsx AnalogPad.test.tsx ActionButton.test.tsx`.
</verification>

<success_criteria>
- CTRL-02 is covered by pointer-safe reusable controls and stuck-input reset tests.
- CTRL-04 is covered by keyboard fallback tests for analog, primary, and secondary controls.
- ARCH-01 is consumed by importing shared race input contracts from `@blitz/shared`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-02-SUMMARY.md`
</output>
