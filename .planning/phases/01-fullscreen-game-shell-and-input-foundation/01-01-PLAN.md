---
phase: 01-fullscreen-game-shell-and-input-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/game.ts
  - packages/shared/src/contracts.ts
  - packages/shared/src/contracts.test.ts
autonomous: true
requirements:
  - ARCH-01
user_setup: []
must_haves:
  truths:
    - "Shared exports contain discriminated race input shapes for analog vectors, button state changes, and action events."
    - "Shared exports contain a common race shell snapshot shape for fullscreen HUD/rendering consumers."
    - "Generic session input transport remains compatible with existing lights, penalty, and current race runtimes."
  artifacts:
    - path: "packages/shared/src/game.ts"
      provides: "RaceGameInput, RaceShellSnapshot, race mode/status/control constants, and runtime guard helpers"
    - path: "packages/shared/src/contracts.ts"
      provides: "GameInputPayload compatibility while exposing explicit race input contracts"
    - path: "packages/shared/src/contracts.test.ts"
      provides: "ARCH-01 contract smoke tests"
  key_links:
    - from: "packages/shared/src/contracts.ts"
      to: "packages/shared/src/game.ts"
      via: "type imports"
      pattern: "RaceGameInput"
    - from: "packages/shared/src/contracts.test.ts"
      to: "packages/shared/src/index.ts"
      via: "public barrel import"
      pattern: "RaceShellSnapshot"
---

<objective>
Create the shared contract foundation for rebuilt race controls and fullscreen race snapshots.

Purpose: ARCH-01 requires explicit input and snapshot shapes before later race phases add drag, dodge, circle, and figure-eight mechanics. This plan implements D-25 through D-29 without moving gameplay authority to the browser.
Output: Shared TypeScript contracts and tests that downstream web/server code can import from `@blitz/shared`.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/1-CONTEXT.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-UI-SPEC.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-RESEARCH.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-VALIDATION.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-PATTERNS.md
@packages/shared/src/game.ts
@packages/shared/src/contracts.ts
@packages/shared/src/contracts.test.ts

<interfaces>
Existing public contract boundary:
```typescript
export type GameInputPayload = Record<string, unknown>;
export interface GameSessionEnvelope<TState = GameInputPayload | null> {
  sessionId: string;
  lobbyCode: string;
  game: PartyGame;
  variant: PartyGameVariant;
  status: GameSessionStatus;
  countdown: number | null;
  state: TState;
  results: GameResults | null;
}
export interface PlayerInput {
  tick: number;
  steer: SteeringInput;
  accelerate: boolean;
  brake: boolean;
}
```

Required new public shape:
```typescript
export type RaceShellModeId = 'drag' | 'dodge' | 'circle' | 'figure-eight';
export type RaceGameInput =
  | RaceAnalogInput
  | RaceButtonInput
  | RaceActionInput;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 shared contract tests</name>
  <files>packages/shared/src/contracts.test.ts</files>
  <read_first>
    - packages/shared/src/contracts.test.ts
    - packages/shared/src/game.ts
    - packages/shared/src/contracts.ts
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/1-CONTEXT.md
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-VALIDATION.md
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: A `RaceGameInput` analog sample has `kind: 'analog'`, `sequence`, `clientTimeMs`, `modeId: 'circle'`, and vector `{ x: 0.5, y: -0.25, magnitude: 0.56 }`, with no `playerId` key.
    - Test 2: A `RaceGameInput` button sample has `kind: 'button'`, `button: 'primary'`, and `state: 'pressed'`.
    - Test 3: A `RaceGameInput` action sample has `kind: 'action'`, `action: 'shift'`, and `sequence`.
    - Test 4: A `RaceShellSnapshot` sample has `modeId: 'figure-eight'`, `status: 'racing'`, `countdown`, `players`, `hud.objective`, `hud.inputLabel`, and `mode: {}`.
    - Test 5: The existing `PlayerInput` sample still compiles and `GameInputPayload` still accepts existing non-race payload shapes such as `{ reactionAtMs: 123 }`.
  </behavior>
  <action>Add imports from `./index.js` for `RaceGameInput`, `RaceShellSnapshot`, `RaceShellModeId`, `RaceShellStatus`, `RaceGameInputKind`, and `GameInputPayload`. Add tests named exactly `exports discriminated race game input contract shapes` and `exports race shell snapshot contract shape`. Keep existing tests intact. Per D-25, D-26, D-27, D-28, and D-29, tests must assert the discriminants and common shell fields listed in the behavior block before implementation.</action>
  <verify>
    <automated>pnpm --filter @blitz/shared test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/contracts.test.ts` contains `exports discriminated race game input contract shapes`.
    - `packages/shared/src/contracts.test.ts` contains `exports race shell snapshot contract shape`.
    - `packages/shared/src/contracts.test.ts` contains `kind: 'analog'`.
    - `packages/shared/src/contracts.test.ts` contains `modeId: 'figure-eight'`.
    - The first run of `pnpm --filter @blitz/shared test` fails before Task 2 because new exports do not exist yet.
  </acceptance_criteria>
  <done>Wave 0 shared contract tests exist and fail only because the new contract exports are missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared race input and shell snapshot contracts</name>
  <files>packages/shared/src/game.ts, packages/shared/src/contracts.ts</files>
  <read_first>
    - packages/shared/src/game.ts
    - packages/shared/src/contracts.ts
    - packages/shared/src/contracts.test.ts
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/1-CONTEXT.md
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-UI-SPEC.md
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-RESEARCH.md
  </read_first>
  <action>In `packages/shared/src/game.ts`, add constants and exported types with these exact names: `RACE_SHELL_MODE_IDS`, `RaceShellModeId`, `RACE_SHELL_STATUS`, `RaceShellStatus`, `RACE_GAME_INPUT_KIND`, `RaceGameInputKind`, `RACE_GAME_BUTTONS`, `RaceGameButton`, `RACE_GAME_BUTTON_STATES`, `RaceGameButtonState`, `RaceAnalogVector`, `RaceInputBase`, `RaceAnalogInput`, `RaceButtonInput`, `RaceActionInput`, `RaceGameInput`, `RaceShellPlayer`, `RaceShellHud`, and `RaceShellSnapshot`. Use these literal values: modes `drag`, `dodge`, `circle`, `figure-eight`; statuses `countdown`, `racing`, `finished`; input kinds `analog`, `button`, `action`; buttons `primary`, `secondary`; button states `pressed`, `released`. Add `export function clampRaceAnalogVector(vector: RaceAnalogVector): RaceAnalogVector` that clamps `x` and `y` to `-1..1`, clamps `magnitude` to `0..1`, converts non-finite numbers to `0`, and rounds with `Math.round(value * 1000) / 1000`. Add `export function isRaceGameInput(input: unknown): input is RaceGameInput` that checks object shape, `kind`, finite non-negative `sequence`, finite non-negative `clientTimeMs`, and kind-specific fields; for analog, call `clampRaceAnalogVector` only for validation and require `vector` to be an object. In `packages/shared/src/contracts.ts`, import `RaceGameInput` and change `GameInputPayload` to `RaceGameInput | PlayerInput | Record<string, unknown>` so existing lights/penalty/current-race inputs still type-check. Do not remove stale race socket event constants in this phase; the concerns file marks consolidation as a separate risk. Per T1-01, document in code with one short comment above `isRaceGameInput` that shared guards help runtimes reject malformed race input but Socket.IO TypeScript types are not server validation by themselves.</action>
  <verify>
    <automated>pnpm --filter @blitz/shared test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/game.ts` contains `export const RACE_GAME_INPUT_KIND`.
    - `packages/shared/src/game.ts` contains `export interface RaceAnalogInput`.
    - `packages/shared/src/game.ts` contains `export interface RaceShellSnapshot`.
    - `packages/shared/src/game.ts` contains `export function clampRaceAnalogVector`.
    - `packages/shared/src/game.ts` contains `export function isRaceGameInput`.
    - `packages/shared/src/contracts.ts` contains `RaceGameInput | PlayerInput | Record<string, unknown>`.
    - `pnpm --filter @blitz/shared test` exits 0.
  </acceptance_criteria>
  <done>Shared package exports explicit race input/snapshot contracts, existing generic inputs remain compatible, and shared tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->shared contract | Browser code imports shared types and helpers, but local TypeScript cannot prove runtime payload integrity. |
| browser->Socket.IO server | Future `client:game-input` packets cross from untrusted browser to authoritative server runtimes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T1-01 | Tampering | `packages/shared/src/game.ts` race input contracts | mitigate | Add discriminated input unions and `isRaceGameInput(input: unknown)` runtime guard; keep comment that later server runtimes must still check discriminants and clamp values before trusting input. |
| T1-04 | Tampering | `GameInputPayload` session input transport | mitigate | Keep payloads as player intent only; do not add any client-supplied result, ranking, or authoritative position fields to `RaceGameInput`. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/shared test`. Final phase verification later runs `pnpm test` and `pnpm run build`.
</verification>

<success_criteria>
- ARCH-01 is satisfied for Phase 1 by exported shared race input and snapshot foundations.
- Existing shared contract tests and newly added Wave 0 contract tests pass.
- No database schema or persistence files are modified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-01-SUMMARY.md`
</output>
