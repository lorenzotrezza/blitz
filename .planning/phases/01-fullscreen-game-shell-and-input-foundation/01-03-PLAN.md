---
phase: 01-fullscreen-game-shell-and-input-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - apps/web/src/components/game/FullscreenGameShell.tsx
  - apps/web/src/components/game/FullscreenGameShell.test.tsx
  - apps/web/src/components/game/GameViewport.tsx
  - apps/web/src/components/game/GameHud.tsx
  - apps/web/src/components/game/GameHud.test.tsx
  - apps/web/src/components/game/GameStates.tsx
  - apps/web/src/styles.css
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - ARCH-01
user_setup: []
must_haves:
  truths:
    - "Race session UI can render as a fullscreen mobile-first surface without normal app chrome."
    - "The track/canvas viewport remains the main visual surface."
    - "HUD status is React-rendered text with objective, speed/progress, penalty/status, current input, and a mode metric."
    - "Gameplay CSS uses safe areas, dynamic viewport height, fixed thumb zones, and no page scrolling."
  artifacts:
    - path: "apps/web/src/components/game/FullscreenGameShell.tsx"
      provides: "Route-level fullscreen shell layout"
    - path: "apps/web/src/components/game/GameViewport.tsx"
      provides: "Stable track/canvas slot"
    - path: "apps/web/src/components/game/GameHud.tsx"
      provides: "Inspectable HUD and polite live region"
    - path: "apps/web/src/components/game/GameStates.tsx"
      provides: "Phase 1 empty/error state copy"
    - path: "apps/web/src/styles.css"
      provides: "Fullscreen gameplay, HUD, viewport, and controls styling"
  key_links:
    - from: "apps/web/src/components/game/GameHud.tsx"
      to: "packages/shared/src/game.ts"
      via: "RaceShellSnapshot type"
      pattern: "RaceShellSnapshot"
    - from: "apps/web/src/styles.css"
      to: "apps/web/src/components/game/FullscreenGameShell.tsx"
      via: "game-shell class names"
      pattern: "game-shell"
---

<objective>
Create the fullscreen gameplay shell, viewport, HUD, empty/error states, and gameplay CSS foundation.

Purpose: CTRL-01 and CTRL-03 require a mobile-first fullscreen route surface with visible, testable HUD text. This plan implements D-01 through D-05 and D-16 through D-20 while adding CSS support for the control primitives from Plan 02.
Output: Shell/HUD components, tests, and global gameplay CSS.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/1-CONTEXT.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-UI-SPEC.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-VALIDATION.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-PATTERNS.md
@packages/shared/src/game.ts
@apps/web/src/pages/SprintCircuitPage.tsx
@apps/web/src/pages/SprintCircuitPage.test.tsx
@apps/web/src/styles.css

<interfaces>
Expected shared snapshot from Plan 01:
```typescript
export interface RaceShellSnapshot extends Record<string, unknown> {
  sessionId: string;
  modeId: RaceShellModeId;
  status: RaceShellStatus;
  countdown: number | null;
  players: RaceShellPlayer[];
  hud: RaceShellHud;
  mode: Record<string, unknown>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 shell and HUD tests</name>
  <files>apps/web/src/components/game/FullscreenGameShell.test.tsx, apps/web/src/components/game/GameHud.test.tsx</files>
  <read_first>
    - apps/web/src/pages/SprintCircuitPage.test.tsx
    - apps/web/src/pages/SprintCircuitPage.tsx
    - apps/web/src/styles.css
    - packages/shared/src/game.ts
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-UI-SPEC.md
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-VALIDATION.md
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `FullscreenGameShell` renders a root with class `game-shell`, a viewport region, HUD slot, controls slot, and no elements with classes `panel`, `card`, `viewport`, or `topbar`.
    - Test 2: `FullscreenGameShell` can render empty state copy `Waiting for race` and `No live snapshot yet. Keep this screen open; the countdown appears when the server starts the session.`
    - Test 3: `GameHud` renders objective, progress label, speed label, penalty label, input label, mode metric label/value, and session/countdown status as text.
    - Test 4: `GameHud` includes an element with `aria-live="polite"` for countdown/status/penalty/finish changes.
  </behavior>
  <action>Create `FullscreenGameShell.test.tsx` and `GameHud.test.tsx` using Vitest, Testing Library React, and `@testing-library/jest-dom/vitest`. Test names must be exactly `renders fullscreen shell without app chrome classes`, `renders waiting empty state copy`, `renders common gameplay hud text`, and `marks status updates as polite live text`. Use a `RaceShellSnapshot` fixture with `modeId: 'drag'`, `status: 'racing'`, `countdown: 0`, `hud.objective: 'Reach the finish'`, `hud.progressLabel: '54%'`, `hud.speedLabel: '220 km/h'`, `hud.penaltyLabel: 'Clean'`, `hud.inputLabel: 'Analog neutral'`, `hud.modeMetricLabel: 'Gear'`, and `hud.modeMetricValue: '2'`.</action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- FullscreenGameShell.test.tsx GameHud.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/game/FullscreenGameShell.test.tsx` contains `renders fullscreen shell without app chrome classes`.
    - `apps/web/src/components/game/FullscreenGameShell.test.tsx` contains `Waiting for race`.
    - `apps/web/src/components/game/GameHud.test.tsx` contains `renders common gameplay hud text`.
    - `apps/web/src/components/game/GameHud.test.tsx` contains `aria-live`.
    - The first run of `pnpm --filter @blitz/web test -- FullscreenGameShell.test.tsx GameHud.test.tsx` fails before Task 2 because components do not exist yet.
  </acceptance_criteria>
  <done>Wave 0 shell/HUD tests exist and fail only because shell/HUD implementation files are missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fullscreen shell, HUD, states, viewport, and gameplay CSS</name>
  <files>apps/web/src/components/game/FullscreenGameShell.tsx, apps/web/src/components/game/GameViewport.tsx, apps/web/src/components/game/GameHud.tsx, apps/web/src/components/game/GameStates.tsx, apps/web/src/styles.css</files>
  <read_first>
    - apps/web/src/components/game/FullscreenGameShell.test.tsx
    - apps/web/src/components/game/GameHud.test.tsx
    - apps/web/src/styles.css
    - packages/shared/src/game.ts
    - apps/web/src/pages/SprintCircuitPage.tsx
    - .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-UI-SPEC.md
  </read_first>
  <action>Create `FullscreenGameShell.tsx` with props `{ hud: ReactNode; viewport: ReactNode; controls: ReactNode; stateOverlay?: ReactNode; backHref?: string; backLabel?: string; }`, root `<section className="game-shell" aria-label="Fullscreen race session">`, child class names `game-shell__hud`, `game-shell__viewport`, `game-shell__controls`, and optional `game-shell__state`. Do not render `.panel`, `.card`, `.viewport`, `.topbar`, `.scanlines`, or `.shell-glow`. Create `GameViewport.tsx` with props `{ children?: ReactNode; label: string; aspectRatio?: string; }`, render `<div className="game-viewport" role="img" aria-label={label}>`, and use a nested `game-viewport__stage`; it must support canvas children and not draw by itself. Create `GameHud.tsx` with prop `{ snapshot: RaceShellSnapshot | null; fallbackSessionId: string; }`, render React text fields for `Mode`, `Objective`, `Progress`, `Speed`, `Penalty`, `Input`, and the mode metric, plus a `game-hud__live` element with `aria-live="polite"`. Create `GameStates.tsx` exporting `GameEmptyState` and `GameErrorState` using exact UI-SPEC copy: heading `Waiting for race`, body `No live snapshot yet. Keep this screen open; the countdown appears when the server starts the session.`, error `Connection lost. Rejoin from the lobby or refresh this session.` In `apps/web/src/styles.css`, append gameplay classes using exact contract values: `.game-shell { position: fixed; inset: 0; width: 100vw; min-height: 100dvh; overflow: hidden; background: #06060d; color: #ddeeff; padding: max(16px, env(safe-area-inset-top)) max(16px, env(safe-area-inset-right)) max(24px, env(safe-area-inset-bottom)) max(16px, env(safe-area-inset-left)); }`, `.game-shell__viewport` centered, `.game-shell__controls` fixed/docked bottom with bottom-left analog and bottom-right actions, `.game-analog-pad { min-width: 144px; min-height: 144px; touch-action: none; user-select: none; -webkit-user-select: none; }`, portrait media query reducing analog to `128px`, `.game-action-button { min-width: 72px; min-height: 72px; touch-action: none; user-select: none; -webkit-user-select: none; }`, focus ring `outline: 3px solid #ffd700`, typography sizes only `12px`, `16px`, `20px`, and `28px` in gameplay selectors, and `letter-spacing: 0`. Per D-05, include safe-area `env()` values. Per D-20 and T1-03, do not put critical HUD values inside canvas-only rendering.</action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- FullscreenGameShell.test.tsx GameHud.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/game/FullscreenGameShell.tsx` contains `className="game-shell"`.
    - `apps/web/src/components/game/FullscreenGameShell.tsx` does not contain `panel`.
    - `apps/web/src/components/game/GameHud.tsx` contains `aria-live="polite"`.
    - `apps/web/src/components/game/GameStates.tsx` contains `Waiting for race`.
    - `apps/web/src/styles.css` contains `min-height: 100dvh`.
    - `apps/web/src/styles.css` contains `touch-action: none`.
    - `apps/web/src/styles.css` contains `env(safe-area-inset-bottom)`.
    - `apps/web/src/styles.css` contains `min-width: 144px`.
    - `pnpm --filter @blitz/web test -- FullscreenGameShell.test.tsx GameHud.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Fullscreen shell and HUD components exist, gameplay CSS enforces the mobile/no-scroll/control contract, and focused tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server snapshot->React HUD | Server state becomes user-visible gameplay status. |
| React route->canvas | Visual track rendering may be inaccessible if status is canvas-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T1-03 | Information Disclosure / Accessibility | `GameHud`, `FullscreenGameShell` | mitigate | Render objective, progress, speed, penalty/status, input state, countdown/status, and mode metric as React text; add `aria-live="polite"` for status updates. |
| T1-02 | Denial of Service / Tampering | `apps/web/src/styles.css` gameplay controls | mitigate | Apply `touch-action: none`, stable fixed-size controls, and no-scroll fullscreen containment so browser panning does not interrupt active gameplay controls. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/web test -- FullscreenGameShell.test.tsx GameHud.test.tsx`.
</verification>

<success_criteria>
- CTRL-01 has a fullscreen shell with no normal app chrome classes and no scroll-oriented panel layout.
- CTRL-03 has inspectable HUD text and live status semantics.
- CTRL-02 has CSS support for fixed, pointer-safe thumb controls.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-03-SUMMARY.md`
</output>
