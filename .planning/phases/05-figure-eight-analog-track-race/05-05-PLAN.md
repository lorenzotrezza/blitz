---
phase: 05-figure-eight-analog-track-race
plan: 05
type: execute
wave: 4
depends_on:
  - 05-03
  - 05-04
files_modified:
  - apps/web/src/lib/sessionRoutes.ts
  - apps/web/src/lib/sessionRoutes.test.ts
  - apps/web/src/app/router.tsx
  - apps/web/src/app/router.test.tsx
  - apps/web/src/pages/ResultsPage.tsx
  - apps/web/src/pages/ResultsPage.test.tsx
autonomous: true
requirements:
  - ANLG-03
must_haves:
  truths:
    - "Starting a figure-eight race routes to the figure-eight fullscreen page."
    - "Figure-eight completion reaches results with lap and crossing penalty summary visible when present."
    - "Existing circular analog tests still pass after figure-eight changes."
  artifacts:
    - path: "apps/web/src/lib/sessionRoutes.ts"
      provides: "Variant-aware route for figure-eight sessions"
      contains: "figure-eight-track"
    - path: "apps/web/src/app/router.tsx"
      provides: "Top-level fullscreen figure-eight route outside AppLayout"
      contains: "FigureEightTrackPage"
    - path: "apps/web/src/pages/ResultsPage.tsx"
      provides: "Figure-eight summary rendering if generic summary support is insufficient"
      contains: "summary"
  key_links:
    - from: "apps/web/src/lib/sessionRoutes.ts"
      to: "apps/web/src/app/router.tsx"
      via: "figure-eight route path"
      pattern: "figure-eight"
    - from: "apps/server/src/games/race/figureEightTrack.ts"
      to: "apps/web/src/pages/ResultsPage.tsx"
      via: "SessionFinishedPayload.results.summary"
      pattern: "figure-eight-track"
---

<objective>
Wire figure-eight sessions into routing/results and run the phase regression gate.

Purpose: Make the new mode reachable from the existing session flow and verify it does not regress circular analog behavior.
Output: Variant-aware session route, top-level router entry, result summary coverage, and full verification.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-figure-eight-analog-track-race/05-CONTEXT.md
@.planning/phases/05-figure-eight-analog-track-race/05-VALIDATION.md
@.planning/phases/05-figure-eight-analog-track-race/05-03-SUMMARY.md
@.planning/phases/05-figure-eight-analog-track-race/05-04-SUMMARY.md
@packages/shared/src/lobby.ts
@packages/shared/src/contracts.ts
@apps/web/src/lib/sessionRoutes.ts
@apps/web/src/app/router.tsx
@apps/web/src/app/router.test.tsx
@apps/web/src/pages/ResultsPage.tsx
@apps/web/src/pages/ResultsPage.test.tsx
@apps/web/src/pages/CircleTrackPage.test.tsx
@apps/server/src/games/race/circleTrackRules.test.ts
@apps/server/src/games/race/circleTrack.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add routing and results tests</name>
  <files>apps/web/src/lib/sessionRoutes.test.ts, apps/web/src/app/router.test.tsx, apps/web/src/pages/ResultsPage.test.tsx</files>
  <read_first>
    apps/web/src/lib/sessionRoutes.test.ts
    apps/web/src/lib/sessionRoutes.ts
    apps/web/src/app/router.test.tsx
    apps/web/src/app/router.tsx
    apps/web/src/pages/ResultsPage.test.tsx
    apps/web/src/pages/ResultsPage.tsx
    apps/web/src/pages/FigureEightTrackPage.tsx
    packages/shared/src/lobby.ts
    packages/shared/src/contracts.ts
  </read_first>
  <behavior>
    - `resolveSessionRoute()` returns a figure-eight fullscreen path for `variant: PARTY_GAME_VARIANTS.figureEightTrack`.
    - Router renders `FigureEightTrackPage` at the figure-eight route outside app chrome.
    - Results page displays summary fields for `track: 'figure-eight-track'`, laps, penalties, and center cuts when present.
  </behavior>
  <action>
    Create `apps/web/src/lib/sessionRoutes.test.ts` if it does not exist, or extend existing route tests if Phase 4 created it. Add assertions that a `SessionStartedPayload` with `game: 'race'`, `variant: PARTY_GAME_VARIANTS.figureEightTrack`, and `sessionId: 'session-1'` resolves to a dedicated fullscreen path such as `/race/figure-eight/session-1`. Extend `apps/web/src/app/router.test.tsx` to mock or render `FigureEightTrackPage` and assert the route has `Fullscreen race session` and no `.topbar`, `.panel`, `.card`, or normal `.viewport`. Extend `apps/web/src/pages/ResultsPage.test.tsx` with a figure-eight `SessionFinishedPayload` containing `summary: { mode: 'Figure Eight', track: 'figure-eight-track', laps: 3, penaltiesByPlayer: 1, centerCutsByPlayer: 2, finishedPlayers: 1 }` and assert visible text includes `Figure Eight`, `figure-eight-track`, `3`, `1`, and `2` or the existing generic summary labels. Tests should fail before Task 2 if routing/results are not wired.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- sessionRoutes router ResultsPage</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "figure-eight-track" apps/web/src/lib/sessionRoutes.test.ts apps/web/src/app/router.test.tsx apps/web/src/pages/ResultsPage.test.tsx` prints matches in all three files. `grep -n "/race/figure-eight/session-1" apps/web/src/lib/sessionRoutes.test.ts apps/web/src/app/router.test.tsx` prints matches. The web package focused test command reaches RED before Task 2 if implementation is missing.
  </acceptance_criteria>
  <done>Routing and results expectations for figure-eight are captured in focused web tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire figure-eight route and result summary rendering</name>
  <files>apps/web/src/lib/sessionRoutes.ts, apps/web/src/lib/sessionRoutes.test.ts, apps/web/src/app/router.tsx, apps/web/src/app/router.test.tsx, apps/web/src/pages/ResultsPage.tsx, apps/web/src/pages/ResultsPage.test.tsx</files>
  <read_first>
    apps/web/src/lib/sessionRoutes.ts
    apps/web/src/lib/sessionRoutes.test.ts
    apps/web/src/app/router.tsx
    apps/web/src/app/router.test.tsx
    apps/web/src/pages/ResultsPage.tsx
    apps/web/src/pages/ResultsPage.test.tsx
    apps/web/src/pages/FigureEightTrackPage.tsx
    packages/shared/src/lobby.ts
    packages/shared/src/contracts.ts
  </read_first>
  <action>
    Update `apps/web/src/lib/sessionRoutes.ts` so `payload.game === 'race' && payload.variant === PARTY_GAME_VARIANTS.figureEightTrack` returns `/race/figure-eight/${payload.sessionId}`. Preserve existing circle-track, drag, sprint, lights, and penalty route behavior. Update `apps/web/src/app/router.tsx` to import `FigureEightTrackPage` and add a top-level route `{ path: '/race/figure-eight/:sessionId', element: <FigureEightTrackPage /> }` outside `AppLayout`, matching the fullscreen route pattern. In `apps/web/src/pages/ResultsPage.tsx`, if existing summary rendering is not already generic, add a compact summary list that renders key/value pairs from `payload.results.summary` using stable labels and values; this must display figure-eight `mode`, `track`, `laps`, `penaltiesByPlayer`, `centerCutsByPlayer`, and `finishedPlayers` when supplied. Do not perform broad lobby catalog copy/card polish, remove old variants, or redesign results beyond rendering supplied summary values.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- sessionRoutes router ResultsPage</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "PARTY_GAME_VARIANTS.figureEightTrack" apps/web/src/lib/sessionRoutes.ts` prints one match. `grep -n "/race/figure-eight" apps/web/src/lib/sessionRoutes.ts apps/web/src/app/router.tsx` prints matches in both files. `grep -n "FigureEightTrackPage" apps/web/src/app/router.tsx apps/web/src/app/router.test.tsx` prints matches in both files. `grep -n "centerCutsByPlayer\\|finishedPlayers" apps/web/src/pages/ResultsPage.tsx apps/web/src/pages/ResultsPage.test.tsx` prints matches in both files if summary rendering was added; if generic summary rendering already existed, tests must still assert these keys render. `pnpm --filter @blitz/web test -- sessionRoutes router ResultsPage` exits 0.
  </acceptance_criteria>
  <done>Figure-eight sessions route to the fullscreen page and results can display the runtime summary.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 5 regression gate</name>
  <files>No files modified by this task</files>
  <read_first>
    .planning/phases/05-figure-eight-analog-track-race/05-VALIDATION.md
    packages/shared/src/contracts.test.ts
    apps/server/src/games/race/circleTrackRules.test.ts
    apps/server/src/games/race/circleTrack.test.ts
    apps/server/src/games/race/figureEightTrackRules.test.ts
    apps/server/src/games/race/figureEightTrack.test.ts
    apps/web/src/pages/CircleTrackPage.test.tsx
    apps/web/src/pages/FigureEightTrackPage.test.tsx
    apps/web/src/game/circleTrackCanvas.test.ts
    apps/web/src/game/figureEightTrackCanvas.test.ts
  </read_first>
  <action>
    Run the required Phase 5 verification commands. The regression gate must explicitly preserve Phase 4 circular analog tests before claiming D-16 complete. If a focused circle test file from Phase 4 has a final equivalent name, run that equivalent and record it in the summary. Do not mark the phase complete if any command fails.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/shared test</automated>
    <automated>pnpm --filter @blitz/server test</automated>
    <automated>pnpm --filter @blitz/web test -- CircleTrackPage circleTrackCanvas FigureEightTrackPage figureEightTrackCanvas sessionRoutes router ResultsPage</automated>
    <automated>pnpm test</automated>
    <automated>pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    `pnpm --filter @blitz/shared test`, `pnpm --filter @blitz/server test`, `pnpm --filter @blitz/web test -- CircleTrackPage circleTrackCanvas FigureEightTrackPage figureEightTrackCanvas sessionRoutes router ResultsPage`, `pnpm test`, and `pnpm run build` all exit 0. The plan summary names the exact circular analog test files or final equivalents that passed.
  </acceptance_criteria>
  <done>Full phase verification is green, including existing circular analog regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session-started payload -> client route | Server-selected game/variant determines which fullscreen page opens. |
| session-finished payload -> results page | Server-owned summary data is displayed to players. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | Session routing | mitigate | Route only `PARTY_GAME_VARIANTS.figureEightTrack` to the figure-eight page; other variants keep their existing pages. |
| T-05-05-02 | Tampering | Results summary rendering | mitigate | Render summary values from server `SessionFinishedPayload`; do not let client route state create lap/checkpoint/finish authority. |
| T-05-05-03 | Denial of Service | Route/results UI | accept | Rendering fixed-size summary records and route matching is bounded; high-frequency input/snapshot spam is mitigated in runtime and page plans. |
| T-05-05-04 | Elevation of Privilege | End-to-end session flow | mitigate | Preserve server-authoritative `GameManager.applyInput()` path and validate final tests for wrong variant/mode behavior. |
</threat_model>

<verification>
Run the focused web routing/results tests, then shared/server/web package tests, `pnpm test`, and `pnpm run build`.
</verification>

<success_criteria>
Figure-eight is reachable, playable, finishable, result-visible, and does not break circular analog regression tests or root build/test gates.
</success_criteria>

<output>
After completion, create `.planning/phases/05-figure-eight-analog-track-race/05-05-SUMMARY.md`.
</output>
