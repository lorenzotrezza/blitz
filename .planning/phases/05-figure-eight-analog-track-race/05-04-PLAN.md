---
phase: 05-figure-eight-analog-track-race
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/web/src/game/figureEightTrackCanvas.ts
  - apps/web/src/game/figureEightTrackCanvas.test.ts
  - apps/web/src/pages/FigureEightTrackPage.tsx
  - apps/web/src/pages/FigureEightTrackPage.test.tsx
  - apps/web/src/styles.css
autonomous: true
requirements:
  - ANLG-03
must_haves:
  truths:
    - "The figure-eight page renders as a fullscreen mobile-first gameplay screen."
    - "The crossing looks intentional through bridge/underpass treatment, arrows, and next-path highlighting."
    - "HUD and track feedback expose next gate, lap, direction cue, crossing/course state, speed, and penalties from server snapshots."
  artifacts:
    - path: "apps/web/src/game/figureEightTrackCanvas.ts"
      provides: "Canvas drawing helper for figure-eight crossing and active route cues"
      contains: "drawFigureEightTrack"
    - path: "apps/web/src/pages/FigureEightTrackPage.tsx"
      provides: "Fullscreen figure-eight route page with analog controls"
      contains: "FigureEightTrackPage"
    - path: "apps/web/src/pages/FigureEightTrackPage.test.tsx"
      provides: "Page tests for HUD, controls, warnings, finish navigation, and no app chrome"
      contains: "Figure Eight"
  key_links:
    - from: "apps/web/src/pages/FigureEightTrackPage.tsx"
      to: "apps/web/src/lib/useGameSessionSocket.ts"
      via: "generic session hook submitInput"
      pattern: "useGameSessionSocket"
    - from: "apps/web/src/pages/FigureEightTrackPage.tsx"
      to: "packages/shared/src/game.ts"
      via: "RACE_SHELL_MODE_IDS.figureEight"
      pattern: "RACE_SHELL_MODE_IDS.figureEight"
---

<objective>
Create the mobile fullscreen figure-eight gameplay screen and crossing renderer.

Purpose: Make the crossing readable and playable on phones while keeping HUD state React-rendered and server-derived.
Output: Figure-eight canvas helper, page, styles, and web tests.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-UI-SPEC.md
@.planning/phases/05-figure-eight-analog-track-race/05-CONTEXT.md
@.planning/phases/05-figure-eight-analog-track-race/05-PATTERNS.md
@.planning/phases/05-figure-eight-analog-track-race/05-01-SUMMARY.md
@packages/shared/src/game.ts
@apps/web/src/components/game/FullscreenGameShell.tsx
@apps/web/src/components/game/GameViewport.tsx
@apps/web/src/components/game/GameHud.tsx
@apps/web/src/components/game/AnalogPad.tsx
@apps/web/src/components/game/useGameControls.ts
@apps/web/src/lib/useGameSessionSocket.ts
@apps/web/src/game/circleTrackCanvas.ts
@apps/web/src/pages/CircleTrackPage.tsx
@apps/web/src/styles.css

<interfaces>
From Phase 1 shell:
```typescript
export function FullscreenGameShell(props: { hud: ReactNode; viewport: ReactNode; controls: ReactNode; stateOverlay?: ReactNode }): JSX.Element;
export function GameViewport(props: { label: string; aspectRatio: string; children: ReactNode }): JSX.Element;
export function GameHud(props: { snapshot: RaceShellSnapshot | null; fallbackSessionId: string }): JSX.Element;
export function useGameControls(options: { modeId: RaceShellModeId; enabled: boolean; onInput: (input: RaceGameInput) => void }): ...;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add figure-eight canvas and page tests</name>
  <files>apps/web/src/game/figureEightTrackCanvas.test.ts, apps/web/src/pages/FigureEightTrackPage.test.tsx</files>
  <read_first>
    apps/web/src/game/figureEightTrackCanvas.test.ts
    apps/web/src/pages/FigureEightTrackPage.test.tsx
    apps/web/src/game/circleTrackCanvas.test.ts
    apps/web/src/pages/CircleTrackPage.test.tsx
    apps/web/src/pages/SprintCircuitPage.test.tsx
    apps/web/src/components/game/GameHud.test.tsx
    .planning/phases/01-fullscreen-game-shell-and-input-foundation/01-UI-SPEC.md
    .planning/phases/05-figure-eight-analog-track-race/05-CONTEXT.md
  </read_first>
  <behavior>
    - Canvas drawing exposes bridge/underpass crossing treatment and active next-path highlight.
    - Page renders `Fullscreen race session` with no `.panel`, `.card`, or normal `.viewport` chrome.
    - HUD text includes `Complete the figure eight`, `Lap 1/3`, `Next gate`, `Crossing`, `On course`, warning/penalty text, and speed.
    - Analog control emits `kind: 'analog'` with `modeId: 'figure-eight'`.
    - Finish payload is stored in `sessionStorage` and navigates to `/results/:sessionId`.
  </behavior>
  <action>
    Create `apps/web/src/game/figureEightTrackCanvas.test.ts` and `apps/web/src/pages/FigureEightTrackPage.test.tsx`. Canvas tests should use the same mock-context style as existing canvas/page tests and assert `FIGURE_EIGHT_CANVAS_SIZE`, `drawFigureEightTrack`, bridge/underpass drawing operations, active path highlight `#ffd700`, crossing warning/destructive color `#ff3333`, directional arrows, `Start`, `Left`, `Right`, `Center`, and next gate labeling. Page tests must mock `../lib/useGameSessionSocket`, render the page in router context, and assert exact copy/labels: `Figure Eight`, `Complete the figure eight`, `Lap 1/3`, `Next gate`, `Center eastbound`, `Crossing`, `On course`, `Wrong lobe`, and `42 km/h`. Assert one analog pad labelled `STEER`, no `.panel`, no `.card`, no normal `.viewport`, generic `submitInput` usage, `modeId: 'figure-eight'`, finish storage key `blitz-results:session-1`, and scoped styles containing `.figure-eight-track-page` or `.figure-eight-track-canvas` with fullscreen/no-scroll behavior. Tests should fail before Task 2.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- FigureEightTrackPage figureEightTrackCanvas</automated>
  </verify>
  <acceptance_criteria>
    `test -f apps/web/src/pages/FigureEightTrackPage.test.tsx` exits 0. `test -f apps/web/src/game/figureEightTrackCanvas.test.ts` exits 0. `grep -n "Complete the figure eight" apps/web/src/pages/FigureEightTrackPage.test.tsx` prints one match. `grep -n "modeId: 'figure-eight'" apps/web/src/pages/FigureEightTrackPage.test.tsx` prints one match. `grep -n "bridge\\|underpass\\|Center eastbound" apps/web/src/game/figureEightTrackCanvas.test.ts apps/web/src/pages/FigureEightTrackPage.test.tsx` prints matches. The web package focused test command reaches RED before Task 2.
  </acceptance_criteria>
  <done>Web tests specify visible crossing readability, HUD, analog control emission, and fullscreen behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement figure-eight renderer and fullscreen page</name>
  <files>apps/web/src/game/figureEightTrackCanvas.ts, apps/web/src/game/figureEightTrackCanvas.test.ts, apps/web/src/pages/FigureEightTrackPage.tsx, apps/web/src/pages/FigureEightTrackPage.test.tsx, apps/web/src/styles.css</files>
  <read_first>
    apps/web/src/game/figureEightTrackCanvas.ts
    apps/web/src/game/figureEightTrackCanvas.test.ts
    apps/web/src/pages/FigureEightTrackPage.tsx
    apps/web/src/pages/FigureEightTrackPage.test.tsx
    apps/web/src/game/circleTrackCanvas.ts
    apps/web/src/pages/CircleTrackPage.tsx
    apps/web/src/components/game/FullscreenGameShell.tsx
    apps/web/src/components/game/GameHud.tsx
    apps/web/src/components/game/AnalogPad.tsx
    apps/web/src/components/game/useGameControls.ts
    apps/web/src/lib/useGameSessionSocket.ts
    packages/shared/src/game.ts
    apps/web/src/styles.css
  </read_first>
  <action>
    Create `apps/web/src/game/figureEightTrackCanvas.ts` exporting `FIGURE_EIGHT_CANVAS_SIZE = { width: 560, height: 380 }` and `drawFigureEightTrack(context, snapshot)`. Draw the background `#06060d`, broad figure-eight road width from the snapshot/course or 96px fallback, inner/outer boundaries, one crossing path as a bridge with a light road stroke and shadow, the other as an underpass with a darker clipped/covered center segment, directional arrows through both lobes and the active center crossing, ordered gate labels `Start`, `Left`, `Center`, and `Right`, active next gate highlight `#ffd700`, warnings/penalties in `#ff3333`, and car markers from authoritative `snapshot.playersState`. Create `apps/web/src/pages/FigureEightTrackPage.tsx` using `useParams`, `useLocation`, `useNavigate`, `useGameSessionSocket`, `useGameControls`, `FullscreenGameShell`, `GameViewport`, `GameHud`, `GameEmptyState`, `GameErrorState`, and `AnalogPad`. Add `createFigureEightShellSnapshot(snapshot, sessionId, lobbyCode, countdown, inputLabel)` returning `RaceShellSnapshot` with `modeId: RACE_SHELL_MODE_IDS.figureEight`, objective `Complete the figure eight`, progress label `Lap X/3`, speed label `${Math.round(speedKmh)} km/h`, penalty label from server `courseState` such as `On course`, `Crossing`, `Wrong lobe`, `Wrong way`, `Center cut`, or `Penalty`, metric label `Next gate`, and metric value such as `Center eastbound`. Use `modeId: RACE_SHELL_MODE_IDS.figureEight` in `useGameControls`, enable controls only while server status is `RACE_STATUS.racing`, submit only generic `client:game-input` through `useGameSessionSocket`, reset controls on finish, store `window.sessionStorage.setItem(\`blitz-results:${finished.sessionId}\`, JSON.stringify(finished))`, and navigate to `/results/${finished.sessionId}`. Add only scoped CSS selectors such as `.figure-eight-track-page`, `.figure-eight-track-stage`, and `.figure-eight-track-canvas` to `apps/web/src/styles.css`; keep fullscreen fixed/no-scroll behavior, `touch-action: none` on active play surfaces, safe-area spacing, stable canvas aspect sizing, and 12px+ gameplay-critical text. Do not use `useLiveRaceSocket`, stale race-specific socket events, client-derived lap/checkpoint state, player collision/traffic visuals, broad global CSS rewrites, `.panel`, `.card`, or normal app chrome.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- FigureEightTrackPage figureEightTrackCanvas</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "export function drawFigureEightTrack" apps/web/src/game/figureEightTrackCanvas.ts` prints one match. `grep -n "FIGURE_EIGHT_CANVAS_SIZE" apps/web/src/game/figureEightTrackCanvas.ts apps/web/src/pages/FigureEightTrackPage.tsx` prints matches in both files. `grep -n "useGameSessionSocket" apps/web/src/pages/FigureEightTrackPage.tsx` prints one match. `grep -n "RACE_SHELL_MODE_IDS.figureEight" apps/web/src/pages/FigureEightTrackPage.tsx` prints one match. `grep -n "Complete the figure eight" apps/web/src/pages/FigureEightTrackPage.tsx` prints one match. `grep -n "figure-eight-track" apps/web/src/pages/FigureEightTrackPage.tsx apps/web/src/styles.css` prints matches in both files. `grep -n "useLiveRaceSocket" apps/web/src/pages/FigureEightTrackPage.tsx` exits 1. `pnpm --filter @blitz/web test -- FigureEightTrackPage figureEightTrackCanvas` exits 0.
  </acceptance_criteria>
  <done>Figure-eight page renders a readable crossing, server-derived HUD, analog controls, and fullscreen mobile layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser controls -> client:game-input | User gestures become untrusted socket payloads. |
| Server snapshot -> canvas/HUD | Authoritative state is rendered for players. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | `FigureEightTrackPage` input emission | mitigate | Emit only `RaceAnalogInput` through `useGameControls` with `modeId: RACE_SHELL_MODE_IDS.figureEight`; never emit lap/checkpoint/finish. |
| T-05-04-02 | Tampering | Figure-eight page/session hook | mitigate | Use generic `useGameSessionSocket` and figure-eight mode id; no sprint/circle fallback should receive figure-eight input. |
| T-05-04-03 | Denial of Service | Browser input cadence and rendering | mitigate | Reuse Phase 1 control lifecycle and draw from snapshots; do not add per-frame network loops in the canvas helper. |
| T-05-04-04 | Elevation of Privilege | Client HUD/canvas state | mitigate | Render lap, gate, speed, penalty, crossing, and finish from server snapshots only; runtime remains authoritative through `GameManager.applyInput()`. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/web test -- FigureEightTrackPage figureEightTrackCanvas`.
</verification>

<success_criteria>
The figure-eight gameplay page is fullscreen, mobile-first, crossing-readable, and submits only analog intent with `modeId: figure-eight`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-figure-eight-analog-track-race/05-04-SUMMARY.md`.
</output>
