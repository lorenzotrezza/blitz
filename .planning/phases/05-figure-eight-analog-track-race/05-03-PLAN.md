---
phase: 05-figure-eight-analog-track-race
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - apps/server/src/games/race/figureEightTrack.ts
  - apps/server/src/games/race/figureEightTrack.test.ts
  - apps/server/src/games/registry.ts
  - apps/server/src/games/registry.test.ts
autonomous: true
requirements:
  - ANLG-03
must_haves:
  truths:
    - "The server can start and run a race:figure-eight-track session."
    - "Only mapped players with valid figure-eight analog input mutate authoritative runtime state."
    - "Figure-eight results include rankings plus lap and penalty summary data."
  artifacts:
    - path: "apps/server/src/games/race/figureEightTrack.ts"
      provides: "createFigureEightTrackRuntime authoritative session runtime"
      contains: "createFigureEightTrackRuntime"
    - path: "apps/server/src/games/race/figureEightTrack.test.ts"
      provides: "Runtime tests for input validation, finish, and summary"
      contains: "ignores malformed or non-figure-eight race input"
    - path: "apps/server/src/games/registry.ts"
      provides: "race:figure-eight-track registry entry"
      contains: "race:figure-eight-track"
  key_links:
    - from: "apps/server/src/games/registry.ts"
      to: "apps/server/src/games/race/figureEightTrack.ts"
      via: "createFigureEightTrackRuntime import and registry entry"
      pattern: "race:figure-eight-track"
    - from: "apps/server/src/games/race/figureEightTrack.ts"
      to: "apps/server/src/games/race/figureEightTrackRules.ts"
      via: "advanceFigureEightPlayer"
      pattern: "advanceFigureEightPlayer"
---

<objective>
Wrap the figure-eight rules in an authoritative game runtime and register the race variant.

Purpose: Make ANLG-03 playable through the existing session manager without stale race-specific socket events.
Output: `createFigureEightTrackRuntime()`, runtime tests, and `race:figure-eight-track` registry support.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-figure-eight-analog-track-race/05-CONTEXT.md
@.planning/phases/05-figure-eight-analog-track-race/05-PATTERNS.md
@.planning/phases/05-figure-eight-analog-track-race/05-02-SUMMARY.md
@packages/shared/src/game.ts
@packages/shared/src/lobby.ts
@apps/server/src/games/runtime.ts
@apps/server/src/games/manager.ts
@apps/server/src/games/registry.ts
@apps/server/src/games/registry.test.ts
@apps/server/src/games/race/circleTrack.ts
@apps/server/src/games/race/figureEightTrackRules.ts

<interfaces>
From `apps/server/src/games/runtime.ts`:
```typescript
export interface GameRuntimeInstance<TState = unknown | null> {
  sessionId: string;
  lobbyCode: string;
  start(): GameSessionEnvelope<TState>;
  applyInput(playerId: string, input: GameInputPayload): GameSessionEnvelope<TState> | null;
  removePlayer(playerId: string): void;
  dispose(): void;
}
```

From Plan 05-02:
```typescript
export const FIGURE_EIGHT_TRACK_ID = 'figure-eight-track';
export function createFigureEightCourse(): FigureEightCourse;
export function createFigureEightPlayer(...): FigureEightTrackPlayerState;
export function advanceFigureEightPlayer(...): FigureEightTrackPlayerState;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add figure-eight runtime and registry tests</name>
  <files>apps/server/src/games/race/figureEightTrack.test.ts, apps/server/src/games/registry.test.ts</files>
  <read_first>
    apps/server/src/games/race/figureEightTrack.test.ts
    apps/server/src/games/race/circleTrack.test.ts
    apps/server/src/games/race/sprintCircuit.test.ts
    apps/server/src/games/registry.test.ts
    packages/shared/src/lobby.ts
    packages/shared/src/game.ts
  </read_first>
  <behavior>
    - Runtime starts countdown/active state with `variant: PARTY_GAME_VARIANTS.figureEightTrack`.
    - Valid analog input with `modeId: RACE_SHELL_MODE_IDS.figureEight` advances server-owned state.
    - Malformed input, non-figure mode input, and unknown player input are ignored.
    - Completion emits rankings and `summary.track === 'figure-eight-track'`.
    - Registry resolves `race:figure-eight-track`.
  </behavior>
  <action>
    Create `apps/server/src/games/race/figureEightTrack.test.ts` with tests named exactly: `starts a figure eight session with authoritative crossing state`, `applies valid analog input for figure eight mode`, `ignores malformed or non-figure-eight race input`, `ignores input from players outside the runtime`, and `finishes with rankings and crossing penalty summary`. Use a `LobbyState` fixture with `selectedGame: PARTY_GAMES.race`, `selectedVariant: PARTY_GAME_VARIANTS.figureEightTrack`, `settings.laps: 1`, and connected players. Extend `apps/server/src/games/registry.test.ts` to assert `registry.resolve(PARTY_GAMES.race, PARTY_GAME_VARIANTS.figureEightTrack)` is non-null and has key `race:figure-eight-track`. Tests must prove D-04, D-08, D-11, D-12, D-15, and server-authoritative D-16.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/server test</automated>
  </verify>
  <acceptance_criteria>
    `test -f apps/server/src/games/race/figureEightTrack.test.ts` exits 0. `grep -n "ignores malformed or non-figure-eight race input" apps/server/src/games/race/figureEightTrack.test.ts` prints one match. `grep -n "race:figure-eight-track" apps/server/src/games/registry.test.ts` prints one match. The server package test command reaches RED before Task 2.
  </acceptance_criteria>
  <done>Runtime and registry behavior is specified by failing tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement runtime adapter and registry entry</name>
  <files>apps/server/src/games/race/figureEightTrack.ts, apps/server/src/games/race/figureEightTrack.test.ts, apps/server/src/games/registry.ts, apps/server/src/games/registry.test.ts</files>
  <read_first>
    apps/server/src/games/race/figureEightTrack.ts
    apps/server/src/games/race/figureEightTrack.test.ts
    apps/server/src/games/race/circleTrack.ts
    apps/server/src/games/race/figureEightTrackRules.ts
    apps/server/src/games/registry.ts
    apps/server/src/games/registry.test.ts
    packages/shared/src/game.ts
    packages/shared/src/lobby.ts
  </read_first>
  <action>
    Create `apps/server/src/games/race/figureEightTrack.ts` following the landed `createCircleTrackRuntime()` lifecycle pattern: timer injection, `countdownMs`, `laps`, `start()`, `applyInput()`, `removePlayer()`, and `dispose()`. Use `GAME_SESSION_STATUS`, `RACE_STATUS`, `PARTY_GAME_VARIANTS.figureEightTrack`, `RACE_GAME_INPUT_KIND`, `RACE_SHELL_MODE_IDS.figureEight`, `isRaceGameInput`, and `type FigureEightTrackSnapshot` from `@blitz/shared`. Initialize course and players through Plan 05-02 helpers. In `applyInput()`, return unchanged state unless status is racing, the player id exists in this runtime, `isRaceGameInput(input)` is true, `input.kind === RACE_GAME_INPUT_KIND.analog`, and `input.modeId === RACE_SHELL_MODE_IDS.figureEight`. Advance state from server elapsed time and server-owned rules only. Finish when all active players complete the configured laps. Build rankings by finish time, then lower penalty count, with labels like `42.0s - 1 penalty`. Add `results.summary` fields `mode: 'Figure Eight'`, `track: 'figure-eight-track'`, `laps`, `penaltiesByPlayer`, `centerCutsByPlayer`, and `finishedPlayers`. In `apps/server/src/games/registry.ts`, import `createFigureEightTrackRuntime` with `.js` extension and add a registry entry with key `race:figure-eight-track`, game `PARTY_GAMES.race` or literal `race`, variant `PARTY_GAME_VARIANTS.figureEightTrack`, and countdown 3. Do not use stale race-specific socket events, `useLiveRaceSocket` assumptions, client timestamps for scoring, player collision/traffic, or client-supplied lap/checkpoint/finish state.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/server test</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "export function createFigureEightTrackRuntime" apps/server/src/games/race/figureEightTrack.ts` prints one match. `grep -n "RACE_SHELL_MODE_IDS.figureEight" apps/server/src/games/race/figureEightTrack.ts` prints one match. `grep -n "PARTY_GAME_VARIANTS.figureEightTrack" apps/server/src/games/race/figureEightTrack.ts apps/server/src/games/registry.ts` prints matches in both files. `grep -n "race:figure-eight-track" apps/server/src/games/registry.ts apps/server/src/games/registry.test.ts` prints matches in both files. `grep -n "centerCutsByPlayer" apps/server/src/games/race/figureEightTrack.ts apps/server/src/games/race/figureEightTrack.test.ts` prints matches in both files. `pnpm --filter @blitz/server test` exits 0.
  </acceptance_criteria>
  <done>Figure-eight runs as a registered authoritative runtime through the existing game manager/session flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameManager.applyInput() -> figure-eight runtime | Mapped socket/player input reaches runtime state mutation. |
| runtime -> session finished payload | Server-owned results are emitted to clients and results page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | `createFigureEightTrackRuntime().applyInput()` | mitigate | Accept only `isRaceGameInput()`, analog kind, and `modeId === RACE_SHELL_MODE_IDS.figureEight`; never read lap/checkpoint/finish from input. |
| T-05-03-02 | Tampering | Registry variant resolution | mitigate | Register only `PARTY_GAME_VARIANTS.figureEightTrack` under `race:figure-eight-track`; tests cover wrong mode/variant ignore. |
| T-05-03-03 | Denial of Service | Runtime input loop and snapshot emission | mitigate | Do one bounded helper advancement per valid input and fixed player array updates only; no unbounded client payload processing. |
| T-05-03-04 | Elevation of Privilege | `GameManager.applyInput()` mapping | mitigate | Runtime ignores unknown player ids; manager remains the only socket-to-runtime entry point. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/server test` after each task.
</verification>

<success_criteria>
The server registry can resolve and run `race:figure-eight-track`, valid analog intent changes server-owned state, invalid/wrong input is ignored, and results explain laps plus crossing penalties.
</success_criteria>

<output>
After completion, create `.planning/phases/05-figure-eight-analog-track-race/05-03-SUMMARY.md`.
</output>
