---
phase: 05-figure-eight-analog-track-race
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/game.ts
  - packages/shared/src/lobby.ts
  - packages/shared/src/contracts.test.ts
autonomous: true
requirements:
  - ANLG-03
must_haves:
  truths:
    - "Figure-eight is a distinct race variant and shell mode recognized by shared contracts."
    - "Clients can emit only compact figure-eight analog intent, not authoritative lap/checkpoint/finish state."
    - "Phase 5 execution stops before implementation if Phase 4 circular analog artifacts are absent."
  artifacts:
    - path: "packages/shared/src/lobby.ts"
      provides: "PARTY_GAME_VARIANTS.figureEightTrack and race startability"
      contains: "figure-eight-track"
    - path: "packages/shared/src/game.ts"
      provides: "Figure-eight snapshot/player/course contracts or extensions to existing analog contracts"
      contains: "figure-eight"
    - path: "packages/shared/src/contracts.test.ts"
      provides: "Shared contract regression tests for figure-eight variant and input shape"
      contains: "figureEightTrack"
  key_links:
    - from: "packages/shared/src/lobby.ts"
      to: "apps/server/src/games/registry.ts"
      via: "shared PARTY_GAME_VARIANTS.figureEightTrack"
      pattern: "figure-eight-track"
    - from: "packages/shared/src/game.ts"
      to: "apps/server/src/games/race/figureEightTrack.ts"
      via: "isRaceGameInput + RACE_SHELL_MODE_IDS.figureEight"
      pattern: "RACE_SHELL_MODE_IDS.figureEight"
---

<objective>
Establish the prerequisite gate and shared contract surface for the figure-eight analog race.

Purpose: Phase 5 extends Phase 4 analog track work. Executors must not invent duplicate circular analog helpers if Phase 4 has not landed.
Output: A verified Phase 4 gate, shared figure-eight variant/startability, and contract tests for ANLG-03.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-figure-eight-analog-track-race/05-CONTEXT.md
@.planning/phases/05-figure-eight-analog-track-race/05-RESEARCH.md
@.planning/phases/05-figure-eight-analog-track-race/05-VALIDATION.md
@.planning/phases/05-figure-eight-analog-track-race/05-PATTERNS.md
@.planning/phases/04-circular-analog-track-race/04-CONTEXT.md
@.planning/phases/04-circular-analog-track-race/04-01-PLAN.md
@.planning/phases/04-circular-analog-track-race/04-02-PLAN.md
@.planning/phases/04-circular-analog-track-race/04-03-PLAN.md
@.planning/phases/04-circular-analog-track-race/04-04-PLAN.md
@.planning/phases/04-circular-analog-track-race/04-05-PLAN.md
@packages/shared/src/game.ts
@packages/shared/src/lobby.ts
@packages/shared/src/contracts.test.ts

<interfaces>
From `packages/shared/src/game.ts`:
```typescript
export const RACE_SHELL_MODE_IDS = {
  drag: 'drag',
  dodge: 'dodge',
  circle: 'circle',
  figureEight: 'figure-eight',
} as const;

export interface RaceAnalogInput extends RaceInputBase {
  kind: 'analog';
  vector: RaceAnalogVector;
}

export function clampRaceAnalogVector(vector: RaceAnalogVector): RaceAnalogVector;
export function isRaceGameInput(input: unknown): input is RaceGameInput;
```

From `packages/shared/src/lobby.ts`:
```typescript
export const PARTY_GAME_VARIANTS = {
  sprintCircuit: 'sprint-circuit',
  trafficSurvival: 'traffic-survival',
  dragSprint: 'drag-sprint',
} as const;

export function isLobbySelectionStartable(
  game: PartyGame,
  variant: PartyGameVariant,
  raceMode?: DragSprintMode | null,
  playerCount?: number | null,
): boolean;
```
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Add a distinct figure-eight analog map with readable crossing behavior. | Plans 01-05 cover contracts, rules, runtime, web screen, routing, results, and regression. |
| REQ | ANLG-03 | Listed in every Phase 5 plan frontmatter. |
| RESEARCH | Dedicated `figure-eight-track` variant, explicit ordered gates, server-authoritative runtime, no new dependencies. | Plans 01, 02, and 03. |
| RESEARCH | Fullscreen mobile page using Phase 1 shell and generic session event family. | Plans 04 and 05. |
| RESEARCH | Phase 4 source artifacts absent in current tree; block if still absent. | Plan 01 Task 1. |
| CONTEXT | D-01, D-02, D-03 crossing visual hierarchy and next-path cue. | Plan 04. |
| CONTEXT | D-04 no player collision/traffic at crossing. | Plans 02, 03, and 04 explicitly prohibit it. |
| CONTEXT | D-05, D-06, D-07, D-08 explicit ordered gates and HUD-ready state. | Plans 01, 02, and 03. |
| CONTEXT | D-09, D-10, D-11, D-12 warning-first penalties and visible feedback. | Plans 02, 03, and 04. |
| CONTEXT | D-13, D-14, D-15 forgiving but distinct figure-eight feel. | Plans 02, 04, and 05. |
| CONTEXT | D-16 circular analog tests still pass. | Plan 05 final regression gate. |
| CONTEXT deferred | Lobby catalog copy, broad results redesign, powerups, haptics, simulation-grade physics, player collisions. | Excluded from all plans. |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Gate Phase 5 on landed Phase 4 circular analog artifacts</name>
  <files>No files modified by this task</files>
  <read_first>
    .planning/phases/04-circular-analog-track-race/04-02-PLAN.md
    .planning/phases/04-circular-analog-track-race/04-03-PLAN.md
    .planning/phases/04-circular-analog-track-race/04-04-PLAN.md
    apps/server/src/games/race/circleTrackRules.ts
    apps/server/src/games/race/circleTrack.ts
    apps/web/src/game/circleTrackCanvas.ts
    apps/web/src/pages/CircleTrackPage.tsx
    packages/shared/src/lobby.ts
    packages/shared/src/game.ts
  </read_first>
  <action>
    Before editing shared contracts, verify Phase 4 landed or produced final equivalent artifacts. Check for `apps/server/src/games/race/circleTrackRules.ts`, `apps/server/src/games/race/circleTrack.ts`, `apps/web/src/game/circleTrackCanvas.ts`, `apps/web/src/pages/CircleTrackPage.tsx`, `PARTY_GAME_VARIANTS.circleTrack`, and `circle-track` registry/route support. If any artifact or equivalent is missing, stop this plan and return `CHECKPOINT: Phase 4 circular analog artifacts missing` with the exact missing paths/strings; do not create replacement circle helpers in Phase 5. This implements the phase prerequisite and preserves D-16.
  </action>
  <verify>
    <automated>test -f apps/server/src/games/race/circleTrackRules.ts && test -f apps/server/src/games/race/circleTrack.ts && test -f apps/web/src/game/circleTrackCanvas.ts && test -f apps/web/src/pages/CircleTrackPage.tsx</automated>
    <automated>grep -R "circle-track" -n packages/shared/src apps/server/src/games apps/web/src/lib apps/web/src/app apps/web/src/pages</automated>
  </verify>
  <acceptance_criteria>
    If Phase 4 is present, `grep -R "circle-track" -n packages/shared/src apps/server/src/games apps/web/src/lib apps/web/src/app apps/web/src/pages` prints matches in shared, server, and web source. If Phase 4 is absent, the executor stops before Task 2 and reports `CHECKPOINT: Phase 4 circular analog artifacts missing`.
  </acceptance_criteria>
  <done>Phase 5 proceeds only when Phase 4 analog source exists or final equivalent artifacts are identified from Phase 4 summaries and current source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add figure-eight shared contracts and tests</name>
  <files>packages/shared/src/game.ts, packages/shared/src/lobby.ts, packages/shared/src/contracts.test.ts</files>
  <read_first>
    packages/shared/src/game.ts
    packages/shared/src/lobby.ts
    packages/shared/src/contracts.test.ts
    .planning/phases/05-figure-eight-analog-track-race/05-CONTEXT.md
    .planning/phases/05-figure-eight-analog-track-race/05-PATTERNS.md
    apps/server/src/games/race/circleTrackRules.ts
  </read_first>
  <behavior>
    - `PARTY_GAME_VARIANTS.figureEightTrack` equals `figure-eight-track`.
    - `isLobbySelectionStartable('race', PARTY_GAME_VARIANTS.figureEightTrack)` returns true.
    - Valid analog input with `modeId: 'figure-eight'` passes `isRaceGameInput()`.
    - Figure-eight snapshot/player/course fields expose next gate, lap, direction cue, course state, crossing state, penalty count, center cut count, speed, and pose for HUD/tests.
    - No client input type includes lap, checkpoint, finish, rank, or authoritative position fields.
  </behavior>
  <action>
    Extend `packages/shared/src/lobby.ts` with `figureEightTrack: 'figure-eight-track'` in `PARTY_GAME_VARIANTS` and return true for that variant in `isLobbySelectionStartable()`. In `packages/shared/src/game.ts`, add dedicated figure-eight state types if Phase 4 did not already create reusable analog track contracts: `FigureEightTrackCourseState = 'on-course' | 'crossing' | 'wrong-lobe-warning' | 'wrong-way-warning' | 'center-cut-warning' | 'penalty' | 'finished'`, `FigureEightTrackGateState`, `FigureEightTrackPlayerState`, and `FigureEightTrackSnapshot`. These types must carry `trackId: 'figure-eight-track'`, `lapsTotal`, `currentLap`, `nextGateIndex`, `nextGateLabel`, `directionCue`, `expectedLobe`, `courseState`, `crossingState`, `penaltyCount`, `centerCutCount`, `speed`, `x`, `y`, and `angle`, while reusing existing `RaceShellSnapshot`, `RaceAnalogInput`, `isRaceGameInput()`, and `clampRaceAnalogVector()`. If Phase 4 already created a generic analog snapshot/player type, extend it for figure-eight instead of duplicating fields. Update `packages/shared/src/contracts.test.ts` to assert the variant, startability, valid `modeId: 'figure-eight'` analog input, shell snapshot compatibility, and absence of client-authored lap/checkpoint/finish authority. This implements D-05 through D-08 and preserves the server-authoritative contract required by D-16.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/shared test</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "figureEightTrack: 'figure-eight-track'" packages/shared/src/lobby.ts` prints one match. `grep -n "FigureEightTrackSnapshot" packages/shared/src/game.ts packages/shared/src/contracts.test.ts` prints matches in both files. `grep -n "modeId: 'figure-eight'" packages/shared/src/contracts.test.ts` prints one match. `grep -n "lap\\|checkpoint\\|finish" packages/shared/src/contracts.test.ts` is used only in assertions that these fields are server snapshot/result state, not client input authority. `pnpm --filter @blitz/shared test` exits 0.
  </acceptance_criteria>
  <done>Shared contracts recognize the figure-eight variant and expose HUD-ready authoritative state while client input remains compact analog intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client:game-input -> GameManager.applyInput() | Untrusted browser input enters authoritative server runtime by socket id. |
| shared variant/mode -> registry/runtime/page | Variant and mode identifiers route input and snapshots to the correct implementation. |
| runtime snapshot -> browser canvas/HUD | Server-owned race state becomes user-visible UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | `RaceGameInput` | mitigate | Keep client payloads to analog/button/action intent; do not add lap/checkpoint/finish fields to client input contracts. |
| T-05-01-02 | Tampering | `PARTY_GAME_VARIANTS.figureEightTrack` + `RACE_SHELL_MODE_IDS.figureEight` | mitigate | Add explicit shared constants/tests so wrong variants or wrong mode ids cannot be treated as figure-eight by later runtimes. |
| T-05-01-03 | Denial of Service | Shared input validation | mitigate | Reuse `isRaceGameInput()` and `clampRaceAnalogVector()` constant-time validation over fixed payload fields only. |
| T-05-01-04 | Elevation of Privilege | `GameManager.applyInput()` contract boundary | mitigate | Preserve the existing manager-owned player/session mapping; shared contracts define intent only and runtime plans perform authoritative validation. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/shared test` after Task 2. If Task 1 blocks, do not edit shared files.
</verification>

<success_criteria>
Phase 5 has a hard prerequisite gate and shared tests prove `figure-eight-track` can be selected while figure-eight input remains non-authoritative.
</success_criteria>

<output>
After completion, create `.planning/phases/05-figure-eight-analog-track-race/05-01-SUMMARY.md`.
</output>
