---
phase: 05-figure-eight-analog-track-race
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/server/src/games/race/figureEightTrackRules.ts
  - apps/server/src/games/race/figureEightTrackRules.test.ts
autonomous: true
requirements:
  - ANLG-03
must_haves:
  truths:
    - "Figure-eight progress advances only through an explicit ordered gate sequence."
    - "Center cuts, skipped lobes, and wrong-way movement do not advance lap progress."
    - "Crossing mistakes warn before applying recoverable slowdown/penalty state."
  artifacts:
    - path: "apps/server/src/games/race/figureEightTrackRules.ts"
      provides: "Pure figure-eight geometry, movement, gate, and penalty helpers"
      contains: "FIGURE_EIGHT_TRACK_ID"
    - path: "apps/server/src/games/race/figureEightTrackRules.test.ts"
      provides: "Deterministic gate/crossing/penalty rule coverage"
      contains: "rejects center cuts without advancing gate progress"
  key_links:
    - from: "apps/server/src/games/race/figureEightTrackRules.ts"
      to: "packages/shared/src/game.ts"
      via: "FigureEightTrackSnapshot/player contracts and clampRaceAnalogVector"
      pattern: "clampRaceAnalogVector"
---

<objective>
Build deterministic figure-eight track rules with explicit ordered gates and warning-first crossing penalties.

Purpose: Keep checkpoint/lap/crossing behavior testable and server-owned instead of embedding ambiguous logic in a runtime.
Output: Pure rule helpers and Node tests for ANLG-03 crossing validation.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-figure-eight-analog-track-race/05-CONTEXT.md
@.planning/phases/05-figure-eight-analog-track-race/05-RESEARCH.md
@.planning/phases/05-figure-eight-analog-track-race/05-PATTERNS.md
@.planning/phases/05-figure-eight-analog-track-race/05-01-SUMMARY.md
@packages/shared/src/game.ts
@apps/server/src/games/race/circleTrackRules.ts
@apps/server/src/games/race/circleTrackRules.test.ts

<interfaces>
From Plan 05-01 / `packages/shared/src/game.ts`:
```typescript
export type FigureEightTrackCourseState =
  | 'on-course'
  | 'crossing'
  | 'wrong-lobe-warning'
  | 'wrong-way-warning'
  | 'center-cut-warning'
  | 'penalty'
  | 'finished';

export interface FigureEightTrackPlayerState { /* server-owned lap/gate/pose/penalty fields */ }
export interface FigureEightTrackSnapshot extends Record<string, unknown> { /* authoritative snapshot */ }
export function clampRaceAnalogVector(vector: RaceAnalogVector): RaceAnalogVector;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write figure-eight rule tests first</name>
  <files>apps/server/src/games/race/figureEightTrackRules.test.ts</files>
  <read_first>
    apps/server/src/games/race/figureEightTrackRules.test.ts
    apps/server/src/games/race/circleTrackRules.test.ts
    apps/server/src/games/race/dragGearRules.test.ts
    .planning/phases/05-figure-eight-analog-track-race/05-CONTEXT.md
  </read_first>
  <behavior>
    - Ordered gates include lobe-specific gates and directional center-crossing gates.
    - A full clean sequence completes a lap only at the start/finish gate after every expected gate.
    - Center cuts and skipped lobes do not advance `nextGateIndex`.
    - Wrong lobe, wrong-way, and center-cut movement warn before penalty.
    - Analog vectors are clamped before motion updates.
  </behavior>
  <action>
    Create `apps/server/src/games/race/figureEightTrackRules.test.ts` using `node:test` and `node:assert/strict`. Add tests named exactly: `creates a broad readable figure eight with ordered lobe and crossing gates`, `advances through left lobe center right lobe center and start in order`, `rejects skipped lobes without completing lap progress`, `rejects center cuts without advancing gate progress`, `detects swept crossing gates without endpoint tunneling`, `warns before penalizing wrong lobe wrong way and center cuts`, and `clamps analog vectors before updating figure eight motion`. Target exported helpers `FIGURE_EIGHT_TRACK_ID`, `FIGURE_EIGHT_DEFAULTS`, `createFigureEightCourse`, `createFigureEightPlayer`, `detectFigureEightGateCrossing`, `classifyFigureEightCourseState`, and `advanceFigureEightPlayer`. Encode D-05 through D-12 directly in test names and assertions. The tests should fail before Task 2 because `figureEightTrackRules.ts` does not exist yet.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/server test</automated>
  </verify>
  <acceptance_criteria>
    `test -f apps/server/src/games/race/figureEightTrackRules.test.ts` exits 0. `grep -n "rejects center cuts without advancing gate progress" apps/server/src/games/race/figureEightTrackRules.test.ts` prints one match. `grep -n "detects swept crossing gates without endpoint tunneling" apps/server/src/games/race/figureEightTrackRules.test.ts` prints one match. `grep -n "warns before penalizing wrong lobe wrong way and center cuts" apps/server/src/games/race/figureEightTrackRules.test.ts` prints one match. The server package test command reaches RED before Task 2.
  </acceptance_criteria>
  <done>Rule tests describe the required crossing, gate order, lap, and penalty behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement constant-time figure-eight rule helpers</name>
  <files>apps/server/src/games/race/figureEightTrackRules.ts, apps/server/src/games/race/figureEightTrackRules.test.ts</files>
  <read_first>
    apps/server/src/games/race/figureEightTrackRules.ts
    apps/server/src/games/race/figureEightTrackRules.test.ts
    apps/server/src/games/race/circleTrackRules.ts
    packages/shared/src/game.ts
    .planning/phases/05-figure-eight-analog-track-race/05-CONTEXT.md
  </read_first>
  <action>
    Create `apps/server/src/games/race/figureEightTrackRules.ts`. Export `FIGURE_EIGHT_TRACK_ID = 'figure-eight-track'` and `FIGURE_EIGHT_DEFAULTS` with `laps: 3`, `width: 560`, `height: 380`, `roadWidth: 96`, `maxSpeed: 4.4`, `acceleration: 0.0026`, `drag: 0.0011`, `turnDamping: 0.2`, `wrongWayGraceMs: 1000`, `wrongLobeGraceMs: 700`, `centerCutGraceMs: 700`, and `penaltySpeedMultiplier: 0.55`. Export `createFigureEightCourse()`, `createFigureEightPlayer(playerId, nickname, index, laps = FIGURE_EIGHT_DEFAULTS.laps)`, `detectFigureEightGateCrossing(previousPose, nextPose, player, course)`, `classifyFigureEightCourseState(player, nextPose, course, nowMs)`, and `advanceFigureEightPlayer(player, vector, elapsedMs, course, nowMs)`. `createFigureEightCourse()` must return a broad two-lobe course with ordered gates labeled `Start`, `Left entry`, `Left apex`, `Center eastbound`, `Right entry`, `Right apex`, `Center westbound`, and `Final approach`; the two center gates must have distinct expected travel directions. `advanceFigureEightPlayer()` must clamp with `clampRaceAnalogVector`, update server-owned pose/speed from analog magnitude, classify expected lobe/path, warn quickly for wrong lobe, wrong-way, or center-cut states, apply the penalty speed multiplier only after the configured grace duration, keep mistakes recoverable, and advance laps only after the full gate sequence returns to `Start`. Do not add player-to-player collision, traffic, powerups, teleport reset, instant failure, nearest-projection-only validation, or unbounded loops over client-provided data.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/server test</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "export const FIGURE_EIGHT_TRACK_ID = 'figure-eight-track'" apps/server/src/games/race/figureEightTrackRules.ts` prints one match. `grep -n "Center eastbound\\|Center westbound" apps/server/src/games/race/figureEightTrackRules.ts` prints matches for both center gates. `grep -n "wrongWayGraceMs: 1000" apps/server/src/games/race/figureEightTrackRules.ts` prints one match. `grep -n "centerCutGraceMs: 700" apps/server/src/games/race/figureEightTrackRules.ts` prints one match. `grep -n "clampRaceAnalogVector" apps/server/src/games/race/figureEightTrackRules.ts` prints at least one match. `pnpm --filter @blitz/server test` exits 0.
  </acceptance_criteria>
  <done>Server rule helpers deterministically validate figure-eight crossing order and warning-first penalties with bounded per-input work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client:game-input -> figure-eight rules | Untrusted analog vector affects server-owned pose only after validation/clamping. |
| figure-eight rule helpers -> runtime snapshot | Pure helpers compute authoritative lap, checkpoint, speed, penalty, and finish state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | `advanceFigureEightPlayer()` | mitigate | Derive lap/checkpoint/finish exclusively from ordered server gates; ignore any client-authored authority fields. |
| T-05-02-02 | Tampering | `classifyFigureEightCourseState()` | mitigate | Require expected lobe and directional center gates so wrong variant/mode state cannot advance through figure-eight rules accidentally. |
| T-05-02-03 | Denial of Service | `detectFigureEightGateCrossing()` | mitigate | Use fixed-size gate arrays and constant-time swept segment checks; do not iterate over client-provided collections. |
| T-05-02-04 | Elevation of Privilege | Rule helper boundary | mitigate | Return copied server-owned player state only; later runtime remains called through `GameManager.applyInput()` for mapped players. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/server test` after each task. Preserve Phase 4 circle tests in the same package.
</verification>

<success_criteria>
Figure-eight geometry and checkpoint behavior are implemented as deterministic tested helpers, and circle helper tests still pass under the server package test command.
</success_criteria>

<output>
After completion, create `.planning/phases/05-figure-eight-analog-track-race/05-02-SUMMARY.md`.
</output>
