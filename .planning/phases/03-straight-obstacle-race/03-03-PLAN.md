---
phase: 03-straight-obstacle-race
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - apps/server/src/games/race/straightObstacle.ts
  - apps/server/src/games/race/straightObstacle.test.ts
  - apps/server/src/games/registry.ts
  - apps/server/src/games/registry.test.ts
autonomous: true
requirements:
  - DODGE-01
  - DODGE-02
  - DODGE-03
  - DODGE-05
user_setup: []
must_haves:
  truths:
    - "`race:straight-obstacle` resolves through the existing runtime registry."
    - "Runtime advances from a server tick using latest valid steering intent; neutral input does not stall distance, warnings, slowdown, or finish."
    - "Runtime accepts only straight-obstacle steer intent and ignores stale, non-finite, and client-claimed authoritative fields."
    - "Finished sessions emit rankings with finish time and obstacle hit details."
  artifacts:
    - path: "apps/server/src/games/race/straightObstacle.ts"
      provides: "Authoritative runtime adapter around pure straight-obstacle rules"
    - path: "apps/server/src/games/race/straightObstacle.test.ts"
      provides: "Runtime lifecycle, input validation, tick, finish, and result tests"
    - path: "apps/server/src/games/registry.ts"
      provides: "`race:straight-obstacle` runtime registration"
    - path: "apps/server/src/games/registry.test.ts"
      provides: "Registry coverage for straight-obstacle variant"
  key_links:
    - from: "apps/server/src/games/registry.ts"
      to: "apps/server/src/games/race/straightObstacle.ts"
      via: "createRuntime factory"
      pattern: "createStraightObstacleRuntime"
    - from: "apps/server/src/games/race/straightObstacle.ts"
      to: "apps/server/src/games/race/straightObstacleRules.ts"
      via: "rule helper imports"
      pattern: "advanceStraightObstacleRace"
---

<objective>
Register and run the authoritative straight-obstacle runtime through the existing session lifecycle.

Purpose: The pure rules become playable only when the server runtime accepts input, advances snapshots, emits state, and finishes through `server:session-finished`.
Output: `createStraightObstacleRuntime`, registry wiring, and focused runtime tests.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/03-straight-obstacle-race/03-CONTEXT.md
@.planning/phases/03-straight-obstacle-race/03-RESEARCH.md
@.planning/phases/03-straight-obstacle-race/03-VALIDATION.md
@.planning/phases/03-straight-obstacle-race/03-PATTERNS.md
@packages/shared/src/game.ts
@packages/shared/src/contracts.ts
@packages/shared/src/lobby.ts
@apps/server/src/games/runtime.ts
@apps/server/src/games/registry.ts
@apps/server/src/games/registry.test.ts
@apps/server/src/games/race/straightObstacleRules.ts
@apps/server/src/games/race/sprintCircuit.ts
@apps/server/src/games/race/dragSprint.ts

<interfaces>
Runtime interface:
```typescript
export interface GameRuntimeInstance<TState = unknown | null> {
  sessionId: string;
  lobbyCode: string;
  start(): GameSessionEnvelope<TState>;
  applyInput(playerId: string, input: GameInputPayload): GameSessionEnvelope<TState> | null;
  removePlayer(playerId: string): void;
  dispose(): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add straight-obstacle runtime and registry tests</name>
  <files>apps/server/src/games/race/straightObstacle.test.ts, apps/server/src/games/registry.test.ts</files>
  <read_first>
    - apps/server/src/games/race/straightObstacle.test.ts
    - apps/server/src/games/race/dragSprint.test.ts
    - apps/server/src/games/registry.test.ts
    - apps/server/src/games/runtime.ts
    - apps/server/src/games/registry.ts
    - apps/server/src/games/race/straightObstacleRules.ts
    - packages/shared/src/game.ts
    - packages/shared/src/lobby.ts
    - .planning/phases/03-straight-obstacle-race/03-VALIDATION.md
    - .planning/phases/03-straight-obstacle-race/03-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `createStraightObstacleRuntime(..., { countdownMs: 0, tickMs: 50 })` starts `variant: 'straight-obstacle'`, `status: 'active'`, `state.trackId: 'straight-obstacle'`, and player states with continuous `x`.
    - Test 2: Applying `{ mode: 'straight-obstacle', kind: 'steer', steerX: 99, sequence: 1, clientTimeMs: 1 }` stores clamped steering and later tick moves x no higher than `0.82`.
    - Test 3: Runtime ignores wrong mode, wrong kind, `steerX: Number.NaN`, stale `sequence`, and payloads containing client-claimed `speed`, `distance`, `obstacleHits`, `slowdownUntilMs`, `finishedAtMs`, or `rank`.
    - Test 4: Calling the injected tick callback advances distance and warning state even after steering returns to `0`.
    - Test 5: Runtime emits a single finished payload with `game: 'race'`, `variant: 'straight-obstacle'`, ranking labels ending in `hits`, and `details.obstacleHits`.
    - Test 6: Registry resolves `PARTY_GAMES.race` plus `PARTY_GAME_VARIANTS.straightObstacle` to key `race:straight-obstacle`.
  </behavior>
  <action>Create `apps/server/src/games/race/straightObstacle.test.ts` and extend `apps/server/src/games/registry.test.ts`. Use Node test runner with deterministic timer injection following existing runtime tests. Add runtime tests named exactly `starts straight obstacle runtime with authoritative snapshot`, `stores latest clamped steering intent and ignores stale malformed input`, `advances neutral steering through server tick`, and `finishes with obstacle hit result details`. Add registry assertion text containing `race:straight-obstacle`. Per T-03-01 through T-03-03, tests must explicitly cover out-of-range steer clamping, non-finite rejection, stale sequence ignore, and client-claimed server fields ignored.</action>
  <verify>
    <automated>pnpm --filter @blitz/server test -- src/games/race/straightObstacle.test.ts src/games/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/games/race/straightObstacle.test.ts` contains `starts straight obstacle runtime with authoritative snapshot`.
    - `apps/server/src/games/race/straightObstacle.test.ts` contains `stores latest clamped steering intent and ignores stale malformed input`.
    - `apps/server/src/games/race/straightObstacle.test.ts` contains `speed`.
    - `apps/server/src/games/race/straightObstacle.test.ts` contains `details.obstacleHits`.
    - `apps/server/src/games/registry.test.ts` contains `race:straight-obstacle`.
    - The first run of `pnpm --filter @blitz/server test -- src/games/race/straightObstacle.test.ts src/games/registry.test.ts` fails before Task 2 because runtime/registry code is not implemented.
  </acceptance_criteria>
  <done>Wave 0 runtime and registry tests exist and fail only because straight-obstacle runtime registration is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement straight-obstacle runtime and registry entry</name>
  <files>apps/server/src/games/race/straightObstacle.ts, apps/server/src/games/registry.ts</files>
  <read_first>
    - apps/server/src/games/race/straightObstacle.test.ts
    - apps/server/src/games/registry.test.ts
    - apps/server/src/games/race/straightObstacleRules.ts
    - apps/server/src/games/runtime.ts
    - apps/server/src/games/registry.ts
    - packages/shared/src/game.ts
    - packages/shared/src/contracts.ts
    - packages/shared/src/lobby.ts
    - .planning/phases/03-straight-obstacle-race/03-CONTEXT.md
    - .planning/phases/03-straight-obstacle-race/03-RESEARCH.md
  </read_first>
  <action>Create `apps/server/src/games/race/straightObstacle.ts` exporting `StraightObstacleRuntimeOptions` and `createStraightObstacleRuntime(lobby, sessionId, options)`. The runtime must use `RuntimeCallbacks<StraightObstacleSnapshot>`, `trackId: 'straight-obstacle'`, `countdownMs` default `3000`, `tickMs` default `50`, and injectable timers `{ setInterval, clearInterval, now }` matching existing test patterns. `start()` creates initial rule state from lobby players and emits a `GameSessionEnvelope<StraightObstacleSnapshot>` with `game: 'race'`, `variant: 'straight-obstacle'`, `status: GAME_SESSION_STATUS.active`, and `results: null` when countdown is zero; otherwise countdown then active. `applyInput(playerId, input)` must only accept `{ mode: 'straight-obstacle', kind: 'steer' }`, require finite non-negative `sequence` greater than the player's last accepted sequence, clamp finite `steerX` through `clampSteerX`, store latest intent by player id, and ignore wrong mode/kind, non-finite sequence, stale sequence, and client-supplied authoritative fields. A 50ms server tick must call `advanceStraightObstacleRace()` using latest intents, emit `onState`, and call `onFinished` once when all active players are finished or removed. `removePlayer()` should mark or remove the player's active participation consistently with existing runtime patterns; `dispose()` must clear interval timers. In `apps/server/src/games/registry.ts`, import `createStraightObstacleRuntime` and add a registry entry `{ key: 'race:straight-obstacle', game: 'race', variant: 'straight-obstacle', countdown: 3 }`. Keep `race:drag-sprint` and `race:sprint-circuit` entries unchanged.</action>
  <verify>
    <automated>pnpm --filter @blitz/server test -- src/games/race/straightObstacle.test.ts src/games/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/games/race/straightObstacle.ts` contains `export function createStraightObstacleRuntime`.
    - `apps/server/src/games/race/straightObstacle.ts` contains `tickMs = 50`.
    - `apps/server/src/games/race/straightObstacle.ts` contains `mode !== 'straight-obstacle'`.
    - `apps/server/src/games/race/straightObstacle.ts` contains `sequence <=`.
    - `apps/server/src/games/race/straightObstacle.ts` contains `clampSteerX`.
    - `apps/server/src/games/race/straightObstacle.ts` contains `onFinished`.
    - `apps/server/src/games/registry.ts` contains `race:straight-obstacle`.
    - `pnpm --filter @blitz/server test -- src/games/race/straightObstacle.test.ts src/games/registry.test.ts` exits 0.
  </acceptance_criteria>
  <done>`race:straight-obstacle` runs through the authoritative runtime registry, advances on server tick, validates input, and emits finished results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Socket.IO input->runtime | Untrusted `client:game-input` enters authoritative server runtime. |
| runtime->session broadcast | Server-owned snapshot/results are broadcast to all session clients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `createStraightObstacleRuntime.applyInput` | mitigate | Check `mode`/`kind`, require finite sequence, clamp finite `steerX` to `-1..1`, and ignore malformed steering intent. |
| T-03-02 | Tampering | `createStraightObstacleRuntime`, `advanceStraightObstacleRace` | mitigate | Runtime stores only steering intent; speed, distance, hits, slowdown, finish, ranking, and details are computed server-side. |
| T-03-03 | Denial of Service | latest intent map in `straightObstacle.ts` | mitigate | Store one latest valid intent per player and ignore stale/non-finite sequence values; server tick coalesces input bursts. |
| T-03-04 | Information Disclosure / Accessibility | snapshot warning fields | transfer | Runtime emits warning/slowdown/hit state for React HUD plan to render with live region. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/server test -- src/games/race/straightObstacle.test.ts src/games/registry.test.ts`.
</verification>

<success_criteria>
- `race:straight-obstacle` is registered and startable through the existing session manager.
- Runtime state remains authoritative and advances independently of client input frequency.
- Input validation/clamping/coalescing mitigates malformed, stale, and flooded steering packets.
- Finished payloads include obstacle hit result details.
</success_criteria>

<output>
After completion, create `.planning/phases/03-straight-obstacle-race/03-03-SUMMARY.md`
</output>
