---
phase: 03-straight-obstacle-race
plan: 02
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - apps/server/src/games/race/straightObstacleRules.ts
  - apps/server/src/games/race/straightObstacleRules.test.ts
autonomous: true
requirements:
  - DODGE-01
  - DODGE-02
  - DODGE-03
  - DODGE-05
user_setup: []
must_haves:
  truths:
    - "Continuous horizontal steering changes player `x` smoothly in normalized road coordinates."
    - "Seeded obstacle waves are readable, deterministic, and keep at least one escape path in early waves."
    - "Obstacle collisions use horizontal bounds plus distance bounds, apply immediate speed drop, increment hit count, and recover after 1250ms."
    - "Finish rankings and obstacle hit result details are derived from server-owned rule state."
  artifacts:
    - path: "apps/server/src/games/race/straightObstacleRules.ts"
      provides: "Pure steering, obstacle wave, collision, slowdown, finish, and ranking helpers"
    - path: "apps/server/src/games/race/straightObstacleRules.test.ts"
      provides: "Deterministic rule tests for DODGE mechanics"
  key_links:
    - from: "apps/server/src/games/race/straightObstacleRules.ts"
      to: "packages/shared/src/game.ts"
      via: "shared straight-obstacle types"
      pattern: "StraightObstacleSnapshot"
---

<objective>
Implement deterministic straight-obstacle race rules as pure helpers.

Purpose: DODGE-01 through DODGE-03 require playable steering, readable obstacles, and recoverable slowdown before a runtime adapter or route can be trusted. DODGE-05 requires server-derived finish/result detail helpers.
Output: `straightObstacleRules.ts` plus focused Node tests.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/03-straight-obstacle-race/03-CONTEXT.md
@.planning/phases/03-straight-obstacle-race/03-RESEARCH.md
@.planning/phases/03-straight-obstacle-race/03-VALIDATION.md
@.planning/phases/03-straight-obstacle-race/03-PATTERNS.md
@packages/shared/src/game.ts
@packages/shared/src/contracts.ts
@apps/server/src/games/race/dragSprint.test.ts
@apps/server/src/games/race/sprintCircuit.ts

<interfaces>
Expected shared exports from Plan 01:
```typescript
export interface StraightObstacleInput {
  mode: 'straight-obstacle';
  kind: 'steer';
  steerX: number;
  sequence: number;
  clientTimeMs: number;
}
export interface StraightObstacleSnapshot extends Record<string, unknown> {}
export interface StraightObstacleResultDetails {
  finishTimeMs: number | null;
  obstacleHits: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add straight-obstacle rule tests</name>
  <files>apps/server/src/games/race/straightObstacleRules.test.ts</files>
  <read_first>
    - apps/server/src/games/race/straightObstacleRules.test.ts
    - apps/server/src/games/race/dragSprint.test.ts
    - apps/server/src/games/race/sprintCircuit.ts
    - packages/shared/src/game.ts
    - .planning/phases/03-straight-obstacle-race/03-CONTEXT.md
    - .planning/phases/03-straight-obstacle-race/03-VALIDATION.md
    - .planning/phases/03-straight-obstacle-race/03-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `clampSteerX(-2) === -1`, `clampSteerX(2) === 1`, `clampSteerX(Number.NaN) === 0`, and `clampSteerX(Infinity) === 0`.
    - Test 2: `generateStraightObstacleWaves('seed-a', 900)` returns the same obstacle ids/centers every run, includes `warningDistance >= 140`, and the first two waves do not cover all horizontal escape space.
    - Test 3: `advanceStraightObstacleRace()` with `steerX: 1`, `deltaMs: 500`, and initial `x: 0` increases x without exceeding `0.82`.
    - Test 4: A player overlapping an obstacle receives `obstacleHits + 1`, immediate lower `speed`, `slowdownUntilMs === nowMs + 1250`, and the same obstacle id records the player in `hitPlayerIds`.
    - Test 5: Advancing past `slowdownUntilMs` recovers toward base speed and changes warning from `Hit - recovering` or `Slowdown` back to `Road clear` when no obstacle is in warning range.
    - Test 6: `buildStraightObstacleResults()` ranks lower finish time before higher finish time and includes `details.obstacleHits`.
  </behavior>
  <action>Create `apps/server/src/games/race/straightObstacleRules.test.ts` using `node:test` and `node:assert/strict`. Add tests named exactly `clamps steering intent and rejects non finite values`, `generates seeded readable obstacle waves`, `advances continuous horizontal steering within road bounds`, `applies obstacle hit slowdown and recovery timer`, `recovers speed after slowdown window`, and `builds finish rankings with obstacle hit details`. Use concrete constants in fixtures: distance target `900`, warning distance `150`, slowdown duration `1250`, player width `0.18`, obstacle width `0.24`, player depth `4`, obstacle depth `6`, and base speed `34`. Do not import or assert drag pickup, powerup, gear, RPM, lap, or checkpoint fields.</action>
  <verify>
    <automated>pnpm --filter @blitz/server test -- src/games/race/straightObstacleRules.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/games/race/straightObstacleRules.test.ts` contains `clamps steering intent and rejects non finite values`.
    - `apps/server/src/games/race/straightObstacleRules.test.ts` contains `generates seeded readable obstacle waves`.
    - `apps/server/src/games/race/straightObstacleRules.test.ts` contains `warningDistance >= 140`.
    - `apps/server/src/games/race/straightObstacleRules.test.ts` contains `1250`.
    - `apps/server/src/games/race/straightObstacleRules.test.ts` contains `details.obstacleHits`.
    - The first run of `pnpm --filter @blitz/server test -- src/games/race/straightObstacleRules.test.ts` fails before Task 2 because the helper file does not exist.
  </acceptance_criteria>
  <done>Wave 0 rule tests exist and fail only because deterministic straight-obstacle helpers are not implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic straight-obstacle rule helpers</name>
  <files>apps/server/src/games/race/straightObstacleRules.ts</files>
  <read_first>
    - apps/server/src/games/race/straightObstacleRules.test.ts
    - packages/shared/src/game.ts
    - apps/server/src/games/race/sprintCircuit.ts
    - apps/server/src/games/race/dragSprint.ts
    - .planning/phases/03-straight-obstacle-race/03-CONTEXT.md
    - .planning/phases/03-straight-obstacle-race/03-RESEARCH.md
    - .planning/phases/03-straight-obstacle-race/03-UI-SPEC.md
  </read_first>
  <action>Create `apps/server/src/games/race/straightObstacleRules.ts`. Export `STRAIGHT_OBSTACLE_TUNING` with exact values `{ distanceTarget: 900, baseSpeed: 34, maxSpeed: 46, minSpeed: 18, steeringSpeedPerSecond: 1.8, playerWidth: 0.18, playerDepth: 4, obstacleDepth: 6, warningDistance: 150, slowdownDurationMs: 1250, hitSpeedMultiplier: 0.58, recoverySpeedPerSecond: 14 }`. Export `clampSteerX(value: unknown): number`, `generateStraightObstacleWaves(seed: string, distanceTarget = STRAIGHT_OBSTACLE_TUNING.distanceTarget): StraightObstacleObstacle[]`, `createInitialStraightObstacleState(options)`, `advanceStraightObstacleRace(state, intents, frame)`, `detectObstacleHit(player, obstacle)`, and `buildStraightObstacleResults(state)`. Use normalized x coordinates from `-1` to `1`; clamp player center to `[-0.82, 0.82]` so a `0.18` wide car remains inside the road. Generate seeded readable waves from deterministic templates with centers such as `[-0.45, 0.35]`, `[0, 0.52]`, and `[-0.6, 0.05, 0.62]`, distances starting at `180` and spaced by at least `85`. Collision must use horizontal spans and distance spans per D-04. On first unhit collision per obstacle/player, increment `obstacleHits`, set `slowdownUntilMs` to `frame.nowMs + 1250`, multiply speed by `0.58` but not below `18`, and mark `warning` as `Hit - recovering`; repeated overlap with the same obstacle/player must not double-count. Recovery must increase speed by `14 * deltaSeconds` up to base/max speed after slowdown, and warning must become `Obstacle ahead` when the nearest unhit obstacle is within `150m` ahead. Finish when distance reaches `900`; set `finishedAtMs` from `frame.nowMs` and build rankings sorted by finish time, then distance. Return result entries with labels formatted like `12.3s · 2 hits`, `value` as finish time or rounded distance, and `details: { finishTimeMs, obstacleHits }`. Keep this module pure: no timers, Socket.IO, DOM, or mutable globals.</action>
  <verify>
    <automated>pnpm --filter @blitz/server test -- src/games/race/straightObstacleRules.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/games/race/straightObstacleRules.ts` contains `export const STRAIGHT_OBSTACLE_TUNING`.
    - `apps/server/src/games/race/straightObstacleRules.ts` contains `slowdownDurationMs: 1250`.
    - `apps/server/src/games/race/straightObstacleRules.ts` contains `export function clampSteerX`.
    - `apps/server/src/games/race/straightObstacleRules.ts` contains `export function generateStraightObstacleWaves`.
    - `apps/server/src/games/race/straightObstacleRules.ts` contains `export function advanceStraightObstacleRace`.
    - `apps/server/src/games/race/straightObstacleRules.ts` contains `export function detectObstacleHit`.
    - `apps/server/src/games/race/straightObstacleRules.ts` contains `export function buildStraightObstacleResults`.
    - `pnpm --filter @blitz/server test -- src/games/race/straightObstacleRules.test.ts` exits 0.
  </acceptance_criteria>
  <done>Pure rules deliver smooth steering, seeded readable waves, continuous collision bounds, slowdown/recovery, finish, and result details with passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted input->rule helpers | Runtime-provided steering intent reaches deterministic movement helpers. |
| rule state->result payload | Server-owned rule state becomes rankings and per-player result details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `clampSteerX`, `advanceStraightObstacleRace` | mitigate | Clamp finite `steerX` to `-1..1`; convert non-finite values to neutral `0`; tests cover out-of-range and non-finite inputs. |
| T-03-02 | Tampering | `advanceStraightObstacleRace`, `buildStraightObstacleResults` | mitigate | Compute distance, speed, hit count, slowdown, finish, ranking, and details from server-owned state only; helper APIs do not accept those values from client input. |
| T-03-03 | Denial of Service | pure rule helpers | transfer | Runtime plan handles stale/flooded sequence acceptance; rule helpers remain deterministic and side-effect free. |
| T-03-04 | Information Disclosure / Accessibility | warning state | transfer | Helpers emit warning text states; web HUD plan renders them as React text/live region. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/server test -- src/games/race/straightObstacleRules.test.ts`.
</verification>

<success_criteria>
- DODGE-01 has continuous steering math in normalized road coordinates.
- DODGE-02 has deterministic readable obstacle waves.
- DODGE-03 has speed drop, hit counting, and 1250ms slowdown/recovery.
- DODGE-05 has server-derived ranking and obstacle hit details.
</success_criteria>

<output>
After completion, create `.planning/phases/03-straight-obstacle-race/03-02-SUMMARY.md`
</output>
