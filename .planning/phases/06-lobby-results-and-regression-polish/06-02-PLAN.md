---
phase: 06-lobby-results-and-regression-polish
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - apps/web/src/pages/LobbyPage.tsx
  - apps/web/src/pages/LobbyPage.test.tsx
  - apps/web/src/styles.css
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-05, ARCH-03]
must_haves:
  truths:
    - "Host can choose Drag Gear, Straight Obstacle, Circle Track, and Figure-Eight Track from the lobby."
    - "Each race option shows objective, control, and skill focus copy from the UI spec."
    - "Semaforo and Rigori remain visible and startable first-level game choices."
    - "The selected race catalog card is the lobby focal point followed by the enabled Avvia Sessione CTA."
  artifacts:
    - path: "apps/web/src/pages/LobbyPage.tsx"
      provides: "Mobile-readable final race catalog and CTA flow"
      contains: "Drag Gear"
    - path: "apps/web/src/pages/LobbyPage.test.tsx"
      provides: "Web catalog rendering and selection coverage"
    - path: "apps/web/src/styles.css"
      provides: "Responsive catalog card styles and focus states"
  key_links:
    - from: "apps/web/src/pages/LobbyPage.tsx"
      to: "packages/shared/src/lobby.ts"
      via: "PARTY_GAME_VARIANTS canonical ids"
      pattern: "PARTY_GAME_VARIANTS\\.figureEightTrack"
    - from: "apps/web/src/pages/LobbyPage.tsx"
      to: "apps/web/src/lib/sessionRoutes.ts"
      via: "sessionStarted navigation using resolveSessionRoute()"
      pattern: "resolveSessionRoute\\(sessionStarted\\)"
---

<objective>
Replace the old two-button race selector with the final mobile-readable race catalog.

Purpose: Phase 6 owns the party flow clarity; players should understand how the four rebuilt race modes differ before launching a session.
Output: Lobby UI and tests for final race labels, objective/control/skill copy, selected-card focus, Semaforo/Rigori continuity, and `Avvia Sessione` gating.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lobby-results-and-regression-polish/06-CONTEXT.md
@.planning/phases/06-lobby-results-and-regression-polish/06-RESEARCH.md
@.planning/phases/06-lobby-results-and-regression-polish/06-PATTERNS.md
@.planning/phases/06-lobby-results-and-regression-polish/06-UI-SPEC.md
@apps/web/src/pages/LobbyPage.tsx
@apps/web/src/pages/LobbyPage.test.tsx
@apps/web/src/styles.css
@packages/shared/src/lobby.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add final race catalog rendering and selection tests</name>
  <files>apps/web/src/pages/LobbyPage.test.tsx</files>
  <read_first>
    .planning/phases/06-lobby-results-and-regression-polish/06-CONTEXT.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-UI-SPEC.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-PATTERNS.md;
    apps/web/src/pages/LobbyPage.test.tsx;
    apps/web/src/pages/LobbyPage.tsx;
    packages/shared/src/lobby.ts.
  </read_first>
  <behavior>
    - Lobby race catalog shows exactly these primary labels in order: `Drag Gear`, `Straight Obstacle`, `Circle Track`, `Figure-Eight Track`.
    - Catalog copy includes `Win the straight sprint with clean shifts.`, `Hold throttle, tap shift.`, `Shift timing`.
    - Catalog copy includes `Dodge hazards and keep speed.`, `Steer left/right.`, `Obstacle avoidance`.
    - Catalog copy includes `Complete laps through ordered gates.`, `Analog pad.`, `Smooth control`.
    - Catalog copy includes `Follow the crossing route in order.`, `Analog pad.`, `Route awareness`.
    - Clicking each race card calls `selectGame(PARTY_GAMES.race, PARTY_GAME_VARIANTS.<matchingFinalKey>)`.
    - Semaforo, Rigori, Corse, and `Avvia Sessione` remain visible.
  </behavior>
  <action>
    Update `LobbyPage.test.tsx` per D-01 through D-06, D-20, D-22, and the UI-SPEC copywriting contract before production changes. Replace assertions that treat `Sprint Circuit` or `Drag Sprint` as primary race catalog labels with assertions for the four final labels and exact objective/control/skill focus text. Keep tests that prove Semaforo and Rigori are visible. Add selected-state assertions using accessible button state (`aria-pressed` or equivalent visible selected class) and add an enabled `Avvia Sessione` scenario where all players are ready and the selected final variant is startable.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- LobbyPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    `apps/web/src/pages/LobbyPage.test.tsx` contains `Drag Gear`, `Straight Obstacle`, `Circle Track`, and `Figure-Eight Track`.
    `apps/web/src/pages/LobbyPage.test.tsx` contains every exact objective/control/skill focus string from `06-UI-SPEC.md`.
    `apps/web/src/pages/LobbyPage.test.tsx` contains assertions for `Semaforo`, `Rigori`, and `Avvia Sessione`.
    Focused web test fails before Task 2 and passes after Task 2.
  </acceptance_criteria>
  <done>Tests specify the final lobby catalog and protect non-race game visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Implement compact mobile-first race catalog cards</name>
  <files>apps/web/src/pages/LobbyPage.tsx, apps/web/src/styles.css</files>
  <read_first>
    AGENTS.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-CONTEXT.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-UI-SPEC.md;
    apps/web/src/pages/LobbyPage.tsx;
    apps/web/src/pages/LobbyPage.test.tsx;
    apps/web/src/styles.css;
    packages/shared/src/lobby.ts.
  </read_first>
  <action>
    Implement the catalog per D-01 through D-06 and the Phase 6 UI contract. Replace the local `RACE_VARIANTS` two-label array with four metadata rows in this exact order and copy: `Drag Gear` / `Win the straight sprint with clean shifts.` / `Hold throttle, tap shift.` / `Shift timing`; `Straight Obstacle` / `Dodge hazards and keep speed.` / `Steer left/right.` / `Obstacle avoidance`; `Circle Track` / `Complete laps through ordered gates.` / `Analog pad.` / `Smooth control`; `Figure-Eight Track` / `Follow the crossing route in order.` / `Analog pad.` / `Route awareness`. Render race options as selectable compact cards or rows with visible label, objective, `Control`, and `Skill Focus`. Set accessible selected state with `aria-pressed={joinedLobby.selectedVariant === variant.value}`. Preserve Semaforo, Rigori, and Corse as first-level game choices per D-05. Make the selected race card visually prominent, then keep the host `Avvia Sessione` CTA directly after the lobby grid/action area. Do not expose `Sprint Circuit` or `Drag Sprint` as primary lobby catalog copy per D-04. Update CSS with stable mobile dimensions: one-column race catalog on phones, two columns only when space allows, minimum 44px tap targets, no horizontal overflow, `#ffd700` focus/selected treatment, and readable 16px objective/control/skill text for new catalog copy.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- LobbyPage.test.tsx</automated>
    <automated>pnpm --filter @blitz/web test</automated>
  </verify>
  <acceptance_criteria>
    `apps/web/src/pages/LobbyPage.tsx` contains the exact labels `Drag Gear`, `Straight Obstacle`, `Circle Track`, and `Figure-Eight Track`.
    `apps/web/src/pages/LobbyPage.tsx` contains the exact UI-SPEC objective/control/skill focus strings.
    `apps/web/src/pages/LobbyPage.tsx` contains `aria-pressed`.
    `apps/web/src/pages/LobbyPage.tsx` does not contain `Sprint Circuit` or `Drag Sprint` as visible catalog labels.
    `apps/web/src/styles.css` contains `.race-catalog`, `.race-catalog-card`, and a `:focus-visible` rule using `#ffd700` or `var(--accent-3)`.
    `pnpm --filter @blitz/web test` passes.
  </acceptance_criteria>
  <done>The lobby presents all final race variants as distinct mobile-readable choices and keeps existing party game controls intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI -> server start | Race card disabled/selected state is browser-controlled and cannot be authoritative. |
| visible copy -> shared ids | User-facing final labels must map to canonical shared ids without stale label drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | `LobbyPage` race card selection | mitigate | UI uses `PARTY_GAME_VARIANTS` from shared; server-side startability and registry checks from Plan 01 remain authoritative. |
| T-06-05 | Information Disclosure | unavailable/disabled race card state | accept | No secrets are displayed; blocked state is gameplay availability only. |
| T-06-06 | Denial of Service | mobile text overflow blocking action buttons | mitigate | CSS acceptance requires one-column phone layout, 44px controls, no horizontal overflow, and readable fixed-size text. |
</threat_model>

<verification>
Use the focused web test after each task, then run the full web suite before completing the plan.
</verification>

<success_criteria>
The lobby shows the final four race modes with exact UI-SPEC copy, the selected card is accessible and visually clear, Semaforo/Rigori remain visible, and `Avvia Sessione` remains gated by shared startability.
</success_criteria>

<output>
After completion, create `.planning/phases/06-lobby-results-and-regression-polish/06-02-SUMMARY.md`.
</output>
