---
phase: 06-lobby-results-and-regression-polish
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - apps/web/src/lib/sessionRoutes.ts
  - apps/web/src/lib/sessionRoutes.test.ts
  - apps/web/src/app/router.tsx
  - apps/web/src/app/router.test.tsx
autonomous: true
requirements: [FLOW-03, FLOW-05, ARCH-03]
must_haves:
  truths:
    - "Starting Drag Gear routes to a fullscreen drag screen."
    - "Starting Straight Obstacle routes to a fullscreen dodge screen."
    - "Starting Circle Track routes to a fullscreen circle screen."
    - "Starting Figure-Eight Track routes to a fullscreen figure-eight screen."
    - "Race gameplay routes remain outside AppLayout chrome."
  artifacts:
    - path: "apps/web/src/lib/sessionRoutes.ts"
      provides: "Variant-aware session route resolver"
    - path: "apps/web/src/lib/sessionRoutes.test.ts"
      provides: "Direct route resolver coverage for final variants"
    - path: "apps/web/src/app/router.tsx"
      provides: "Fullscreen route registration"
    - path: "apps/web/src/app/router.test.tsx"
      provides: "No-chrome fullscreen route regression tests"
  key_links:
    - from: "apps/web/src/lib/sessionRoutes.ts"
      to: "apps/web/src/app/router.tsx"
      via: "resolved route path exists in route tree"
      pattern: "/race/figure-eight/:sessionId"
    - from: "apps/web/src/app/router.tsx"
      to: "apps/web/src/pages/*RacePage.tsx"
      via: "route element outside AppLayout"
      pattern: "path: '/race/figure-eight/:sessionId'"
---

<objective>
Make race session navigation variant-aware while preserving fullscreen gameplay routing.

Purpose: The old fallback sent most race sessions to the legacy live route; Phase 6 must open the correct fullscreen screen for each selected race variant.
Output: Route helper and router tests/implementation for all four final race variants.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lobby-results-and-regression-polish/06-CONTEXT.md
@.planning/phases/06-lobby-results-and-regression-polish/06-RESEARCH.md
@.planning/phases/06-lobby-results-and-regression-polish/06-PATTERNS.md
@.planning/phases/06-lobby-results-and-regression-polish/06-UI-SPEC.md
@apps/web/src/lib/sessionRoutes.ts
@apps/web/src/app/router.tsx
@apps/web/src/app/router.test.tsx
@apps/web/src/pages/DragGearRacePage.tsx
@apps/web/src/pages/SprintCircuitPage.tsx
@packages/shared/src/lobby.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add variant-aware route resolver and fullscreen route tests</name>
  <files>apps/web/src/lib/sessionRoutes.test.ts, apps/web/src/app/router.test.tsx</files>
  <read_first>
    .planning/phases/06-lobby-results-and-regression-polish/06-CONTEXT.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-RESEARCH.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-PATTERNS.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-UI-SPEC.md;
    apps/web/src/lib/sessionRoutes.ts;
    apps/web/src/app/router.test.tsx;
    apps/web/src/app/router.tsx;
    apps/web/src/pages/DragGearRacePage.test.tsx;
    packages/shared/src/lobby.ts.
  </read_first>
  <behavior>
    - `resolveSessionRoute()` maps `dragGear` to `/race/drag/:sessionId`.
    - `resolveSessionRoute()` maps `straightObstacle` to `/race/dodge/:sessionId`.
    - `resolveSessionRoute()` maps `circleTrack` to `/race/circle/:sessionId`.
    - `resolveSessionRoute()` maps `figureEightTrack` to `/race/figure-eight/:sessionId`.
    - Unsupported race variants do not fall through to a wrong gameplay screen.
    - Each final race route renders outside `.topbar`, `.viewport`, `.panel`, and `.card` app chrome.
  </behavior>
  <action>
    Create `apps/web/src/lib/sessionRoutes.test.ts` and extend `apps/web/src/app/router.test.tsx` per D-08, D-09, D-10, D-20, and the fullscreen gameplay regression contract. Tests must use `PARTY_GAME_VARIANTS` from `@blitz/shared`, not string literals, for final race variants. Add direct resolver assertions for the four final variants and one unsupported/stale race variant fallback assertion. Add router no-chrome checks for `/race/drag/session-1`, `/race/dodge/session-1`, `/race/circle/session-1`, and `/race/figure-eight/session-1`; each test must assert no `.topbar`, `.viewport`, `.panel`, or `.card` around gameplay. Mock existing session hooks as needed following current router test patterns.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- sessionRoutes.test.ts router.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    `apps/web/src/lib/sessionRoutes.test.ts` exists.
    `apps/web/src/lib/sessionRoutes.test.ts` contains `/race/drag/`, `/race/dodge/`, `/race/circle/`, and `/race/figure-eight/`.
    `apps/web/src/app/router.test.tsx` contains fullscreen no-chrome assertions for all four final race route paths.
    Focused route tests fail before Task 2 and pass after Task 2.
  </acceptance_criteria>
  <done>Route tests define final variant-to-screen behavior and protect fullscreen route isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement final variant session routing outside AppLayout</name>
  <files>apps/web/src/lib/sessionRoutes.ts, apps/web/src/app/router.tsx</files>
  <read_first>
    AGENTS.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-CONTEXT.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-PATTERNS.md;
    apps/web/src/lib/sessionRoutes.ts;
    apps/web/src/lib/sessionRoutes.test.ts;
    apps/web/src/app/router.tsx;
    apps/web/src/app/router.test.tsx;
    apps/web/src/pages/DragGearRacePage.tsx;
    all Phase 3-5 fullscreen race page files under apps/web/src/pages/.
  </read_first>
  <action>
    Update `resolveSessionRoute()` per D-08 through D-12 to import `PARTY_GAME_VARIANTS` and route canonical final variants exactly as follows: Drag Gear -> `/race/drag/${sessionId}`, Straight Obstacle -> `/race/dodge/${sessionId}`, Circle Track -> `/race/circle/${sessionId}`, Figure-Eight Track -> `/race/figure-eight/${sessionId}`. Keep lights and penalty routes unchanged. For unsupported/stale race variants, return `/results/${sessionId}` instead of silently opening the wrong race screen. Update `router.tsx` so the four final race paths exist outside the `AppLayout` branch. Use the fullscreen page components delivered by Phases 2-5. If a Phase 3-5 final race page is absent when this plan executes, stop and record a prerequisite blocker in the summary; do not add empty gameplay placeholders and do not route that variant to `SprintCircuitPage` unless the page has been made mode-aware for that exact variant.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- sessionRoutes.test.ts router.test.tsx</automated>
    <automated>pnpm --filter @blitz/web test</automated>
  </verify>
  <acceptance_criteria>
    `apps/web/src/lib/sessionRoutes.ts` imports `PARTY_GAME_VARIANTS`.
    `apps/web/src/lib/sessionRoutes.ts` contains `/race/drag/`, `/race/dodge/`, `/race/circle/`, and `/race/figure-eight/`.
    `apps/web/src/lib/sessionRoutes.ts` contains a race fallback to `/results/${payload.sessionId}` or equivalent result route for unsupported race variants.
    `apps/web/src/app/router.tsx` registers `/race/drag/:sessionId`, `/race/dodge/:sessionId`, `/race/circle/:sessionId`, and `/race/figure-eight/:sessionId` outside the `AppLayout` children array.
    `pnpm --filter @blitz/web test` passes.
  </acceptance_criteria>
  <done>Session start navigation opens the correct fullscreen game screen for every final race variant and never falls back to the wrong gameplay route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server session payload -> browser route | Browser receives selected variant from the server and maps it to a route. |
| route resolver -> fullscreen route tree | A route helper path must correspond to an actual no-chrome route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | `resolveSessionRoute()` stale race variant | mitigate | Unsupported race variants route to results/no-gameplay fallback instead of an incorrect race screen; server start checks remain authoritative. |
| T-06-08 | Denial of Service | missing route target | mitigate | Router tests require every resolved final route to render; missing Phase 3-5 pages become a prerequisite blocker, not a placeholder. |
| T-06-09 | Information Disclosure | route state | accept | Session ids/lobby codes are already part of existing route/session payloads; no new sensitive data is added. |
</threat_model>

<verification>
Focused route tests must pass first, followed by the full web suite.
</verification>

<success_criteria>
Every final race variant resolves to the correct fullscreen route, unsupported race variants do not open the wrong screen, and route tests prove gameplay pages remain outside normal app chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/06-lobby-results-and-regression-polish/06-03-SUMMARY.md`.
</output>
