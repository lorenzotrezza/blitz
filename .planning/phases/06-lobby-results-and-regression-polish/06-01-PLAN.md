---
phase: 06-lobby-results-and-regression-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/lobby.ts
  - packages/shared/src/contracts.test.ts
  - apps/server/src/games/registry.ts
  - apps/server/src/games/registry.test.ts
autonomous: true
requirements: [FLOW-01, FLOW-03, FLOW-05, ARCH-03]
must_haves:
  truths:
    - "Shared lobby policy exposes Drag Gear, Straight Obstacle, Circle Track, and Figure-Eight Track as the final race variants."
    - "Server registry resolves all four final race variants before any session can start."
    - "Semaforo and Rigori remain registered and startable."
  artifacts:
    - path: "packages/shared/src/lobby.ts"
      provides: "Canonical final race variant ids and shared startability policy"
      contains: "dragGear"
    - path: "apps/server/src/games/registry.ts"
      provides: "Runtime registry entries for the final race catalog"
      contains: "race:figure-eight-track"
    - path: "packages/shared/src/contracts.test.ts"
      provides: "Shared startability regression tests"
    - path: "apps/server/src/games/registry.test.ts"
      provides: "Server registry regression tests"
  key_links:
    - from: "packages/shared/src/lobby.ts"
      to: "apps/server/src/games/registry.ts"
      via: "PARTY_GAME_VARIANTS constants"
      pattern: "PARTY_GAME_VARIANTS\\.figureEightTrack"
    - from: "apps/server/src/games/registry.ts"
      to: "apps/server/src/socket/register.ts"
      via: "createGameRuntimeRegistry().resolve() used by startSession"
      pattern: "resolve\\(game, variant\\)"
---

<objective>
Create the canonical shared/server race catalog foundation for Phase 6.

Purpose: The lobby, runtime registry, session routing, HUD, and results must agree on one final identity per rebuilt race mode before UI polish or routing can be reliable.
Output: Shared final race variant constants/startability plus server registry coverage for all final variants.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/06-lobby-results-and-regression-polish/06-CONTEXT.md
@.planning/phases/06-lobby-results-and-regression-polish/06-RESEARCH.md
@.planning/phases/06-lobby-results-and-regression-polish/06-PATTERNS.md
@.planning/phases/06-lobby-results-and-regression-polish/06-VALIDATION.md
@packages/shared/src/lobby.ts
@packages/shared/src/contracts.test.ts
@apps/server/src/games/registry.ts
@apps/server/src/games/registry.test.ts
@apps/server/src/socket/register.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock final shared race variant startability</name>
  <files>packages/shared/src/lobby.ts, packages/shared/src/contracts.test.ts</files>
  <read_first>
    AGENTS.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-CONTEXT.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-RESEARCH.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-PATTERNS.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-UI-SPEC.md;
    packages/shared/src/lobby.ts;
    packages/shared/src/contracts.test.ts;
    .planning/phases/03-straight-obstacle-race/03-01-SUMMARY.md if present.
  </read_first>
  <behavior>
    - `isLobbySelectionStartable(PARTY_GAMES.race, PARTY_GAME_VARIANTS.dragGear, null, 3)` returns `true`.
    - `isLobbySelectionStartable(PARTY_GAMES.race, PARTY_GAME_VARIANTS.straightObstacle, null, 4)` returns `true`.
    - `isLobbySelectionStartable(PARTY_GAMES.race, PARTY_GAME_VARIANTS.circleTrack, null, 4)` returns `true`.
    - `isLobbySelectionStartable(PARTY_GAMES.race, PARTY_GAME_VARIANTS.figureEightTrack, null, 4)` returns `true`.
    - `isLobbySelectionStartable(PARTY_GAMES.race, 'stale-race' as PartyGameVariant, null, 4)` returns `false`.
    - `isLobbySelectionStartable(PARTY_GAMES.lights, null)` and `isLobbySelectionStartable(PARTY_GAMES.penalty, null)` remain `true`.
  </behavior>
  <action>
    Update `PARTY_GAME_VARIANTS` per D-01, D-03, D-04, D-07, and D-11 so the final canonical race keys are `dragGear: 'drag-gear'`, `straightObstacle: 'straight-obstacle'`, `circleTrack: 'circle-track'`, and `figureEightTrack: 'figure-eight-track'`. Keep any existing `dragSprint`, `sprintCircuit`, or `trafficSurvival` exports only if current source still needs compatibility aliases; do not use those aliases as the final startability examples. Update `isLobbySelectionStartable()` so non-race games remain startable, the four canonical final race variants are startable under their intended constraints, and unsupported/stale/null race variants remain unstartable. Add or update `contracts.test.ts` assertions before implementation and keep shared socket event tests unchanged per D-12.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/shared test</automated>
  </verify>
  <acceptance_criteria>
    `packages/shared/src/lobby.ts` contains `dragGear: 'drag-gear'`, `straightObstacle: 'straight-obstacle'`, `circleTrack: 'circle-track'`, and `figureEightTrack: 'figure-eight-track'`.
    `packages/shared/src/contracts.test.ts` contains assertions for `PARTY_GAME_VARIANTS.dragGear`, `PARTY_GAME_VARIANTS.straightObstacle`, `PARTY_GAME_VARIANTS.circleTrack`, and `PARTY_GAME_VARIANTS.figureEightTrack`.
    `packages/shared/src/contracts.test.ts` contains an unsupported/stale race variant assertion that expects `false`.
    `pnpm --filter @blitz/shared test` passes.
  </acceptance_criteria>
  <done>Shared startability has one final canonical identity per rebuilt race mode, old race labels are not the primary tested policy, and Semaforo/Rigori remain startable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register final race variants on the authoritative server</name>
  <files>apps/server/src/games/registry.ts, apps/server/src/games/registry.test.ts</files>
  <read_first>
    AGENTS.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-CONTEXT.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-RESEARCH.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-PATTERNS.md;
    packages/shared/src/lobby.ts;
    apps/server/src/games/registry.ts;
    apps/server/src/games/registry.test.ts;
    apps/server/src/socket/register.ts;
    apps/server/src/games/race/dragSprint.ts;
    all Phase 3-5 race runtime files that exist under apps/server/src/games/race/.
  </read_first>
  <behavior>
    - `createGameRuntimeRegistry().resolve(PARTY_GAMES.race, PARTY_GAME_VARIANTS.dragGear)` returns a runtime entry.
    - `createGameRuntimeRegistry().resolve(PARTY_GAMES.race, PARTY_GAME_VARIANTS.straightObstacle)` returns a runtime entry.
    - `createGameRuntimeRegistry().resolve(PARTY_GAMES.race, PARTY_GAME_VARIANTS.circleTrack)` returns a runtime entry.
    - `createGameRuntimeRegistry().resolve(PARTY_GAMES.race, PARTY_GAME_VARIANTS.figureEightTrack)` returns a runtime entry.
    - `resolve(PARTY_GAMES.lights, null)` and `resolve(PARTY_GAMES.penalty, null)` still return entries.
  </behavior>
  <action>
    Update `apps/server/src/games/registry.ts` per D-05, D-07, D-11, D-12, D-21, and the Phase 6 threat requirement. Import `PARTY_GAME_VARIANTS` and use shared constants for race registry entries instead of bare string literals. Add canonical entries with keys `race:drag-gear`, `race:straight-obstacle`, `race:circle-track`, and `race:figure-eight-track`. Map `dragGear` to the existing drag gear runtime. Map the other three variants to the runtime factories delivered by Phases 3-5. If any Phase 3-5 runtime factory is absent when this plan executes, stop and record a prerequisite blocker in the summary; do not create gameplay placeholders or route a final variant to the wrong runtime. Update `registry.test.ts` first so it fails until all final variants resolve, while preserving Semaforo and Rigori registry assertions.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/server test -- src/games/registry.test.ts</automated>
    <automated>pnpm --filter @blitz/server test</automated>
  </verify>
  <acceptance_criteria>
    `apps/server/src/games/registry.ts` imports `PARTY_GAME_VARIANTS` from `@blitz/shared`.
    `apps/server/src/games/registry.ts` contains `key: 'race:drag-gear'`, `key: 'race:straight-obstacle'`, `key: 'race:circle-track'`, and `key: 'race:figure-eight-track'`.
    `apps/server/src/games/registry.test.ts` resolves all four final race variants through `createGameRuntimeRegistry()`.
    `apps/server/src/games/registry.test.ts` still asserts `PARTY_GAMES.lights` and `PARTY_GAMES.penalty` resolve.
    `pnpm --filter @blitz/server test` passes.
  </acceptance_criteria>
  <done>The authoritative registry can resolve every final race variant and still resolves existing non-race minigames.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser lobby selection -> shared/server start policy | Client-controlled selected game/variant crosses into authoritative session start. |
| shared constants -> server registry | Final variant ids must not drift between packages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `isLobbySelectionStartable()` and `createGameRuntimeRegistry().resolve()` | mitigate | Server continues to require shared startability and registry resolution before `server:session-started`; tests cover final variants and stale unsupported variants. |
| T-06-02 | Denial of Service | missing Phase 3-5 runtime entry | mitigate | Stop with a prerequisite blocker instead of mapping a final variant to an incorrect runtime or placeholder. |
| T-06-03 | Elevation of Privilege | Semaforo/Rigori registry continuity | mitigate | Keep non-race registry/startability assertions so race catalog work does not remove existing startable games. |
</threat_model>

<verification>
Run focused shared and server tests after each task. This plan is complete when `pnpm --filter @blitz/shared test` and `pnpm --filter @blitz/server test` pass.
</verification>

<success_criteria>
The shared package exports canonical final race ids, shared startability accepts the four final modes and rejects stale race ids, and the server registry resolves the same four ids through authoritative runtime entries.
</success_criteria>

<output>
After completion, create `.planning/phases/06-lobby-results-and-regression-polish/06-01-SUMMARY.md`.
</output>
