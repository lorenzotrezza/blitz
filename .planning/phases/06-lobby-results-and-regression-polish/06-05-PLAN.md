---
phase: 06-lobby-results-and-regression-polish
plan: 05
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - apps/server/src/socket/register.test.ts
  - apps/web/src/pages/LobbyPage.test.tsx
  - apps/web/src/pages/ResultsPage.test.tsx
autonomous: false
requirements: [FLOW-01, FLOW-02, FLOW-03, FLOW-04, FLOW-05, ARCH-03, ARCH-04]
must_haves:
  truths:
    - "Unsupported or stale race variant ids cannot start a session server-side."
    - "Non-host players cannot trigger rematch, return-to-lobby, or change-game server-side."
    - "Root pnpm test and pnpm run build pass after all Phase 6 changes."
    - "Manual mobile viewport verification covers lobby catalog, fullscreen routing, results, and Semaforo/Rigori continuity."
  artifacts:
    - path: "apps/server/src/socket/register.test.ts"
      provides: "Authoritative socket security/regression tests"
    - path: "apps/web/src/pages/LobbyPage.test.tsx"
      provides: "Final catalog and non-race visibility web tests"
    - path: "apps/web/src/pages/ResultsPage.test.tsx"
      provides: "Results summary and post-game regression web tests"
  key_links:
    - from: "apps/server/src/socket/register.ts"
      to: "packages/shared/src/lobby.ts"
      via: "isLobbySelectionStartable() before session start"
      pattern: "isLobbySelectionStartable"
    - from: "apps/server/src/socket/register.ts"
      to: "apps/server/src/games/registry.ts"
      via: "gameRegistry.resolve() before session start"
      pattern: "gameRegistry\\.resolve"
    - from: "apps/web/src/pages/LobbyPage.tsx"
      to: "apps/web/src/pages/ResultsPage.tsx"
      via: "full party flow from selection to post-game actions"
      pattern: "Avvia Sessione"
---

<objective>
Close Phase 6 with security-focused regression tests and the required milestone gate.

Purpose: The final catalog touches shared lobby policy, server start, web routing, and results; the phase is not complete until whole-flow regressions and mobile viewport checks pass.
Output: Socket regression tests for stale variants/non-host actions, final root test/build evidence, and a blocking human mobile viewport verification checkpoint.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lobby-results-and-regression-polish/06-CONTEXT.md
@.planning/phases/06-lobby-results-and-regression-polish/06-RESEARCH.md
@.planning/phases/06-lobby-results-and-regression-polish/06-PATTERNS.md
@.planning/phases/06-lobby-results-and-regression-polish/06-VALIDATION.md
@.planning/phases/06-lobby-results-and-regression-polish/06-UI-SPEC.md
@apps/server/src/socket/register.ts
@apps/server/src/socket/register.test.ts
@apps/web/src/pages/LobbyPage.test.tsx
@apps/web/src/pages/ResultsPage.test.tsx
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add authoritative socket regression tests</name>
  <files>apps/server/src/socket/register.test.ts</files>
  <read_first>
    .planning/phases/06-lobby-results-and-regression-polish/06-CONTEXT.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-VALIDATION.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-PATTERNS.md;
    apps/server/src/socket/register.ts;
    apps/server/src/socket/register.test.ts;
    packages/shared/src/lobby.ts;
    apps/server/src/games/registry.ts.
  </read_first>
  <behavior>
    - Host selecting an unsupported or stale race variant receives `server:lobby-error` with code `game-not-supported` and no `server:session-started`.
    - A non-host triggering `rematch` receives `server:lobby-error` with code `player-not-host` and no `server:session-started`.
    - A non-host triggering `return-to-lobby` receives `server:lobby-error` with code `player-not-host` and no `server:post-game-updated`.
    - A non-host triggering `change-game` receives `server:lobby-error` with code `player-not-host` and no `server:post-game-updated`.
    - Existing host `rematch`, `return-to-lobby`, and `change-game` behavior stays covered.
  </behavior>
  <action>
    Extend `apps/server/src/socket/register.test.ts` per D-19, D-21, D-22, and the security requirement. Use the existing fake socket/room broadcast harness. Add one stale-variant start test that selects a race variant not present in final `PARTY_GAME_VARIANTS` by casting in the test payload, sets all players ready, triggers `client:start-session`, and asserts no session starts plus the `game-not-supported` lobby error. Add non-host tests for all three post-game actions: `rematch`, `return-to-lobby`, and `change-game`. Keep current host post-game tests; if host `change-game` is not explicitly tested yet, add it with the same waiting-lobby and `server:post-game-updated` expectations as return-to-lobby.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/server test -- src/socket/register.test.ts</automated>
    <automated>pnpm --filter @blitz/server test</automated>
  </verify>
  <acceptance_criteria>
    `apps/server/src/socket/register.test.ts` contains `game-not-supported` and `stale` or `unsupported` in a race start test name.
    `apps/server/src/socket/register.test.ts` contains non-host assertions for `rematch`, `return-to-lobby`, and `change-game`.
    `apps/server/src/socket/register.test.ts` contains `player-not-host`.
    `pnpm --filter @blitz/server test` passes.
  </acceptance_criteria>
  <done>Server tests cover stale race variant rejection and host-only post-game actions.</done>
</task>

<task type="auto">
  <name>Task 2: Run final automated Phase 6 gate</name>
  <files>apps/web/src/pages/LobbyPage.test.tsx, apps/web/src/pages/ResultsPage.test.tsx</files>
  <read_first>
    AGENTS.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-VALIDATION.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-UI-SPEC.md;
    package.json;
    apps/web/src/pages/LobbyPage.test.tsx;
    apps/web/src/pages/ResultsPage.test.tsx;
    apps/server/src/socket/register.test.ts.
  </read_first>
  <action>
    Run the final automated gate required by D-20 through D-23 and ARCH-04. First run focused tests for touched boundaries if Task 1 changed server tests or if prior plans left web tests unrun: `pnpm --filter @blitz/server test -- src/socket/register.test.ts`, `pnpm --filter @blitz/web test -- LobbyPage.test.tsx ResultsPage.test.tsx`, and any route test file added by Plan 03. Then run root `pnpm test` and root `pnpm run build`. If either root command fails, fix only Phase 6-caused failures in the files listed by the failing output; do not expand into reconnect identity, persistence, leaderboards, broad socket hardening, or new gameplay mechanics per D-25.
  </action>
  <verify>
    <automated>pnpm test</automated>
    <automated>pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    `pnpm test` passes.
    `pnpm run build` passes.
    Test output includes web coverage for lobby catalog, route resolution/fullscreen rendering, results summaries, and server socket coverage for stale variants and non-host post-game actions.
  </acceptance_criteria>
  <done>Root tests and production build pass after all Phase 6 plan outputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual mobile party-flow verification</name>
  <files>apps/web/src/pages/LobbyPage.tsx, apps/web/src/app/router.tsx, apps/web/src/pages/ResultsPage.tsx</files>
  <read_first>
    .planning/phases/06-lobby-results-and-regression-polish/06-UI-SPEC.md;
    .planning/phases/06-lobby-results-and-regression-polish/06-VALIDATION.md;
    apps/web/src/pages/LobbyPage.tsx;
    apps/web/src/app/router.tsx;
    apps/web/src/pages/ResultsPage.tsx.
  </read_first>
  <what-built>Final Phase 6 catalog, routing, results, and regression test gate.</what-built>
  <how-to-verify>
    1. Open the app at a 320px-wide phone viewport and create or join a lobby.
    2. Confirm Semaforo, Rigori, and Corse remain first-level game choices.
    3. Select Corse and confirm the race catalog stacks without horizontal overflow in this exact order: Drag Gear, Straight Obstacle, Circle Track, Figure-Eight Track.
    4. Confirm each card shows only the UI-SPEC objective, Control, and Skill Focus copy, and the selected card is visibly distinct.
    5. Confirm the primary CTA reads exactly `Avvia Sessione` and becomes enabled only when readiness and startability rules pass.
    6. Start each final race variant and confirm it opens the correct fullscreen route without topbar, normal viewport, panel, card chrome, or page scrolling.
    7. Finish or inject stored results for each mode and confirm rankings render, mode summaries render only when fields exist, and partial payloads fall back to rankings/no-data without broken summary text.
    8. Confirm host post-game actions `Rigioca`, `Torna alla Lobby`, and `Cambia Gioco` still work; confirm non-host sees waiting copy.
    9. Repeat the route/fullscreen check in a landscape phone viewport.
  </how-to-verify>
  <action>
    Pause for human verification after the automated gate. Record the user result in the plan summary. If issues are reported, fix only the Phase 6 party-flow issue described by the user and rerun `pnpm test` plus `pnpm run build` before asking for verification again.
  </action>
  <verify>
    <automated>pnpm test</automated>
    <automated>pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    Human response is `approved` or lists only non-blocking follow-up ideas.
    `pnpm test` passes after any fixes from the manual check.
    `pnpm run build` passes after any fixes from the manual check.
  </acceptance_criteria>
  <resume-signal>Type `approved` or describe the exact mobile viewport issue to fix.</resume-signal>
  <done>Manual mobile viewport party-flow verification is complete and the automated gate remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser selected variant -> socket startSession | Client may submit unsupported/stale race variants despite UI controls. |
| browser post-game action -> socket gateway | Non-host clients can attempt host-only next-step actions. |
| local verification -> milestone completion | Automated tests do not prove phone viewport readability or touch comfort. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | `client:start-session` with stale race variant | mitigate | Socket regression test proves server rejects unsupported variants through shared startability and registry resolution. |
| T-06-14 | Elevation of Privilege | `client:post-game-action` from non-host | mitigate | Socket regression tests cover non-host `rematch`, `return-to-lobby`, and `change-game` rejection. |
| T-06-15 | Denial of Service | phone viewport layout overflow blocks party flow | mitigate | Blocking manual viewport checkpoint at 320px and landscape phone viewport after automated gate. |
</threat_model>

<verification>
Final gate requires `pnpm test`, `pnpm run build`, and blocking manual mobile viewport verification.
</verification>

<success_criteria>
Server security regressions are covered, root automated gates pass, and the user verifies the final mobile party flow before Phase 6 is considered complete.
</success_criteria>

<output>
After completion, create `.planning/phases/06-lobby-results-and-regression-polish/06-05-SUMMARY.md`.
</output>
