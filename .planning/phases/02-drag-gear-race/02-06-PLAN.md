---
phase: 02-drag-gear-race
plan: 06
type: execute
wave: 5
depends_on:
  - 03
  - 05
files_modified:
  - apps/web/src/components/game/DragResultsSummary.tsx
  - apps/web/src/pages/ResultsPage.tsx
  - apps/web/src/pages/ResultsPage.test.tsx
autonomous: true
requirements:
  - DRAG-05
  - ARCH-02
user_setup: []
must_haves:
  truths:
    - "Results page visibly shows drag finish time and shift performance counts."
    - "Summary values come from server-finished payloads, not client-computed gameplay outcomes."
    - "Focused shared/server/web tests pass before full build and test gates."
  artifacts:
    - path: "apps/web/src/components/game/DragResultsSummary.tsx"
      provides: "Visible drag shift summary component"
    - path: "apps/web/src/pages/ResultsPage.tsx"
      provides: "Results page integration for drag summary"
    - path: "apps/web/src/pages/ResultsPage.test.tsx"
      provides: "Result summary assertions"
  key_links:
    - from: "apps/server/src/games/race/dragSprint.ts"
      to: "apps/web/src/pages/ResultsPage.tsx"
      via: "SessionFinishedPayload.results.summary"
      pattern: "perfectShifts"
    - from: "apps/web/src/pages/ResultsPage.tsx"
      to: "apps/web/src/components/game/DragResultsSummary.tsx"
      via: "conditional render for race/drag-sprint"
      pattern: "Shift Summary"
---

<objective>
Render drag-specific results and run the final Phase 2 verification gate.

Purpose: DRAG-05 requires finish time and shift performance summary. ARCH-02 requires deterministic rule/runtime tests, and AGENTS.md requires `pnpm run build` and `pnpm test` before completion.
Output: Drag results component, ResultsPage tests, and recorded verification.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-drag-gear-race/02-CONTEXT.md
@.planning/phases/02-drag-gear-race/02-RESEARCH.md
@.planning/phases/02-drag-gear-race/02-PATTERNS.md
@.planning/phases/02-drag-gear-race/02-UI-SPEC.md
@.planning/phases/02-drag-gear-race/02-VALIDATION.md
@packages/shared/src/contracts.ts
@packages/shared/src/game.ts
@apps/server/src/games/race/dragSprint.ts
@apps/web/src/pages/ResultsPage.tsx
@apps/web/src/pages/ResultsPage.test.tsx

<interfaces>
Server summary keys from Plan 03:
```typescript
summary: {
  mode: 'drag-gear';
  track: 'straight-drag-gear';
  distanceTargetM: number;
  finishTimeMs: number;
  perfectShifts: number;
  goodShifts: number;
  earlyShifts: number;
  lateShifts: number;
  totalShifts: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add drag results summary tests</name>
  <files>apps/web/src/pages/ResultsPage.test.tsx</files>
  <read_first>
    - apps/web/src/pages/ResultsPage.test.tsx
    - apps/web/src/pages/ResultsPage.tsx
    - packages/shared/src/contracts.ts
    - .planning/phases/02-drag-gear-race/02-UI-SPEC.md
    - .planning/phases/02-drag-gear-race/02-VALIDATION.md
    - .planning/phases/02-drag-gear-race/02-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: A `SessionFinishedPayload` with `game: 'race'`, `variant: 'drag-sprint'`, ranking label `13.420s`, and summary counts renders `Shift Summary`.
    - Test 2: The same payload renders visible text `Finish Time`, `13.420s`, `PERFECT`, `GOOD`, `EARLY`, `LATE`, `Total Shifts`, and exact count values.
    - Test 3: Existing host post-game buttons still render for the host.
    - Test 4: Non-drag results do not render `Shift Summary`.
  </behavior>
  <action>Extend `apps/web/src/pages/ResultsPage.test.tsx` with a drag payload fixture whose summary is `{ mode: 'drag-gear', track: 'straight-drag-gear', distanceTargetM: 402, finishTimeMs: 13420, perfectShifts: 2, goodShifts: 1, earlyShifts: 1, lateShifts: 0, totalShifts: 4 }`. Add tests named exactly `renders drag shift summary from finished payload` and `does not render drag shift summary for non-drag results`. Keep existing rankings and host action tests passing. Per D-15 and UI-SPEC, assert the visible heading is exactly `Shift Summary` and labels use exact uppercase `PERFECT`, `GOOD`, `EARLY`, and `LATE`.</action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- ResultsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/pages/ResultsPage.test.tsx` contains `renders drag shift summary from finished payload`.
    - `apps/web/src/pages/ResultsPage.test.tsx` contains `Shift Summary`.
    - `apps/web/src/pages/ResultsPage.test.tsx` contains `PERFECT`.
    - `apps/web/src/pages/ResultsPage.test.tsx` contains `Total Shifts`.
    - `apps/web/src/pages/ResultsPage.test.tsx` contains `does not render drag shift summary for non-drag results`.
    - The first run of `pnpm --filter @blitz/web test -- ResultsPage.test.tsx` fails before Task 2 because the summary component is not integrated.
  </acceptance_criteria>
  <done>Results tests require visible drag finish time and shift quality counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render drag shift summary on results page</name>
  <files>apps/web/src/components/game/DragResultsSummary.tsx, apps/web/src/pages/ResultsPage.tsx</files>
  <read_first>
    - apps/web/src/pages/ResultsPage.tsx
    - apps/web/src/pages/ResultsPage.test.tsx
    - packages/shared/src/contracts.ts
    - .planning/phases/02-drag-gear-race/02-UI-SPEC.md
    - .planning/phases/02-drag-gear-race/02-PATTERNS.md
  </read_first>
  <action>Create `apps/web/src/components/game/DragResultsSummary.tsx` exporting `DragResultsSummary({ summary, rankingLabel }: { summary: Record<string, string | number | boolean | null> | undefined; rankingLabel: string | number | boolean | null | undefined })`. Render nothing unless numeric summary keys `perfectShifts`, `goodShifts`, `earlyShifts`, `lateShifts`, and `totalShifts` exist. Render heading `Shift Summary`, visible label `Finish Time`, finish value from `rankingLabel` when present or `finishTimeMs / 1000` formatted with three decimals plus `s`, and rows for exact labels `PERFECT`, `GOOD`, `EARLY`, `LATE`, and `Total Shifts`. In `apps/web/src/pages/ResultsPage.tsx`, import `DragResultsSummary` and render it when `payload?.game === 'race' && payload.variant === 'drag-sprint'` immediately after rankings and before post-game actions. Preserve `normalizePayload`, roster display, and host post-game actions. Do not compute shift counts from client state; use only `payload.results.summary` per T-02-01.</action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- ResultsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/game/DragResultsSummary.tsx` contains `export function DragResultsSummary`.
    - `apps/web/src/components/game/DragResultsSummary.tsx` contains `Shift Summary`.
    - `apps/web/src/components/game/DragResultsSummary.tsx` contains `perfectShifts`.
    - `apps/web/src/pages/ResultsPage.tsx` contains `payload.variant === 'drag-sprint'`.
    - `apps/web/src/pages/ResultsPage.tsx` contains `DragResultsSummary`.
    - `pnpm --filter @blitz/web test -- ResultsPage.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Results page visibly renders server-provided drag finish time and shift summary counts.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 2 verification gate</name>
  <files>package.json, apps/server/package.json, apps/web/package.json, packages/shared/package.json</files>
  <read_first>
    - AGENTS.md
    - package.json
    - apps/server/package.json
    - apps/web/package.json
    - packages/shared/package.json
    - .planning/phases/02-drag-gear-race/02-VALIDATION.md
  </read_first>
  <action>Run the focused checks in this exact order: `pnpm --filter @blitz/shared test`, `pnpm --filter @blitz/server test -- src/games/race/dragGearRules.test.ts src/games/race/dragSprint.test.ts`, `pnpm --filter @blitz/web test -- DragGearRacePage.test.tsx ResultsPage.test.tsx`, then root `pnpm test`, then `pnpm run build`. If a failure is caused by Phase 2 changes, fix it in the touched Phase 2 files and rerun the failing command. If a failure is unrelated to Phase 2, record the exact command, exit status, and first relevant error lines in `.planning/phases/02-drag-gear-race/02-06-SUMMARY.md`. Do not mark Phase 2 complete until `pnpm run build` and `pnpm test` pass or the summary documents an unrelated blocker.</action>
  <verify>
    <automated>pnpm --filter @blitz/shared test</automated>
    <automated>pnpm --filter @blitz/server test -- src/games/race/dragGearRules.test.ts src/games/race/dragSprint.test.ts</automated>
    <automated>pnpm --filter @blitz/web test -- DragGearRacePage.test.tsx ResultsPage.test.tsx</automated>
    <automated>pnpm test</automated>
    <automated>pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @blitz/shared test` exits 0.
    - `pnpm --filter @blitz/server test -- src/games/race/dragGearRules.test.ts src/games/race/dragSprint.test.ts` exits 0.
    - `pnpm --filter @blitz/web test -- DragGearRacePage.test.tsx ResultsPage.test.tsx` exits 0.
    - `pnpm test` exits 0.
    - `pnpm run build` exits 0.
    - `.planning/phases/02-drag-gear-race/02-06-SUMMARY.md` records every verification command and result.
  </acceptance_criteria>
  <done>Focused and root verification commands have passed, or unrelated blockers are recorded with exact command evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server finished payload->results page | Server-derived summary becomes visible result text. |
| client route state/sessionStorage->results page | Stored browser payload is untrusted display input and must not affect authoritative race outcome. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `DragResultsSummary`, `ResultsPage` | mitigate | Render only server-provided `SessionFinishedPayload.results.summary`; do not calculate finish time, ranking, speed, RPM, or shift counts from client gameplay state. |
| T-02-02 | Denial of Service / Tampering | phase verification | mitigate | Run deterministic server tests proving malformed/repeated input guards still pass after UI/results integration. |
| T-02-03 | Tampering / Reliability | final web tests | mitigate | Run drag page tests that include throttle release lifecycle before final build/test gate. |
</threat_model>

<verification>
Run focused shared, server, and web checks, then `pnpm test` and `pnpm run build`.
</verification>

<success_criteria>
- Results page displays `Shift Summary`, finish time, and perfect/good/early/late/total shift counts.
- Existing rankings and host post-game actions keep working.
- Focused tests, root tests, and build pass before completion.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source Type | Item | Coverage |
|-------------|------|----------|
| GOAL | Replace drag race with a skill-based gear timing game on a straight map | Covered by Plans 01, 02, 03, 04, 05, and 06 |
| REQ | DRAG-01: Player can start a straight drag race and control acceleration | Covered by Plans 01, 02, 03, 04, and 05 |
| REQ | DRAG-02: Player can shift gears during the race | Covered by Plans 01, 02, 03, 04, and 05 |
| REQ | DRAG-03: Runtime scores shift timing and changes acceleration/result | Covered by Plans 01, 02, and 03 |
| REQ | DRAG-04: HUD shows RPM, gear, speed, distance, and last shift quality | Covered by Plans 01, 04, and 05 |
| REQ | DRAG-05: Results show finish time and shift performance summary | Covered by Plans 01, 03, and 06 |
| REQ | ARCH-02: Server race rules covered by deterministic tests | Covered by Plans 02, 03, and 06 |
| RESEARCH | Keep existing pnpm workspace and source roots | Covered by all plans; only `apps/**/src` and `packages/shared/src` are modified |
| RESEARCH | Server remains authoritative | Covered by Plans 01, 02, 03, 05, and 06 |
| RESEARCH | Use generic `client:game-input`, `server:session-state`, and `server:session-finished` | Covered by Plans 01, 03, and 05 |
| RESEARCH | Avoid stale race-specific events for new work | Covered by Plans 03 and 05 |
| RESEARCH | Extract deterministic helpers instead of adding more logic to large runtime file | Covered by Plan 02 and consumed by Plan 03 |
| RESEARCH | Phase 1 shell source may be absent | Covered by Plan 01 Wave 0 prerequisite gate |
| RESEARCH | Results summary must be visible, not hidden in generic JSON | Covered by Plan 06 |
| RESEARCH | Guard high-rate shift/throttle spam | Covered by Plans 02 and 03 |
| CONTEXT | D-01 visible, learnable shift window | Covered by Plans 01, 02, 04, and 05 |
| CONTEXT | D-02 shift outcomes early/good/perfect/late | Covered by Plans 01, 02, 03, 04, and 06 |
| CONTEXT | D-03 quality affects acceleration/result | Covered by Plans 02 and 03 |
| CONTEXT | D-04 forgiving but better timing clearly improves time | Covered by Plan 02 finish-order tests |
| CONTEXT | D-05 hold-to-accelerate plus separate shift | Covered by Plans 01, 03, 04, and 05 |
| CONTEXT | D-06 no steering | Covered by Plans 03, 04, and 05 |
| CONTEXT | D-07 Phase 1 two-thumb shell mapping | Covered by Plans 01, 04, and 05 |
| CONTEXT | D-08 desktop fallback held acceleration and discrete shift | Covered by Plan 05 through Phase 1 `useGameControls` integration |
| CONTEXT | D-09 12-18 second competent run target | Covered by Plan 02 tuning tests and Plan 03 runtime options |
| CONTEXT | D-10 four gears | Covered by Plans 01, 02, 03, and 04 |
| CONTEXT | D-11 one bad shift recoverable | Covered by Plan 02 finish-order tests |
| CONTEXT | D-12 manual timing matters in one/two player tests | Covered by Plans 02 and 03 ranking/finish tests |
| CONTEXT | D-13 HUD fields RPM, gear, speed, distance, last shift quality | Covered by Plans 01, 04, and 05 |
| CONTEXT | D-14 RPM and shift cue are mode-critical | Covered by Plan 04 |
| CONTEXT | D-15 results include finish time and shift summary | Covered by Plans 03 and 06 |
| CONTEXT | D-16 key status is React-rendered text | Covered by Plans 04, 05, and 06 |
| CONTEXT Deferred | Steering, obstacles, lane changes, slowdown recovery, obstacle counts | Excluded from Plans 03, 04, and 05 |
| CONTEXT Deferred | Analog steering, checkpoints, laps, off-track, wrong-way logic | Excluded from all Phase 2 plans |
| CONTEXT Deferred | Lobby catalog copy, mode cards, broad result polish | Excluded except required drag route mapping and drag summary display |
| CONTEXT Deferred | Sound, haptics, camera shake, advanced visual polish | Excluded from all Phase 2 plans |
| VALIDATION | T-02-01 forged speed/RPM/finish payload | Covered in every plan threat model; mitigated by intent-only input and server-derived state/results |
| VALIDATION | T-02-02 high-rate shift/throttle spam | Covered by Plans 02, 03, 05, and 06 |
| VALIDATION | T-02-03 stuck throttle after cancel/background/unmount | Covered by Plans 01, 04, 05, and 06 |
</source_audit>

<output>
After completion, create `.planning/phases/02-drag-gear-race/02-06-SUMMARY.md`
</output>
