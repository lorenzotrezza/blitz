---
phase: 02-drag-gear-race
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - apps/server/src/games/race/dragSprint.ts
  - apps/server/src/games/race/dragSprint.test.ts
  - apps/server/src/games/registry.ts
autonomous: true
requirements:
  - DRAG-01
  - DRAG-02
  - DRAG-03
  - DRAG-05
  - ARCH-02
user_setup: []
must_haves:
  truths:
    - "The existing `race:drag-sprint` runtime starts a straight gear-timing race."
    - "Runtime input accepts throttle hold/release and one-shot shift, with no steering, obstacles, pickups, survival, or best-of-3 behavior."
    - "Finished payloads include rankings, finish time, and shift summary counts."
  artifacts:
    - path: "apps/server/src/games/race/dragSprint.ts"
      provides: "Runtime adapter around pure drag gear rules"
    - path: "apps/server/src/games/race/dragSprint.test.ts"
      provides: "Session lifecycle tests for drag gear runtime"
    - path: "apps/server/src/games/registry.ts"
      provides: "Registry verification for `race:drag-sprint`"
  key_links:
    - from: "apps/server/src/games/registry.ts"
      to: "apps/server/src/games/race/dragSprint.ts"
      via: "createRuntime factory"
      pattern: "createDragSprintRuntime"
    - from: "apps/server/src/games/race/dragSprint.ts"
      to: "apps/server/src/games/race/dragGearRules.ts"
      via: "pure rule helper imports"
      pattern: "advanceDragGearPlayer"
---

<objective>
Replace the old drag sprint runtime behavior with the authoritative drag gear race while preserving the existing registry key and session lifecycle.

Purpose: DRAG-01, DRAG-02, DRAG-03, and DRAG-05 must work through the actual Socket.IO session runtime, not just isolated helpers. ARCH-02 requires runtime integration tests covering finish and ranking behavior.
Output: Rewritten `createDragSprintRuntime` plus focused runtime tests.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-drag-gear-race/02-CONTEXT.md
@.planning/phases/02-drag-gear-race/02-RESEARCH.md
@.planning/phases/02-drag-gear-race/02-PATTERNS.md
@.planning/phases/02-drag-gear-race/02-VALIDATION.md
@packages/shared/src/game.ts
@packages/shared/src/contracts.ts
@apps/server/src/games/race/dragGearRules.ts
@apps/server/src/games/race/dragGearRules.test.ts
@apps/server/src/games/race/dragSprint.ts
@apps/server/src/games/race/dragSprint.test.ts
@apps/server/src/games/runtime.ts
@apps/server/src/games/registry.ts

<interfaces>
Runtime interface:
```typescript
export interface GameRuntimeInstance<TState = unknown | null> {
  sessionId: string;
  lobbyCode: string;
  start(): GameSessionEnvelope<TState>;
  applyInput(playerId: string, input: GameInputPayload): GameSessionEnvelope<TState> | null;
  removePlayer(playerId: string): void;
  dispose(): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite drag sprint runtime tests for gear racing</name>
  <files>apps/server/src/games/race/dragSprint.test.ts</files>
  <read_first>
    - apps/server/src/games/race/dragSprint.test.ts
    - apps/server/src/games/race/dragSprint.ts
    - apps/server/src/games/race/dragGearRules.ts
    - packages/shared/src/game.ts
    - packages/shared/src/contracts.ts
    - .planning/phases/02-drag-gear-race/02-CONTEXT.md
    - .planning/phases/02-drag-gear-race/02-VALIDATION.md
    - .planning/phases/02-drag-gear-race/02-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `createDragSprintRuntime(..., { countdownMs: 0 })` starts with `variant: 'drag-sprint'`, `status: 'active'`, `state.status: RACE_STATUS.racing`, `distanceTargetM: 402`, and each player in gear 1 of 4.
    - Test 2: Applying `{ kind: 'drag-throttle', pressed: true, sequence: 1, clientTimeMs: 1 }` advances player speed/RPM/distance from server state.
    - Test 3: Applying `{ kind: 'drag-shift', sequence: 2, clientTimeMs: 2 }` near the perfect RPM window records `lastShiftQuality: 'perfect'`, increments `shiftSummary.perfect`, and advances gear.
    - Test 4: Runtime ignores steering/brake/lane/obstacle payloads and repeated shift sequence values.
    - Test 5: Runtime emits one `SessionFinishedPayload` with `results.rankings[0].label` ending in `s` and summary keys `finishTimeMs`, `perfectShifts`, `goodShifts`, `earlyShifts`, `lateShifts`, and `totalShifts`.
  </behavior>
  <action>Replace old lane, obstacle, pickup, survival, and best-of-3 tests in `apps/server/src/games/race/dragSprint.test.ts` with gear-race tests named exactly `starts a straight drag gear race from the existing registry variant`, `advances throttle and shift input through authoritative server state`, `ignores old steering repeated shift and non-racing input`, and `finishes with rankings and drag shift summary`. Keep the existing `createLobby()` fixture pattern with `selectedGame: 'race'`, `selectedVariant: 'drag-sprint'`, `LOBBY_STATUS.waiting`, and connected players. Use deterministic `now()` and `countdownMs: 0`. Per D-06, include an assertion that old steering input `{ steer: 1, accelerate: true, brake: false }` does not shift gear or create lane/obstacle state.</action>
  <verify>
    <automated>pnpm --filter @blitz/server test -- src/games/race/dragSprint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/games/race/dragSprint.test.ts` contains `starts a straight drag gear race from the existing registry variant`.
    - `apps/server/src/games/race/dragSprint.test.ts` contains `advances throttle and shift input through authoritative server state`.
    - `apps/server/src/games/race/dragSprint.test.ts` contains `ignores old steering repeated shift and non-racing input`.
    - `apps/server/src/games/race/dragSprint.test.ts` contains `finishes with rankings and drag shift summary`.
    - `apps/server/src/games/race/dragSprint.test.ts` does not contain `survival mode`.
    - The first run of `pnpm --filter @blitz/server test -- src/games/race/dragSprint.test.ts` fails before Task 2 because runtime behavior is still old.
  </acceptance_criteria>
  <done>Runtime tests target gear timing and remove old lane/obstacle/pickup/survival/best-of-3 assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace drag sprint runtime with gear adapter</name>
  <files>apps/server/src/games/race/dragSprint.ts, apps/server/src/games/registry.ts</files>
  <read_first>
    - apps/server/src/games/race/dragSprint.ts
    - apps/server/src/games/race/dragSprint.test.ts
    - apps/server/src/games/race/dragGearRules.ts
    - apps/server/src/games/runtime.ts
    - apps/server/src/games/registry.ts
    - packages/shared/src/game.ts
    - packages/shared/src/contracts.ts
    - .planning/phases/02-drag-gear-race/02-CONTEXT.md
    - .planning/phases/02-drag-gear-race/02-RESEARCH.md
  </read_first>
  <action>Rewrite `apps/server/src/games/race/dragSprint.ts` so `createDragSprintRuntime(lobby, sessionId, options)` remains the exported factory and returns `GameRuntimeInstance<DragGearSnapshot>`. Preserve options `countdownMs`, `distanceTarget`, `now`, `schedule`, `cancel`, `onState`, and `onFinished` for test harness compatibility, mapping `distanceTarget` to `distanceTargetM`. Initialize active connected lobby players through `createInitialDragGearPlayer` with `maxGear: 4` and `distanceTargetM`. On `start()`, emit countdown then activate race exactly like the existing runtime pattern. On `applyInput(playerId, input)`, return current state when not racing, player is unknown, or `readDragGearInput(input)` returns null; otherwise call `applyDragGearInput` and advance the server tick by elapsed server time from `now()`. Build snapshots with `trackId: 'straight-drag-gear'`, `status`, `tick`, `startedAt`, `countdown`, `distanceTargetM`, `shiftWindow`, and `playersState`. Finish when any player reaches `distanceTargetM`; set `results.rankings` from `buildDragGearRankings`; set `results.summary` to exact keys `mode: 'drag-gear'`, `track: 'straight-drag-gear'`, `distanceTargetM`, `finishTimeMs`, `perfectShifts`, `goodShifts`, `earlyShifts`, `lateShifts`, and `totalShifts` using the winning player summary. `removePlayer(playerId)` must release throttle for that player and finish only if remaining racing players have all finished. `dispose()` must cancel timers. Check `apps/server/src/games/registry.ts` and keep `race:drag-sprint` pointing to `createDragSprintRuntime`; update only if TypeScript requires the state type. Per D-06 and deferred scope, remove or stop emitting old `obstacles`, `pickups`, `lane`, `powerUps`, `survival`, and `best-of-3` runtime behavior for this variant.</action>
  <verify>
    <automated>pnpm --filter @blitz/server test -- src/games/race/dragGearRules.test.ts src/games/race/dragSprint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/games/race/dragSprint.ts` contains `GameRuntimeInstance<DragGearSnapshot>`.
    - `apps/server/src/games/race/dragSprint.ts` contains `trackId: 'straight-drag-gear'`.
    - `apps/server/src/games/race/dragSprint.ts` contains `buildDragGearRankings`.
    - `apps/server/src/games/race/dragSprint.ts` contains `perfectShifts`.
    - `apps/server/src/games/race/dragSprint.ts` contains `totalShifts`.
    - `apps/server/src/games/race/dragSprint.ts` does not contain `pickups`.
    - `apps/server/src/games/race/dragSprint.ts` does not contain `survival`.
    - `pnpm --filter @blitz/server test -- src/games/race/dragGearRules.test.ts src/games/race/dragSprint.test.ts` exits 0.
  </acceptance_criteria>
  <done>The active `race:drag-sprint` runtime is a server-authoritative drag gear race with tested finish and summary output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Socket.IO gateway->runtime | Untrusted `client:game-input` reaches `createDragSprintRuntime.applyInput`. |
| runtime->client snapshot/results | Authoritative server state is broadcast as session state and finished payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `apps/server/src/games/race/dragSprint.ts` | mitigate | Runtime derives speed, RPM, distance, finish time, rankings, and shift summary from server state and pure helpers; client payloads cannot set those fields. |
| T-02-02 | Denial of Service / Tampering | `applyInput` | mitigate | Runtime ignores malformed drag input, repeated sequence values, invalid gear transitions, unknown players, and inputs when not racing. |
| T-02-03 | Tampering / Reliability | `removePlayer`, `dispose` | mitigate | Player removal releases throttle and disposal cancels timers so server state cannot keep advancing a missing player indefinitely. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/server test -- src/games/race/dragGearRules.test.ts src/games/race/dragSprint.test.ts`.
</verification>

<success_criteria>
- `race:drag-sprint` still resolves through the registry.
- Runtime behavior is hold-throttle plus shift timing with no steering, obstacles, pickups, survival, or best-of-3 mechanics.
- Session finished payload includes visible result summary keys for finish time and shift counts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-drag-gear-race/02-03-SUMMARY.md`
</output>
