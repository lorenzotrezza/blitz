---
phase: 02-drag-gear-race
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 03
files_modified:
  - apps/web/src/components/game/DragGearHud.tsx
  - apps/web/src/components/game/RpmShiftMeter.tsx
  - apps/web/src/components/game/ShiftQualityBadge.tsx
  - apps/web/src/components/game/StraightDragTrack.tsx
  - apps/web/src/components/game/DragActionControls.tsx
  - apps/web/src/styles.css
autonomous: true
requirements:
  - DRAG-01
  - DRAG-02
  - DRAG-04
user_setup: []
must_haves:
  truths:
    - "Drag HUD renders RPM, gear, speed, distance, and last shift quality as DOM text."
    - "RPM meter shows a visible ideal shift window before the player shifts."
    - "Touch controls expose bottom-left hold throttle and bottom-right discrete shift without steering."
    - "Straight track visualization shows progress and finish line without obstacles, pickups, lanes, analog vectors, or checkpoints."
  artifacts:
    - path: "apps/web/src/components/game/DragGearHud.tsx"
      provides: "React text HUD for drag telemetry"
    - path: "apps/web/src/components/game/RpmShiftMeter.tsx"
      provides: "Visible RPM and ideal shift cue"
    - path: "apps/web/src/components/game/DragActionControls.tsx"
      provides: "Throttle hold and shift action control adapter"
    - path: "apps/web/src/components/game/StraightDragTrack.tsx"
      provides: "Straight drag track canvas"
  key_links:
    - from: "apps/web/src/components/game/DragGearHud.tsx"
      to: "packages/shared/src/game.ts"
      via: "DragGearSnapshot and DragGearPlayerState imports"
      pattern: "DragGearSnapshot"
    - from: "apps/web/src/components/game/DragActionControls.tsx"
      to: "apps/web/src/components/game/useGameControls.ts"
      via: "Phase 1 control hook"
      pattern: "emitAction('shift')"
---

<objective>
Build the reusable drag-specific UI components for HUD, RPM cue, straight track, and action controls.

Purpose: DRAG-01, DRAG-02, and DRAG-04 require visible throttle/shift controls and React-rendered gameplay telemetry on a fullscreen mobile screen. These components consume server snapshots and Phase 1 control primitives without creating a route yet.
Output: Drag UI components and gameplay CSS classes.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-drag-gear-race/02-CONTEXT.md
@.planning/phases/02-drag-gear-race/02-UI-SPEC.md
@.planning/phases/02-drag-gear-race/02-PATTERNS.md
@.planning/phases/02-drag-gear-race/02-VALIDATION.md
@packages/shared/src/game.ts
@apps/web/src/components/game/FullscreenGameShell.tsx
@apps/web/src/components/game/ActionButton.tsx
@apps/web/src/components/game/useGameControls.ts
@apps/web/src/components/RetroHud.tsx
@apps/web/src/components/RetroRaceView.tsx
@apps/web/src/styles.css

<interfaces>
Drag snapshot fields from Plan 01 and Plan 03:
```typescript
export interface DragGearPlayerState {
  gear: number;
  maxGear: number;
  rpm: number;
  speedKmh: number;
  distanceM: number;
  distanceTargetM: number;
  lastShiftQuality: DragShiftQuality | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create drag HUD and track display components</name>
  <files>apps/web/src/components/game/DragGearHud.tsx, apps/web/src/components/game/RpmShiftMeter.tsx, apps/web/src/components/game/ShiftQualityBadge.tsx, apps/web/src/components/game/StraightDragTrack.tsx</files>
  <read_first>
    - packages/shared/src/game.ts
    - apps/web/src/components/game/GameHud.tsx
    - apps/web/src/components/RetroHud.tsx
    - apps/web/src/components/RetroRaceView.tsx
    - apps/web/src/styles.css
    - .planning/phases/02-drag-gear-race/02-UI-SPEC.md
    - .planning/phases/02-drag-gear-race/02-PATTERNS.md
  </read_first>
  <action>Create `DragGearHud.tsx` exporting `DragGearHud({ snapshot, playerId }: { snapshot: DragGearSnapshot | null; playerId: string | null })`. Select the player by `playerId`, falling back to the first `playersState` entry. Render exact labels `Time the shift window`, `RPM`, `GEAR`, `KM/H`, `M`, and `LAST SHIFT`; gear format must be `GEAR 1/4`, `GEAR 2/4`, `GEAR 3/4`, or `GEAR 4/4`; distance format must be `${Math.round(distanceM)} / ${Math.round(distanceTargetM)} M`; no critical value may be canvas-only per D-16. Create `RpmShiftMeter.tsx` exporting `RpmShiftMeter({ rpm, window }: { rpm: number; window: DragShiftWindow })` with a stable root class `drag-rpm-meter`, visible text `RPM`, numeric rounded RPM, an element class `drag-rpm-meter__window` for the ideal window, and an element class `drag-rpm-meter__needle` for current RPM. Create `ShiftQualityBadge.tsx` exporting `ShiftQualityBadge({ quality }: { quality: DragShiftQuality | null })`; render text `--`, `EARLY`, `GOOD`, `PERFECT`, or `LATE` and class modifiers `is-early`, `is-good`, `is-perfect`, `is-late`. Create `StraightDragTrack.tsx` exporting `StraightDragTrack({ snapshot, playerId }: { snapshot: DragGearSnapshot | null; playerId: string | null })`; draw a canvas with `aria-label="Straight drag race track"`, width `720`, height `320`, distance markers, player/opponent progress, and finish line. In the canvas effect, use the existing jsdom guard pattern from `RetroRaceView` and do not draw steering lanes, obstacles, pickups, powerups, analog vectors, laps, or checkpoints.</action>
  <verify>
    <automated>pnpm --filter @blitz/web lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/game/DragGearHud.tsx` contains `Time the shift window`.
    - `apps/web/src/components/game/DragGearHud.tsx` contains `LAST SHIFT`.
    - `apps/web/src/components/game/RpmShiftMeter.tsx` contains `drag-rpm-meter__window`.
    - `apps/web/src/components/game/ShiftQualityBadge.tsx` contains `PERFECT`.
    - `apps/web/src/components/game/StraightDragTrack.tsx` contains `aria-label="Straight drag race track"`.
    - `apps/web/src/components/game/StraightDragTrack.tsx` does not contain `obstacle`.
  </acceptance_criteria>
  <done>Drag display components render the required telemetry, shift cue, feedback labels, and straight track surface.</done>
</task>

<task type="auto">
  <name>Task 2: Create drag action controls and CSS</name>
  <files>apps/web/src/components/game/DragActionControls.tsx, apps/web/src/styles.css</files>
  <read_first>
    - apps/web/src/components/game/ActionButton.tsx
    - apps/web/src/components/game/useGameControls.ts
    - apps/web/src/styles.css
    - packages/shared/src/game.ts
    - .planning/phases/02-drag-gear-race/02-UI-SPEC.md
    - .planning/phases/02-drag-gear-race/02-VALIDATION.md
  </read_first>
  <action>Create `DragActionControls.tsx` exporting `DragActionControls({ controls, disabled }: { controls: ReturnType<typeof useGameControls>; disabled?: boolean })`. Render exactly two accessible controls: bottom-left throttle with accessible name `THROTTLE`, text `Hold Throttle`, `aria-pressed={controls.primaryPressed}`, and `controls.setButtonState('primary', pressed)` for hold state; bottom-right shift with accessible name `SHIFT`, text `SHIFT`, and `controls.emitAction('shift')` for a discrete action. If Phase 1 `ActionButton` only emits press/release, wrap it so shift fires once on press and ignores hold repeats. Do not render analog pad, steering, brake, lane, or obstacle controls. Append CSS to `apps/web/src/styles.css` using only UI-SPEC typography sizes `12px`, `16px`, `20px`, and `28px` in new selectors. Add classes `.drag-gear-hud`, `.drag-rpm-meter`, `.drag-rpm-meter__window`, `.drag-rpm-meter__needle`, `.shift-quality-badge`, `.straight-drag-track`, `.drag-action-controls`, `.drag-action-controls__throttle`, `.drag-action-controls__shift`; set throttle minimum `112px` by `88px`, shift minimum `88px` square, RPM meter min-height `40px`, `touch-action: none`, `user-select: none`, `-webkit-user-select: none`, focus ring `outline: 3px solid #ffd700`, and stable layout so `EARLY`, `GOOD`, `PERFECT`, `LATE`, and `--` do not resize parent chips. Per T-02-03, rely on Phase 1 control lifecycle for pointer cancel, blur, visibility change, unmount, finish, and connection loss reset paths.</action>
  <verify>
    <automated>pnpm --filter @blitz/web lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/game/DragActionControls.tsx` contains `THROTTLE`.
    - `apps/web/src/components/game/DragActionControls.tsx` contains `Hold Throttle`.
    - `apps/web/src/components/game/DragActionControls.tsx` contains `emitAction('shift')`.
    - `apps/web/src/components/game/DragActionControls.tsx` does not contain `AnalogPad`.
    - `apps/web/src/styles.css` contains `.drag-rpm-meter__window`.
    - `apps/web/src/styles.css` contains `min-height: 40px`.
    - `apps/web/src/styles.css` contains `min-width: 112px`.
    - `apps/web/src/styles.css` contains `min-width: 88px`.
    - `pnpm --filter @blitz/web lint` exits 0.
  </acceptance_criteria>
  <done>Drag controls and styling match the two-thumb throttle/shift contract and compile under web TypeScript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server snapshot->React components | Authoritative state becomes DOM text and canvas visuals. |
| physical input->Phase 1 controls | Touch/keyboard interactions become local intent callbacks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `DragGearHud`, `StraightDragTrack` | mitigate | Components render server snapshot fields only and do not calculate finish time, ranking, speed, RPM, or shift quality locally. |
| T-02-02 | Denial of Service / Tampering | `DragActionControls` | mitigate | Shift is wired as a discrete action and throttle as a pressed/released state; server guards repeated or malformed inputs in Plans 02 and 03. |
| T-02-03 | Tampering / Reliability | `DragActionControls` | mitigate | Controls are built on Phase 1 `useGameControls`/`ActionButton` reset lifecycle and expose disabled state for finish/disconnect handling in Plan 05. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/web lint`.
</verification>

<success_criteria>
- Drag HUD, RPM meter, shift badge, straight track, and two action controls exist.
- UI copy matches the Phase 2 UI-SPEC exact labels.
- No steering, analog, obstacle, pickup, lap, or checkpoint affordances are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-drag-gear-race/02-04-SUMMARY.md`
</output>
