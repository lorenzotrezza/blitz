---
phase: 04-circular-analog-track-race
plan: 05
type: execute
wave: 4
depends_on:
  - 04-03
  - 04-04
files_modified:
  - apps/web/src/lib/sessionRoutes.ts
  - apps/web/src/lib/sessionRoutes.test.ts
  - apps/web/src/app/router.tsx
  - apps/web/src/app/router.test.tsx
  - apps/web/src/pages/ResultsPage.tsx
  - apps/web/src/pages/ResultsPage.test.tsx
autonomous: true
requirements:
  - ANLG-01
  - ANLG-02
  - ANLG-04
  - ANLG-05
must_haves:
  truths:
    - "Starting a circle-track session opens the Circle Track fullscreen route."
    - "Results explain Circle Track finish time, lap count, rank, and penalties."
    - "Full workspace tests and build pass after Phase 4 implementation."
  artifacts:
    - path: "apps/web/src/lib/sessionRoutes.ts"
      provides: "circle-track session route resolution"
    - path: "apps/web/src/app/router.tsx"
      provides: "top-level fullscreen Circle Track route outside AppLayout"
    - path: "apps/web/src/pages/ResultsPage.tsx"
      provides: "mode summary rendering for Circle Track results"
  key_links:
    - from: "apps/web/src/lib/sessionRoutes.ts"
      to: "apps/web/src/app/router.tsx"
      via: "/race/circle/:sessionId"
      pattern: "/race/circle/"
    - from: "apps/web/src/pages/ResultsPage.tsx"
      to: "apps/server/src/games/race/circleTrack.ts"
      via: "results.summary.mode/laps/penaltiesByPlayer"
      pattern: "Penalties"
---

<objective>
Wire Circle Track into session routing and result explanation, then run the phase gate.

Purpose: The new mode must be startable through the existing session flow and explain outcomes after finish.
Output: Route resolver, router branch, results summary rendering, route/results tests, and final verification commands.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-circular-analog-track-race/04-CONTEXT.md
@.planning/phases/04-circular-analog-track-race/04-UI-SPEC.md
@.planning/phases/04-circular-analog-track-race/04-RESEARCH.md
@.planning/phases/04-circular-analog-track-race/04-VALIDATION.md
@.planning/phases/04-circular-analog-track-race/04-PATTERNS.md
@.planning/phases/04-circular-analog-track-race/04-03-SUMMARY.md
@.planning/phases/04-circular-analog-track-race/04-04-SUMMARY.md
@packages/shared/src/lobby.ts
@packages/shared/src/contracts.ts
@apps/web/src/lib/sessionRoutes.ts
@apps/web/src/app/router.tsx
@apps/web/src/app/router.test.tsx
@apps/web/src/pages/CircleTrackPage.tsx
@apps/web/src/pages/ResultsPage.tsx
@apps/web/src/pages/ResultsPage.test.tsx

<interfaces>
From `apps/web/src/lib/sessionRoutes.ts`:
```typescript
export function resolveSessionRoute(payload: SessionStartedPayload): string;
```

From `packages/shared/src/contracts.ts`:
```typescript
export interface GameResults {
  rankings: GameResultEntry[];
  summary?: Record<string, unknown>;
}

export interface SessionFinishedPayload {
  sessionId: string;
  lobbyCode: string;
  game: PartyGame;
  variant: PartyGameVariant;
  results: GameResults;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Wave 0 route and result tests</name>
  <read_first>
    apps/web/src/lib/sessionRoutes.ts
    apps/web/src/lib/sessionRoutes.test.ts
    apps/web/src/app/router.tsx
    apps/web/src/app/router.test.tsx
    apps/web/src/pages/ResultsPage.tsx
    apps/web/src/pages/ResultsPage.test.tsx
    apps/web/src/pages/CircleTrackPage.tsx
    .planning/phases/04-circular-analog-track-race/04-UI-SPEC.md
    .planning/phases/04-circular-analog-track-race/04-VALIDATION.md
  </read_first>
  <files>apps/web/src/lib/sessionRoutes.test.ts, apps/web/src/app/router.test.tsx, apps/web/src/pages/ResultsPage.test.tsx</files>
  <action>
    Create `apps/web/src/lib/sessionRoutes.test.ts` if absent and add tests for `resolveSessionRoute({ game: 'race', variant: PARTY_GAME_VARIANTS.circleTrack, sessionId: 'session-1', lobbyCode: 'ABCD12', countdown: 3 }) === '/race/circle/session-1'` plus existing lights, penalty, and default race fallback behavior. Extend `apps/web/src/app/router.test.tsx` to mock `CircleTrackPage` dependencies and assert `/race/circle/session-1` renders `Fullscreen race session` outside `.topbar`, `.viewport`, `.panel`, and `.card`. Extend `apps/web/src/pages/ResultsPage.test.tsx` with a `SessionFinishedPayload` for `variant: PARTY_GAME_VARIANTS.circleTrack`, ranking label `42.0s · 1 penalty`, and `results.summary` containing `mode: 'Circle Track'`, `laps: 3`, `finishedPlayers: 1`, and `penaltiesByPlayer: { 'socket-host': 1 }`; assert visible text `Circle Track`, `Time`, `Laps`, and `Penalties`.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- sessionRoutes router ResultsPage</automated>
  </verify>
  <acceptance_criteria>
    `test -f apps/web/src/lib/sessionRoutes.test.ts` exits 0.
    `grep -n "/race/circle/session-1" apps/web/src/lib/sessionRoutes.test.ts apps/web/src/app/router.test.tsx` prints matches in both files.
    `grep -n "Circle Track" apps/web/src/pages/ResultsPage.test.tsx` prints one match.
    `grep -n "Penalties" apps/web/src/pages/ResultsPage.test.tsx` prints one match.
    The focused web test command reaches RED before Task 2 because routing/results implementation is missing.
  </acceptance_criteria>
  <done>Route and results tests pin the final session flow and Circle Track result explanation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Circle Track route and result summary rendering</name>
  <read_first>
    apps/web/src/lib/sessionRoutes.ts
    apps/web/src/lib/sessionRoutes.test.ts
    apps/web/src/app/router.tsx
    apps/web/src/app/router.test.tsx
    apps/web/src/pages/CircleTrackPage.tsx
    apps/web/src/pages/ResultsPage.tsx
    apps/web/src/pages/ResultsPage.test.tsx
    packages/shared/src/lobby.ts
    packages/shared/src/contracts.ts
  </read_first>
  <files>apps/web/src/lib/sessionRoutes.ts, apps/web/src/lib/sessionRoutes.test.ts, apps/web/src/app/router.tsx, apps/web/src/app/router.test.tsx, apps/web/src/pages/ResultsPage.tsx, apps/web/src/pages/ResultsPage.test.tsx</files>
  <behavior>
    - `circle-track` sessions resolve to the dedicated fullscreen Circle Track route.
    - The route remains outside `AppLayout` chrome like the Phase 1 live race route.
    - Results render mode-specific Circle Track summary fields from `results.summary`.
  </behavior>
  <action>
    In `apps/web/src/lib/sessionRoutes.ts`, import `PARTY_GAME_VARIANTS` and add a race branch: if `payload.game === 'race' && payload.variant === PARTY_GAME_VARIANTS.circleTrack`, return `/race/circle/${payload.sessionId}`. Keep lights, penalty, and existing fallback `/race/live/${payload.sessionId}` behavior intact. In `apps/web/src/app/router.tsx`, import `CircleTrackPage` and add a top-level route object `{ path: '/race/circle/:sessionId', element: <CircleTrackPage /> }` outside the `AppLayout` branch. In `apps/web/src/pages/ResultsPage.tsx`, add a small summary renderer for `payload.results.summary`: when `summary.mode === 'Circle Track'`, render labels `Circle Track`, `Time`, `Laps`, and `Penalties`; show the first ranking label as Time, `summary.laps` as Laps, and the current player penalty count from `summary.penaltiesByPlayer` when present. Keep existing rankings and post-game actions unchanged. Do not implement final lobby catalog cards or figure-eight result copy.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/web test -- sessionRoutes router ResultsPage</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "PARTY_GAME_VARIANTS.circleTrack" apps/web/src/lib/sessionRoutes.ts apps/web/src/lib/sessionRoutes.test.ts` prints matches in both files.
    `grep -n "CircleTrackPage" apps/web/src/app/router.tsx apps/web/src/app/router.test.tsx` prints matches in both files.
    `grep -n "path: '/race/circle/:sessionId'" apps/web/src/app/router.tsx` prints one match.
    `grep -n "Penalties" apps/web/src/pages/ResultsPage.tsx apps/web/src/pages/ResultsPage.test.tsx` prints matches in both files.
    `pnpm --filter @blitz/web test -- sessionRoutes router ResultsPage` exits 0.
  </acceptance_criteria>
  <done>Circle Track sessions route to the dedicated fullscreen page and results explain time/laps/penalties.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 4 verification gate</name>
  <read_first>
    package.json
    apps/server/package.json
    apps/web/package.json
    packages/shared/package.json
    .planning/phases/04-circular-analog-track-race/04-VALIDATION.md
  </read_first>
  <files>none</files>
  <action>
    Run the Phase 4 gate commands in order and record the results in the plan summary: `pnpm --filter @blitz/shared test`, `pnpm --filter @blitz/server test`, `pnpm --filter @blitz/web test`, `pnpm test`, and `pnpm run build`. If a command fails, stop, fix only Phase 4-related failures, and rerun the failed command plus any broader command it invalidates. Do not stage, commit, revert, or overwrite unrelated current worktree changes.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/shared test && pnpm --filter @blitz/server test && pnpm --filter @blitz/web test && pnpm test && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    The summary includes exact pass/fail lines for all five commands.
    `pnpm test` exits 0.
    `pnpm run build` exits 0.
  </acceptance_criteria>
  <done>Phase 4 automated gate has current evidence for shared, server, web, root test, and build success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session started payload -> browser route | Server-selected game/variant determines the frontend route. |
| finished payload -> results page | Server-emitted result summary is displayed to users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | route/result rendering | mitigate | Route and result pages display server payload data only; no route code accepts client analog vector state as authoritative. |
| T-04-02 | Denial of Service | route resolver/results renderer | accept | Work is constant-time over the existing small rankings/summary objects; no per-input work occurs in these files. |
| T-04-03 | Tampering | `ResultsPage` | mitigate | Render lap/penalty/finish fields from `SessionFinishedPayload.results.summary`; never accept those fields from client input. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/web test -- sessionRoutes router ResultsPage`, then `pnpm --filter @blitz/shared test`, `pnpm --filter @blitz/server test`, `pnpm --filter @blitz/web test`, `pnpm test`, and `pnpm run build`.
</verification>

<success_criteria>
Circle Track is routable from session start, results explain finish time/laps/penalties, all Phase 4 requirements are covered, and full workspace tests/build pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-circular-analog-track-race/04-05-SUMMARY.md`.
</output>
