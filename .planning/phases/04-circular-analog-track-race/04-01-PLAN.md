---
phase: 04-circular-analog-track-race
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/game.ts
  - packages/shared/src/lobby.ts
  - packages/shared/src/contracts.test.ts
autonomous: true
requirements:
  - ANLG-01
  - ANLG-02
  - ANLG-04
  - ANLG-05
must_haves:
  truths:
    - "Circle Track has a dedicated shared variant identity."
    - "Analog circle snapshots expose lap, gate, direction, speed, and penalty state."
    - "Client inputs remain analog intent only; authoritative race state is not accepted from clients."
  artifacts:
    - path: "packages/shared/src/game.ts"
      provides: "CircleTrackSnapshot, CircleTrackPlayerState, CircleTrackCourseState, and circle course/gate types"
    - path: "packages/shared/src/lobby.ts"
      provides: "PARTY_GAME_VARIANTS.circleTrack and race startability for circle-track"
    - path: "packages/shared/src/contracts.test.ts"
      provides: "contract coverage for circle variant, analog guard, and snapshot fields"
  key_links:
    - from: "packages/shared/src/lobby.ts"
      to: "apps/server/src/games/registry.ts"
      via: "PARTY_GAME_VARIANTS.circleTrack"
      pattern: "circleTrack: 'circle-track'"
    - from: "packages/shared/src/game.ts"
      to: "apps/server/src/games/race/circleTrack.ts"
      via: "CircleTrackSnapshot and isRaceGameInput"
      pattern: "interface CircleTrackSnapshot"
---

<objective>
Create the shared contract foundation for the circular analog race.

Purpose: Phase 4 needs an explicit `circle-track` identity and typed server-authoritative snapshot fields before server and web slices can implement against them.
Output: Shared variant, startability, circle snapshot/player/course types, and contract tests.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-circular-analog-track-race/04-CONTEXT.md
@.planning/phases/04-circular-analog-track-race/04-UI-SPEC.md
@.planning/phases/04-circular-analog-track-race/04-RESEARCH.md
@.planning/phases/04-circular-analog-track-race/04-VALIDATION.md
@.planning/phases/04-circular-analog-track-race/04-PATTERNS.md
@.planning/phases/01-fullscreen-game-shell-and-input-foundation/01-01-SUMMARY.md
@packages/shared/src/game.ts
@packages/shared/src/lobby.ts
@packages/shared/src/contracts.test.ts

<interfaces>
From `packages/shared/src/game.ts`:
```typescript
export const RACE_SHELL_MODE_IDS = {
  drag: 'drag',
  dodge: 'dodge',
  circle: 'circle',
  figureEight: 'figure-eight',
} as const;

export interface RaceAnalogVector {
  x: number;
  y: number;
  magnitude: number;
}

export interface RaceAnalogInput extends RaceInputBase {
  kind: 'analog';
  vector: RaceAnalogVector;
}

export interface RaceSnapshot extends Record<string, unknown> {
  sessionId: string;
  lobbyCode: string;
  trackId: string;
  status: RaceStatus;
  tick: number;
  startedAt: number | null;
  countdown: number | null;
  playersState: RacePlayerState[];
  botsState: RaceBotState[];
}

export function clampRaceAnalogVector(vector: RaceAnalogVector): RaceAnalogVector;
export function isRaceGameInput(input: unknown): input is RaceGameInput;
```

From `packages/shared/src/lobby.ts`:
```typescript
export const PARTY_GAME_VARIANTS = {
  sprintCircuit: 'sprint-circuit',
  trafficSurvival: 'traffic-survival',
  dragSprint: 'drag-sprint',
} as const;

export function isLobbySelectionStartable(
  game: PartyGame,
  variant: PartyGameVariant,
  raceMode: DragSprintMode | null = null,
  playerCount: number | null = null,
): boolean;
```
</interfaces>
</context>

<source_coverage_audit>
GOAL: Ship analog-pad control on a circular checkpoint/lap track. Covered by plans 04-01 through 04-05.
REQ ANLG-01: Covered by shared analog contracts in 04-01, runtime input in 04-03, and web controls in 04-04.
REQ ANLG-02: Covered by circle snapshot/course contracts in 04-01, deterministic gate/lap rules in 04-02, runtime in 04-03, and canvas route in 04-04.
REQ ANLG-04: Covered by course-state contracts in 04-01, off-track/wrong-way rules in 04-02, runtime penalties in 04-03, and HUD/canvas feedback in 04-04.
REQ ANLG-05: Covered by HUD-ready snapshot fields in 04-01, page HUD in 04-04, and results summary rendering in 04-05.
RESEARCH: Dedicated `race:circle-track`, pure rule helpers, generic session events, no new dependencies, and Wave 0 tests are covered by 04-01 through 04-05.
CONTEXT: D-01 through D-25 are mapped across task actions; figure-eight, final catalog polish, advanced physics, haptics, powerups, and player collisions are excluded because they are deferred in `04-CONTEXT.md`.
UI-SPEC: Fullscreen shell, `Circle Track` copy, one bottom-left analog pad, HUD fields, circle ring/gates/arrows, and result summary labels are covered by 04-04 and 04-05.
</source_coverage_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add Wave 0 shared contract tests for Circle Track</name>
  <read_first>
    .planning/phases/04-circular-analog-track-race/04-CONTEXT.md
    .planning/phases/04-circular-analog-track-race/04-VALIDATION.md
    packages/shared/src/game.ts
    packages/shared/src/lobby.ts
    packages/shared/src/contracts.test.ts
  </read_first>
  <files>packages/shared/src/contracts.test.ts</files>
  <action>
    Add failing contract tests before production changes. Tests must assert `PARTY_GAME_VARIANTS.circleTrack === 'circle-track'`; `isLobbySelectionStartable('race', PARTY_GAME_VARIANTS.circleTrack, null, 1) === true`; `isLobbySelectionStartable('race', null) === false`; a valid `CircleTrackSnapshot` fixture includes `trackId: 'circle-track'`, `lapsTotal: 3`, `checkpointsTotal: 4`, `direction: 'clockwise'`, `course.gates` with labels `Start`, `1`, `2`, `3`, `4`, and a player with `nextCheckpoint`, `courseState`, `penalties`, `input`, `finished`, and `finishTimeMs`. Add a guard test proving an analog input with `modeId: 'circle'` and extreme finite vector values passes `isRaceGameInput()` while malformed `kind` or invalid `modeId` is rejected. This implements D-01, D-20, and D-21 without accepting client lap/checkpoint/finish fields.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/shared test</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "circleTrack" packages/shared/src/contracts.test.ts` prints at least one match.
    `grep -n "CircleTrackSnapshot" packages/shared/src/contracts.test.ts` prints at least one match.
    `grep -n "trackId: 'circle-track'" packages/shared/src/contracts.test.ts` prints at least one match.
    The shared package test command reaches RED before Task 2 because the new exports do not exist yet.
  </acceptance_criteria>
  <done>Wave 0 tests describe the Circle Track shared contract and fail only because implementation is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Circle Track shared contracts and startability</name>
  <read_first>
    packages/shared/src/game.ts
    packages/shared/src/lobby.ts
    packages/shared/src/contracts.test.ts
    .planning/phases/04-circular-analog-track-race/04-PATTERNS.md
  </read_first>
  <files>packages/shared/src/game.ts, packages/shared/src/lobby.ts, packages/shared/src/contracts.test.ts</files>
  <behavior>
    - `circle-track` is a typed race variant and can start through existing lobby startability.
    - Circle snapshot types expose server-owned lap, checkpoint, direction, speed, off-track, wrong-way, penalty, and finish fields.
    - Analog input validation remains limited to player intent fields: `kind`, `modeId`, `sequence`, `clientTimeMs`, and clamped vector values.
  </behavior>
  <action>
    In `packages/shared/src/game.ts`, add `export type CircleTrackDirection = 'clockwise' | 'wrong-way'`; `export type CircleTrackCourseState = 'on-course' | 'off-track-warning' | 'off-track-penalty' | 'wrong-way-warning' | 'wrong-way-penalty'`; `export interface CircleTrackGateState`; `export interface CircleTrackCourse`; `export interface CircleTrackPlayerState extends RaceEntrantState`; and `export interface CircleTrackSnapshot extends Record<string, unknown>`. The snapshot must include `trackId: 'circle-track'`, `lapsTotal: number`, `checkpointsTotal: number`, `direction: CircleTrackDirection`, `course: CircleTrackCourse`, `playersState: CircleTrackPlayerState[]`, and `botsState: []`. The player state must include `input: RaceAnalogVector`, `nextCheckpoint: number`, `checkpointsTotal: number`, `lapsTotal: number`, `direction`, `courseState`, `penaltyReason: string | null`, `finished: boolean`, and `finishTimeMs: number | null`. In `packages/shared/src/lobby.ts`, add `circleTrack: 'circle-track'` to `PARTY_GAME_VARIANTS` and return true for `game === PARTY_GAMES.race` plus `variant === PARTY_GAME_VARIANTS.circleTrack`. Do not add figure-eight fields, lobby card copy, or broad catalog polish.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/shared test</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "export interface CircleTrackSnapshot" packages/shared/src/game.ts` prints one match.
    `grep -n "CircleTrackCourseState" packages/shared/src/game.ts` prints at least two matches.
    `grep -n "circleTrack: 'circle-track'" packages/shared/src/lobby.ts` prints one match.
    `grep -n "PARTY_GAME_VARIANTS.circleTrack" packages/shared/src/lobby.ts packages/shared/src/contracts.test.ts` prints matches in both files.
    `pnpm --filter @blitz/shared test` exits 0.
  </acceptance_criteria>
  <done>Shared package exports Circle Track contracts and startability with passing contract tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client input -> shared contract | Browser emits analog intent that the server must validate before applying. |
| shared contract -> server runtime | Shared types must not imply client authority over lap, checkpoint, penalty, position, or finish state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `packages/shared/src/game.ts` | mitigate | Preserve `isRaceGameInput()` validation for `kind`, `modeId`, `sequence`, `clientTimeMs`, finite vector values, and clamped analog vectors. |
| T-04-02 | Denial of Service | `RaceGameInput` payload shape | mitigate | Keep Circle Track input payload compact by reusing `RaceAnalogInput`; do not add client-sent path, lap, checkpoint, or finish objects. |
| T-04-03 | Tampering | `CircleTrackSnapshot` contract | mitigate | Snapshot fields describe server output only; tests must not create any client input shape containing lap/checkpoint/penalty/finish authority. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/shared test`.
</verification>

<success_criteria>
`circle-track` is startable as a race variant, Circle Track snapshot/player/course types exist, and shared tests prove analog intent stays separate from server-authoritative state.
</success_criteria>

<output>
After completion, create `.planning/phases/04-circular-analog-track-race/04-01-SUMMARY.md`.
</output>
