---
phase: 04-circular-analog-track-race
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/server/src/games/race/circleTrackRules.ts
  - apps/server/src/games/race/circleTrackRules.test.ts
autonomous: true
requirements:
  - ANLG-02
  - ANLG-04
must_haves:
  truths:
    - "Gate order is explicit and deterministic."
    - "Cardinal analog vectors map to the intended screen-space heading convention."
    - "Gate crossing is detected between previous and next poses so fast movement cannot tunnel through gates."
    - "A lap completes only after ordered gates and the start gate are crossed."
    - "Off-track and wrong-way behavior warn first, then apply recoverable penalties."
  artifacts:
    - path: "apps/server/src/games/race/circleTrackRules.ts"
      provides: "pure circle geometry, analog heading, segment/arc gate crossing, lap, course-state, and penalty helpers"
    - path: "apps/server/src/games/race/circleTrackRules.test.ts"
      provides: "deterministic coverage for cardinal analog vectors, gate crossing between ticks, laps, off-track, wrong-way, and clamping"
  key_links:
    - from: "apps/server/src/games/race/circleTrackRules.ts"
      to: "packages/shared/src/game.ts"
      via: "CircleTrackPlayerState and clampRaceAnalogVector"
      pattern: "clampRaceAnalogVector"
---

<objective>
Build and test the deterministic rule layer for the circular analog track.

Purpose: Phase 4 decisions require explicit gates and isolated helpers so checkpoint/lap/penalty behavior is provable outside the runtime shell.
Output: Pure rule helpers and Node tests for circle movement, gates, laps, off-track, and wrong-way penalties.
</objective>

<execution_context>
@/home/user/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-circular-analog-track-race/04-CONTEXT.md
@.planning/phases/04-circular-analog-track-race/04-RESEARCH.md
@.planning/phases/04-circular-analog-track-race/04-VALIDATION.md
@.planning/phases/04-circular-analog-track-race/04-PATTERNS.md
@.planning/phases/04-circular-analog-track-race/04-01-SUMMARY.md
@packages/shared/src/game.ts
@apps/server/src/games/race/sprintCircuit.ts
@apps/server/src/games/race/sprintCircuit.test.ts

<interfaces>
Use the Circle Track contracts created by Plan 04-01:
```typescript
export interface CircleTrackSnapshot extends Record<string, unknown>;
export interface CircleTrackPlayerState extends RaceEntrantState;
export type CircleTrackCourseState =
  | 'on-course'
  | 'off-track-warning'
  | 'off-track-penalty'
  | 'wrong-way-warning'
  | 'wrong-way-penalty';
export function clampRaceAnalogVector(vector: RaceAnalogVector): RaceAnalogVector;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Wave 0 circle rule tests</name>
  <read_first>
    apps/server/src/games/race/circleTrackRules.ts
    apps/server/src/games/race/circleTrackRules.test.ts
    apps/server/src/games/race/sprintCircuit.test.ts
    .planning/phases/04-circular-analog-track-race/04-VALIDATION.md
  </read_first>
  <files>apps/server/src/games/race/circleTrackRules.test.ts</files>
  <action>
    Create `apps/server/src/games/race/circleTrackRules.test.ts` using `node:test` and `node:assert/strict`. Add tests named exactly: `creates a readable clockwise circle course with four checkpoints`, `maps cardinal analog vectors to screen-space headings`, `detects gate crossing between previous and next pose without tunneling`, `advances gates in order and completes a lap at the start gate`, `rejects skipped gates without completing progress`, `classifies off-track warning before penalty`, `classifies wrong-way warning before penalty`, and `clamps analog motion before updating server state`. Tests should target exported helpers named `createCircleTrackCourse`, `createCircleTrackPlayer`, `vectorToHeadingRadians`, `detectCircleGateCrossing`, `advanceCircleTrackPlayer`, and `CIRCLE_TRACK_DEFAULTS`. For the cardinal-vector test, assert right `{ x: 1, y: 0 }`, down `{ x: 0, y: 1 }`, left `{ x: -1, y: 0 }`, and up `{ x: 0, y: -1 }` produce the documented screen-space headings used by motion and canvas pose output. For the gate-crossing test, construct a previous pose just before the expected gate and a next pose just after it, with neither endpoint exactly on the gate line, and assert the segment/arc helper reports the expected gate rather than `none`; this covers the RESEARCH.md gate-tunneling pitfall. Use the decisions D-06 through D-17: clockwise direction, start gate plus four checkpoints, three laps, broad road width, off-track grace, and wrong-way grace.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/server test</automated>
  </verify>
  <acceptance_criteria>
    `test -f apps/server/src/games/race/circleTrackRules.test.ts` exits 0.
    `grep -n "maps cardinal analog vectors to screen-space headings" apps/server/src/games/race/circleTrackRules.test.ts` prints one match.
    `grep -n "detects gate crossing between previous and next pose without tunneling" apps/server/src/games/race/circleTrackRules.test.ts` prints one match.
    `grep -n "advances gates in order and completes a lap at the start gate" apps/server/src/games/race/circleTrackRules.test.ts` prints one match.
    `grep -n "classifies wrong-way warning before penalty" apps/server/src/games/race/circleTrackRules.test.ts` prints one match.
    The server package test command reaches RED before Task 2 because `circleTrackRules.ts` does not exist yet.
  </acceptance_criteria>
  <done>Rule tests cover the missing validation references from `04-VALIDATION.md` and fail for missing implementation only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure circular track rules</name>
  <read_first>
    apps/server/src/games/race/circleTrackRules.ts
    apps/server/src/games/race/circleTrackRules.test.ts
    packages/shared/src/game.ts
    .planning/phases/04-circular-analog-track-race/04-CONTEXT.md
  </read_first>
  <files>apps/server/src/games/race/circleTrackRules.ts, apps/server/src/games/race/circleTrackRules.test.ts</files>
  <behavior>
    - Ordered gates advance only in sequence: Start, 1, 2, 3, 4, Start.
    - Analog heading uses one documented screen-space convention for right/down/left/up vectors.
    - Gate crossing evaluates the movement between previous and next poses, not only the final pose.
    - Off-track and wrong-way states have warning grace before penalty.
    - Analog input magnitude controls acceleration while release/coast slows the car.
    - Helper output is deterministic for fixed input, elapsed milliseconds, and course state.
  </behavior>
  <action>
    Create `apps/server/src/games/race/circleTrackRules.ts`. Export `CIRCLE_TRACK_ID = 'circle-track'` and `CIRCLE_TRACK_DEFAULTS` with `laps: 3`, `checkpoints: 4`, `width: 520`, `height: 360`, `roadWidth: 92`, `maxSpeed: 4.6`, `acceleration: 0.0028`, `drag: 0.0012`, `turnDamping: 0.18`, `offTrackGraceMs: 600`, and `wrongWayGraceMs: 1000`. Export `createCircleTrackCourse()`, `createCircleTrackPlayer(playerId, nickname, index, laps = CIRCLE_TRACK_DEFAULTS.laps)`, `vectorToHeadingRadians(vector)`, `detectCircleGateCrossing(previousPose, nextPose, progress, course)`, and `advanceCircleTrackPlayer(player, vector, elapsedMs, course, nowMs)`. `vectorToHeadingRadians()` must document and enforce the screen coordinate convention used everywhere in this mode: positive x points right, positive y points down, negative x points left, negative y points up. `detectCircleGateCrossing()` must evaluate the swept segment/arc from previous pose to next pose against the expected gate so a fast tick that crosses a checkpoint between endpoints still advances progress. `createCircleTrackCourse()` must return an oval/circle course with one `Start` gate and four ordered checkpoint gates. `advanceCircleTrackPlayer()` must clamp the vector with `clampRaceAnalogVector`, update velocity/position using magnitude as throttle, damp sudden heading changes, classify radial off-track state against the road ring, compute clockwise progress, detect wrong-way persistence from negative progress, advance explicit gate order through `detectCircleGateCrossing`, increment lap only when crossing Start after all four gates, cap/slow speed during persistent penalties, and return a full `CircleTrackPlayerState`. Do not implement figure-eight, player collisions, powerups, or simulation-grade physics.
  </action>
  <verify>
    <automated>pnpm --filter @blitz/server test</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "export const CIRCLE_TRACK_ID = 'circle-track'" apps/server/src/games/race/circleTrackRules.ts` prints one match.
    `grep -n "offTrackGraceMs: 600" apps/server/src/games/race/circleTrackRules.ts` prints one match.
    `grep -n "wrongWayGraceMs: 1000" apps/server/src/games/race/circleTrackRules.ts` prints one match.
    `grep -n "export function vectorToHeadingRadians" apps/server/src/games/race/circleTrackRules.ts` prints one match.
    `grep -n "export function detectCircleGateCrossing" apps/server/src/games/race/circleTrackRules.ts` prints one match.
    `grep -n "clampRaceAnalogVector" apps/server/src/games/race/circleTrackRules.ts` prints at least one match.
    `pnpm --filter @blitz/server test` exits 0.
  </acceptance_criteria>
  <done>Pure circle rules are implemented, deterministic, tested, and isolated from runtime lifecycle code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime input -> rule helpers | Untrusted analog vectors enter deterministic server rules. |
| rule helpers -> snapshots/results | Helpers derive authoritative position, lap, gate, penalty, and finish state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `advanceCircleTrackPlayer()` | mitigate | Clamp vectors with `clampRaceAnalogVector`, ignore non-finite values through shared guard upstream, and compute movement server-side. |
| T-04-02 | Denial of Service | `advanceCircleTrackPlayer()` | mitigate | Keep per-input work bounded to constant-time oval/gate math; do not iterate over unbounded client data. |
| T-04-03 | Tampering | gate/lap/penalty helpers | mitigate | Derive `nextCheckpoint`, `lap`, `courseState`, `penalties`, and `finished` only from prior server state plus explicit gate crossings. |
</threat_model>

<verification>
Run `pnpm --filter @blitz/server test`.
</verification>

<success_criteria>
Circle rules prove ordered checkpoints, multi-lap completion, off-track recovery, wrong-way recovery, and analog clamping with deterministic server tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-circular-analog-track-race/04-02-SUMMARY.md`.
</output>
